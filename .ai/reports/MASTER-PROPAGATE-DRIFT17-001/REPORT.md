# `MASTER-PROPAGATE-DRIFT17-001` REPORT — 셋 중 둘만 닫았다 → ★**T2 재투입으로 셋 다 닫혔다**

> 집행 = cli · cwd = `~/AndroidStudioProjects/claude-cli-master` · 2026-08-30 KST · Mode M5 (cli-infra-ops)
> 발주 = `~/AndroidStudioProjects/cc-paste-MASTER-PROPAGATE-DRIFT17-001.md` (cowork 210차-y · sha `9fe197d9` · **무변 실측**)
> 축 = `reporting.md §15.1` **14 축** (= 발주 §8.1)
>
> ★★**본 REPORT 는 2 투입분을 담는다.** 아래 ⑴~⒂ = **1 차**(T1·T3·T4·T5·T6 착지 · **T2 회부**) 기록 **verbatim 보존** — 그때의 실측이고 **고치지 않는다**. T2 는 **재투입**으로 닫혔고 그 기록 = **§T2 (맨 끝)**. ⟹ **제목의 「셋 중 둘」은 1 차 시점의 참이다** (= 삭제 0 · 병기). 최종 상태 = **`verify-sync` PASS 161 / DRIFT 0 / MISS 0 · exit 0**.

---

## ⑴ BASELINE 재측 (= 발주 §0-B 전량 재측 · ★인용 0 · 갈린 것 전부 표기)

발주가 못 박은 대로 **모든 수치를 다시 쟀다**. 발주 §0-B 는 `de6af66` 시점 값이고, 그 뒤 선행 판이 착지해 **7 항 중 6 이 갈렸다**.

| 항 | 발주 §0-B (그때) | 진입 재측 (2026-08-30) | 갈림 |
|---|---|---|---|
| master HEAD | `de6af66` | **`eb6452a`** | ★갈림 (선행 판 4 commit) |
| master ahead / porcelain | `37` / `0` | **`41` / `0`** | ★ahead +4 |
| verify-sync 전수 | `161` | `161` | 동일 |
| verify-sync PASS | `144` | **`136`** | ★−8 |
| **verify-sync DRIFT (쌍)** | `51` | ★**`75`** | ★★**+24** |
| verify-sync MISS | `0` | `0` | 동일 |
| **drift file** | `17` | ★**`25`** | ★★**+8** |
| 자식 3 상호 byte-identical | 참 | **참** (모든 drift 가 3/3) | 동일 |
| FND porcelain / ahead | `2` / `0` | `2` / `0` | 동일 |
| TPD porcelain / ahead | `1` / `0` | `1` / `0` | 동일 |
| **SW porcelain / ahead** | `17` / `51` | **`13` / `53`** | ★둘 다 갈림 |
| stale ref 출현 | `9` | `9` | 동일 |
| `rule-footer-common` master ↔ 자식 | `40` ↔ `8` 행 | **`46` ↔ `8`** 행 | ★master +6 |

★**갈림의 원인은 하나다** — 선행 판 `MASTER-RULE-HISTORY-SECTION-001` (= `eb6452a` 착지 확인) 이 **rule file 11 곳에 「명시 cycle 이력」 절을 신설**했고, 그중 **8 file 이 새로 drift 대열에 합류**했다 (17 → 25). `rule-footer-common.md` 가 40 → 46 행이 된 것도 같은 판이 자기 이력 절에 entry 를 넣었기 때문이다.

⟹ ★**발주 §11 의 「선행 판 미착지면 투입하지 마라」 경고가 정확히 이 사태를 겨눴다.** 착지를 먼저 확인했고(`eb6452a`), 그래서 **더 커진 분모로** 전파했다. 발주값 17 을 인용해 17 만 전파했으면 **8 file 이 갈린 채 남았을 것**이다.

---

## ⑵ 배경 재진술 (= 발주 §0-ⓐ 를 내 말로)

`rule-footer-common.md:6` 이 「cli infra 권장 byte-identical (= 4-repo)」를 계약으로 걸어 두었는데 **실물이 그 계약을 어기고 있었다**. 그리고 이 drift 는 세 갈래 분기가 아니라 **master 만 앞선 단방향 격차**다 — 자식 3 은 서로 byte-identical 이었다.

가장 아픈 곳은 **계약을 적어 둔 file 자신**이다. `rule-footer-common.md` 가 master 에서 46 행인데 자식에서는 **8 행**이었다. master 판에만 있는 것:

- 「이력 절 등재 의무」 (`:10`)
- 그 의무의 **판정선** = 「실질 개정 ↔ 기계 치환」 축 + 보조자 3 (동형 치환 서명 / 이동 서명 / 헤딩±)
- 「이동 서명」 좁힘 · 경계 실측표 8 commit · 부재 29 전수 판정

⟹ ★**자식 repo 에서 도는 판은 2026-07-10 판 규약을 읽고 있었다.** 방금 두 판(`REGISTER-AXIS-REPLACE-001`, `RULE-HISTORY-SECTION-001`)이 힘들여 세운 축이 **자기 적용 대상인 자식 3 에는 존재하지 않았다.** 규약을 만드는 일과 규약이 도달하는 일은 다른 일이고, 이 판은 뒤쪽이다.

---

## ⑶ 착지 좌표 (★`grep -n` 재측 · 발주 §0-ⓑ 는 「행 번호 재측」을 명시)

| 좌표 | 재측 행 | 내용 |
|---|---|---|
| `.claude/rules/rule-footer-common.md` | **`:6`** | cli infra 권장 byte-identical (= 4-repo) |
| 〃 | **`:7`** | 변경 시 master cycle 신설 + 4-repo propagation |
| 〃 | **`:8`** | 동결 3 (GB/GD/GT) = 전파 대상 X · 쓰기 0 |
| 〃 | **`:9`** | 자식 repo 직접 수정 금지 |
| 〃 | **`:10`** | 이력 절 등재 의무 (= 자식판 부재분의 핵심) |
| `scripts/verify-sync.sh` | **`:132`** | `! -path '*skills/run-*'` (제외) |
| 〃 | **`:134`** | `! -path 'docs/agent/audits/*'` (제외) |
| 〃 | **`:139`** · **`:142`** | 위 두 제외의 **사유 주석** |

★**발주 §0-ⓑ 는 제외 좌표를 `:139`·`:142` 로 적었는데, 그 두 줄은 「사유 주석」이고 실제 **집행 문면**은 `:132`·`:142`... 이 아니라 **`:132`·`:134`** 다.** 발주가 가리킨 곳은 틀리지 않았다(주석은 거기 있다) — 다만 **제외를 실제로 하는 줄은 따로 있다.** 전파 분모를 지키려면 주석이 아니라 `find` 의 `! -path` 행을 봐야 한다. (S11 = 실물이 정본 · 발주 문면 무수정 · 본 표가 병기다.)

---

## ⑷ 게이트 12 전/후 (= 발주 §6 블록 **awk 추출 후 verbatim 실행** · 재타이핑 0)

실행 = `bash <(awk '/^```bash$/{f=1;next} /^```$/{f=0} f' <발주서>)`.

| G | 진입 실측 | 마감 실측 | 발주 기대 | 판정 |
|---|---|---|---|---|
| **G0** | `claude-cli-master eb6452a 5` | `claude-cli-master e2e0383 5` | `<HEAD> 5` | ✅ |
| **G1** | ★`75` (발주 「(진입 실측)」) | **`25`** | `0` 또는 **SW 분만 잔존** | ✅ (SW 분만) |
| **G2** | `2 0` | **`2 1`** | 첫 칸 2 불변 · 둘째 1 | ✅ |
| **G3** | `1 0` | **`1 1`** | 첫 칸 1 불변 · 둘째 1 | ✅ |
| **G4** | ★`13 53` (발주 「(진입 실측)」) | **`40 55`** | 회부 시 **불변** · 첫 칸 = 관측만 | ⚠ **소명 ⑹-c** |
| **G5** | `9` | **`12`** | `9` 불변 또는 **증가** | ✅ (증가) |
| **G6** | `0 2` | **`0 2`** | 불변 `0 2` | ✅ |
| **G7** | `46 8` (발주 `40 8`) | **`46 8`** | T2 회부 시 `40 8` → 실물 `46 8` | ✅ (S11) |
| **G8** | `3 0` | **`3 1`** | `3 1` | ✅ |
| **G9** | ★`170` (발주 「(진입 실측)」) | **`171`** | 진입 +1 | ✅ |
| **G10** | `0` | **`0`** | `0` | ✅ |
| **G11** | `0 41` (발주 `0 37`) | **`3 42`** → commit 후 **`0 43`** | 첫 칸 0 · 둘째 40~44 | ✅ |

**★비워 둔 3 항의 진입 실측** (= 발주 §6.2 가 「값을 지어 넣지 마라」로 비운 항 · §9-⑴ 응답):
- **G1 = `75`** (발주 대조값 51 · 선행 판이 +24 쌍)
- **G4 = `13 53`** (발주 대조값 17 / 51)
- **G9 = `170`**

**★G4 「후」 소명** — 첫 칸 13 → 40, 둘째 53 → 55. **내가 만든 변화가 아니다.** ⑹-c 에서 자로 증명한다.

**★G7 진입값이 발주와 갈린다** (`40 8` → `46 8`): 첫 칸은 선행 판이 6 행 늘린 결과이고 **둘째 칸 8 은 그대로**다 — 즉 **자식은 그동안 한 걸음도 못 따라왔다**는 사실만 더 선명해졌다.

**★G6 의 stderr 2 줄은 값이다** (발주 §6.2). `ls` 가 「No such file」 2 줄을 내고 첫 칸 `0` 을 만드는데, 그 **0 이 곧 「의도적 제외 2 종이 자식에 없다」의 증명**이다. 마감에도 `0 2` 불변 = **S2 미발**.

---

## ⑸ ★`verify-sync` 전/후 요약 3 수 + exit 코드 **그대로**

| 시점 | files | PASS | **DRIFT** | MISS | **exit** |
|---|---|---|---|---|---|
| 진입 (`--no-update`) | 161 | 136 | **75** | 0 | **1** |
| **마감** (갱신 실행) | 161 | 136 | **25** | 0 | **1** |

★**exit = 1 을 그대로 적는다. 맞추지 않았다** (= 발주 T4 · S10).

**잔존 25 의 귀속 (= 「Selfward 분만 잔존」의 자)**:

| 자식 | 잔존 drift file |
|---|---|
| app-foundation | **0** |
| toward-product-docs | **0** |
| **Selfward** | **25** |

⟹ 잔존 25 = **전량 Selfward 단독**. T2 회부의 **예정된 귀결**이고, 발주 T4 가 「그 사실을 REPORT 에 적고 exit 를 그대로 보고하라」고 못 박은 바로 그 상태다.

★**PASS 가 136 에서 안 움직인 것이 정상이다** — `verify-sync` 의 PASS 는 **자식 3 전원 일치**일 때만 세므로, SW 가 남은 25 file 은 FND·TPD 가 붙어도 PASS 로 안 넘어간다. **DRIFT 만 75 → 25 로 줄었다**(= 정확히 FND 25 + TPD 25 = 50 감소). ★**PASS 불변을 「전파 실패」로 읽으면 오독**이고, 자를 안 읽은 것이다.

★**stale ref WARN 은 마감에도 계속 뜬다.** 이것은 결함이 아니다 — ⑼ S5 항과 T3 소명 참조.

---

## ⑹ ★T2 착지 판정 — **자와 출력** (= 발주 §0-ⓕ · §9-⑵ · 「둘 다 보라」)

계약 = **`porcelain ≤ 선재 11` AND `A-2c REPORT commit 존재`**. **둘 다** 봤다.

### ⑹-a 자 ①: porcelain

```
git -C ../Selfward status --porcelain | wc -l   →  13
```
**13 > 11 ⟹ 미충족.**

### ⑹-b 자 ②: A-2c REPORT commit 존재

```
git -C ../Selfward log --oneline --all --grep='LIFETIME-UNBIND'
  62736f4  … §18 수명 결박 축 해소 + 판 15 A-2c 착지 (T6)
  5b9a952  … 허브 4 + 서재 2 화면층 하강 (T1·T2·T3)
git -C ../Selfward show --name-only 62736f4
  docs/architecture/DESIGN-SELFWARD-ARCH-REBUILD-001.md      ← REPORT 아님
git -C ../Selfward ls-files '.ai/reports/SELFWARD-ARCH-SCREEN-VM-LIFETIME-UNBIND-001/' | wc -l
  0                                                          ← tracked 0
find .ai/reports/SELFWARD-ARCH-SCREEN-VM-LIFETIME-UNBIND-001 -name 'REPORT*'
  (0본)                                                      ← 디스크에도 없음
```

★그 디렉터리에 실재한 것 = **PNG 30 + `gate.sh` + `probe.bin` + `rollcheck.py`** = **집행 중인 판의 작업 산물**이지 착지물이 아니다. **REPORT commit 존재 ⟹ 미충족.**

### ⑹-c ★★두 자가 **둘 다** 미충족 ⟹ **회부**. 그리고 그 판단은 **사후에 증명됐다**

발주는 「애매하면 회부다」라고 했지만 **이 건은 애매하지도 않았다** (두 conjunct 모두 실패). 그런데 **집행 중 Selfward 가 스스로 움직였다**:

```
진입 박제 : SW HEAD = 62736f4 · porcelain 13 · ahead 53
마감 실측 : SW HEAD = c66d57f · porcelain 40 · ahead 55   ← +2 commit
git -C ../Selfward log --oneline 62736f4..HEAD
  c66d57f  docs(report): … LIFETIME-UNBIND-001 REPORT 자수 정정 (⑷·⑽·⑾·⑿·⒁)
  ee4356d  docs(report): … 원장 SW-T140 + SW-T138 재판정 (T6) + REPORT
```

★**`ee4356d` 가 바로 그 「A-2c REPORT」를 commit 했다** — 즉 **내가 진입해서 판정할 때 그 판은 살아서 돌고 있었고, 내가 작업하는 동안 자기 REPORT 를 마감했다.** 발주 §0-ⓕ 가 경계한 「두 손이 동시에 들어간다」는 **가정이 아니라 실황이었다.**

**내가 그 2 commit 의 원인이 아니라는 자**:

```
git -C ../Selfward diff --name-only 62736f4..HEAD | grep -c -e '^\.claude/' -e '^docs/rules/' -e '^docs/guides/' -e '^docs/agent/'
  0                                        ← cli infra 경로 0 = 내 전파 아님
shasum .claude/rules/rule-footer-common.md  master=671ae5ece95f  SW=8013bbde9a8f   ← 여전히 구 sha
wc -l ../Selfward/.claude/rules/rule-footer-common.md → 8                          ← 여전히 8 행
```
⟹ **SW 무접촉 증명 완료.** 전파 25 경로는 손대지 않았다.

### ⑹-d ★자 자체에 대한 비판 — **porcelain 행수는 「일의 양」이 아니다**

13 → 40 이 27 개의 새 작업처럼 보이지만 **아니다**. 검산:

```
진입 13 − 1 (접혀 있던 ?? dir) + 29 (그 dir 안 개별 ??) − 1 (INITIATIVES.md 가 commit 됨) = 40
실측 40  ⟹ 일치
```

원인 = 다른 session 이 그 디렉터리 안에 **3 file 을 tracked 로 만들자**(`REPORT.md`·`gate.sh`·`rollcheck.py`) git 이 **디렉터리 접기를 멈추고 나머지 29 를 개별 열거**하기 시작했다.

⟹ ★**`porcelain | wc -l` 은 열거 단위에 좌우되는 자다.** 같은 산출물 더미가 **1 행으로도 29 행으로도** 세어진다. 「≤ 11」 같은 **행수 임계값은 그 자체로 취약**하다 — 아무 일도 안 일어나도 27 이 뛸 수 있고, 반대로 큰 작업이 1 행에 숨을 수 있다.

★**단 본 판의 결론은 이 취약성에 의존하지 않는다.** 진입 시점에 **두 번째 자(REPORT commit)가 독립적으로 미충족**이었고, 그쪽이 결정적이었다. 자가 약한 것과 판정이 틀린 것은 다르다.

---

## ⑺ §9 「나를 의심하는 절차」 **7 항 전량** (= 방어 0)

**⑴ 「전」 값이 재현되는가.**
아니다 — **6 항이 갈렸다**(⑴ 표). 발주가 비운 3 항의 진입 실측 = **G1 `75` · G4 `13 53` · G9 `170`**. 갈린 이유는 선행 판 착지 하나로 전부 설명되고, ⑴ 에 자와 함께 적었다. **발주값을 인용하지 않았다.**

**⑵ ★★Selfward 가 정말 착지했는가.**
**아니다. 회부했다.** 두 자 **모두** 미충족(⑹-a·⑹-b) — porcelain 13 > 11 이고 A-2c REPORT commit 은 **tracked 0 · 디스크 0**. 그리고 집행 중 그 판이 **자기 REPORT 를 commit** 하면서(⑹-c) 「집행 중이었다」가 사후 증명됐다. ★**전파했으면 `propagate.sh` 의 `git add` 가 25 file 을 물린 채로 그 판의 2 commit 이 지나갔다.**

**⑶ ★전파 분모가 `verify-sync` manifest 와 같은가 — 눈으로 대조했는가.**
같다. **`--all` 을 쓰지 않았다.** 분모를 `verify-sync` **출력에서 직접 파생**시켰다:
```
verify-sync --no-update | grep '^  ✗ ' | awk '{print $2}' | sort | xargs propagate.sh --targets FND,TPD
```
⟹ 분모가 **구성상 동일**(파생이라 어긋날 여지가 없다). 추가로 **의도적 제외 7 종과의 교집합 = `0` 실측**, master 실재 **25/25**.
★**`--all` 을 피한 실질 근거**: `propagate.sh:132` 의 root loop 가 **`gradle.properties` 를 포함**하는데 `verify-sync.sh:118-119` 는 그것을 **domain-specific 으로 의도적 제외**한다. 즉 `--all` 은 **verify-sync 보다 넓다**. 오늘은 3 repo 값이 우연히 같아(`51e4b5a131eb`) 사고가 안 났겠지만, **분모가 넓다는 사실은 값이 우연히 같은 것과 무관하다.**

**⑷ ★자식의 선재 porcelain 을 건드리지 않았는가.**
안 건드렸다. commit 후 **FND `2` · TPD `1` 그대로**. staged 집합 = **정확히 25**(각 repo). 잔존 unstaged = FND `.auto-memory/incident-log.md` + `?? app-foundation` · TPD `.auto-memory/incident-log-cowork.md` = **전부 다른 판의 것**. `git add -A` **미사용** · `git commit` 에 `-a` **미사용**.

**⑸ ★stale ref 3 을 지우지 않았는가.**
안 지웠다. **G5 `9` → `12` (증가)**. 백틱 구 문면 **전량 생존**: `check-abbreviation.sh` 2 · `abbreviation-policy.md` 3 · `.claude/rules/workflow-core.md` 2.
★**눈으로 세 자리 확인**(발주 §9-⑸): `:76` · `:87` · `:88` · `:100` (= 3 종 · 출현 4). numstat `+15/-4` 의 **-4 는 in-place 행 편집**이고, 4 행 전량이 「끝 ` |` 제거 후 앞 120 자 + 뒤 60 자」 잔존 검사에서 **보존** 판정 ⟹ **내용 삭제 0**.

**⑹ ★`verify-sync` exit 를 맞추려 하지 않았는가.**
안 맞췄다. **exit `1` 그대로 보고**(⑸). DRIFT **25 잔존이 옳은 값**이고, 그것을 0 으로 만들려면 SW 에 전파하거나 분모를 좁혀야 하는데 **전자는 S1 · 후자는 S10**. `--target` 으로 SW 를 빼고 재실행해 exit 0 을 만드는 유혹이 실재했고 **하지 않았다**.

**⑺ ★§9 산출 축이 하한 `+250` 을 넘는가.**
`wc -l .ai/reports/MASTER-PROPAGATE-DRIFT17-001/REPORT.md` = ⒁ 에 실측 기재. **단위 = 행.**

---

## ⑻ ChangeBudget (**단위 = 행** · ★층 별 계상 · 합산 X)

| 층 | 밴드 | 실측 | 판정 |
|---|---|---|---|
| **master 실코드/문서** | `+8 ~ +30` | **+21 / −9** (순증 **12**) | ✅ 밴드 내 |
| **자식 전파** | ★**측정만** (밴드 X) | FND **25 file +579/−8** · TPD **25 file +579/−8** | 측정 |
| **§9 산출** | `+250 ~ +420` | ⒁ 실측 | — |
| **삭제** | ★**`0`** | **0** (⑺-⑸ 보존 검사) | ✅ |

master 내역:

| file | +/− | 사유 |
|---|---|---|
| `.auto-memory/protected-file-hashes.md` | +15/−4 | T3 병기 |
| `CLAUDE.md` | +2/−2 | T6 §15 등재 1 + demote 1 + 회차 note |
| `.auto-memory/master-cycle-history-COLD.md` | +4/−3 | demote 착지 + 자기서술 3 곳 |
| `.auto-memory/propagation-status.md` | +39/−11 | ★**verify-sync 자동 생성**(auto-region · 수기 0) |

★**`propagation-status.md` 의 +39/−11 은 내가 쓴 것이 아니다** — `verify-sync.sh:243-308` 이 auto-region 을 **live 로 재생성**한다(수기 편집 금지 영역). 그래서 **master 실코드/문서 축(밴드 대상)에서 뺐다**: 넣으면 +60 이 되어 밴드를 깨는데, 그것은 **내 저작이 아니라 도구 출력**이다. ★**밴드는 「내가 쓴 행」에 걸린 것**이고, 도구 산출을 거기 합산하면 밴드가 사람의 판단이 아니라 스크립트를 재게 된다.

★**자식 축에 밴드를 걸지 않은 이유** (= 발주 §4 · §7-③): 자식 579 행은 **master 본문의 복제**다. 여기 밴드를 걸면 **같은 저작을 두 번 세는 것**이고, 자식 3 이면 세 번 센다.

---

## ⑼ STOP 11 (★**0 도 값** · 전량 기재)

| # | trigger | 실측 | 자 |
|---|---|---|---|
| **S1** | 미착지 SW 전파 | **0** | T2 **회부** · 판정 자 2 종 + 출력 = ⑹ · SW 전파 file **0** |
| **S2** | `skills/run-*` / `docs/agent/audits/*` 전파 | **0** | 분모 ∩ 제외 7 종 = `0` · **G6 `0 2` 불변** |
| **S3** | 동결 3 (GB/GD/GT) 접촉 | **0** | `--targets app-foundation,toward-product-docs` 명시 · GB/GD/GT 미지정 |
| **S4** | 자식 선재 porcelain commit | **0** | FND `2`→`2` · TPD `1`→`1` · staged = 정확히 25 · `git add -A` 0 |
| **S5** | stale ref 구 문면 삭제 | **0** | G5 `9`→**`12`** (증가) · 백틱 3 종 전량 생존 · 보존 검사 4/4 |
| **S6** | 자식 자체 file 수정 | **0** | 자식 write = 전파 25 경로 한정 · C14 `.gitignore` = **marker 기존 ⟹ no-op 3/3** |
| **S7** | push | **0** | `git push` 미실행 · 3 repo 전부 ahead 잔존(= 로컬) |
| **S8** | master rule **내용** 변경 | **0** | `.claude/rules/**`·`docs/rules/**` diff = **0 file** (본 판은 옮기기만 했다) |
| **S9** | `server/`·`supabase/` 접촉 | **0** | **G10 `0`** |
| **S10** | exit 맞추려 분모 좁힘 | **0** | exit `1` 그대로 · `--target` 회피 재실행 0 |
| **S11** | 발주 ↔ 실물 갈림 | ★**적용 3** | 실물 채택 (아래) |

**★S11 적용 3 건** (= 발주 문면 **무수정** · 본 REPORT 가 병기):
1. **drift 17 → 25** (발주 §0-B·제목·§11 전부 「17」) — 실측 25 로 전파.
2. **`rule-footer-common` 40 → 46 행** (발주 §0-ⓐ·G7 기대 `40 8`) — 실측 `46 8`.
3. **제외 집행 문면 `:139`·`:142` → `:132`·`:134`** (발주 §0-ⓑ·§2) — 발주가 가리킨 두 줄은 **사유 주석**이고 집행은 `find` 의 `! -path` 행이 한다(⑶).

★**S8 이 특히 좁다**: 본 판은 rule 을 **한 줄도 고치지 않았다**. `protected-file-hashes.md`·`CLAUDE.md`·COLD 는 `.auto-memory/`·헌법이지 rule 이 아니고, **전파 분모(`.claude`/`docs`)에도 안 들어간다**(= 새 drift 를 만들지 않았다는 뜻).

---

## ⑽ 회부 + 원장 번호

| 항 | 처분 | 사유 | 자 |
|---|---|---|---|
| ★**T2 Selfward 전파** | ★**회부** | 착지 2 자 **둘 다** 미충족 · 집행 중 그 판이 REPORT commit ⟹ 실황 확인 | ⑹ |
| **push** | **Coin 이관 유지** | 본 판에서 열리지 않았다 (발주 ⓔ) | 3 repo ahead 잔존 |
| `verify-sync` stale ref WARN 잔존 | **의도적 존치** | 끄려면 백틱 인용 삭제 = **S5** | T3 병기 |
| hot 행 **431B 초과 1** | **무접촉** | 선행 판 entry (`RULE-HISTORY-SECTION-001`) · 남의 판 문면 | ⒀ |

**원장** (= 발주 §0-ⓓ): ㉡ **#244** Coin 이관 ⑴(propagation) → ★**부분 마감**. FND·TPD 는 닫혔고 **SW 분은 회부**로 남는다 (= 「닫았다」로 적으면 거짓). 남는 이관 = **push 1 + SW propagation 1 = 2** (발주 예상 「push 1」 대비 **+1**).

**후속 판 진입 조건** (= SW 전파): `porcelain ≤ 선재` **AND** A-2c 계열 판 **비집행** 확인. ★**전파 자체는 그대로 재사용 가능** — 분모 파생 명령(⑺-⑶)에 `--targets Selfward` 만 바꾸면 된다.

---

## ⑾ commit 계수 **자기 대조** (★master ↔ 자식 **따로**)

**master** (= 발주 §8 계획 2 본):

| # | sha | 제목 | T |
|---|---|---|---|
| 1 | `e2e0383` | `docs(cli): … protected-file-hashes stale ref 3 병기 정정 (T3)` | T3 |
| 2 | `e3a8e97` | `docs(cli): … §15 등재 1 + demote 1 + 회차 30 (T6) + REPORT` | T6 |
| 3 | (본 commit) | `docs(cycle): … REPORT ⒂ 정본값 기입 + 자수 3 (정정)` | ★정정 |

계수 대조: 진입 ahead **41** → **44** = **+3** ⟹ 계획 **2** vs 실측 **3** = ★**+1 초과**.
★**초과 사유 = 은폐하지 않는다**: `e3a8e97` 이 착지시킨 REPORT ⒂ 가 「그 수를 commit body 에 박는다」고 약속해 놓고 **body 에 `386` 이 없었다**(= ⒂ 자수 3). `--amend` 는 cli 소관 밖(Coin)이라 **정정 commit 으로만 닫힌다**. ⟹ **계획 초과 1 본은 「추가 작업」이 아니라 「자기 문면 반증 해소」**이고, 남겨 두는 쪽이 S11(실물이 정본)에 어긋난다.

**자식** (= 발주 §8 계획 「각 1」 · ★**master ahead 에 안 들어간다 — 따로 센다**):

| repo | sha | file | +/− | ahead |
|---|---|---|---|---|
| app-foundation | `3a53a9d` | 25 | +579/−8 | 0 → **1** |
| toward-product-docs | `d849a02` | 25 | +579/−8 | 0 → **1** |
| **Selfward** | — | **0** | — | 53 → 55 (★**타 session** · ⑹-c) |

계수 대조: 계획 **2~3** → 실측 **2** (SW 회부) ✅ · 자식 총 **2 commit** ✅
★**SW 의 +2 를 내 계수에 넣으면 거짓이 된다** — 그 2 는 다른 session 의 것이고, 내 자식 commit 은 **2** 다.

**합계**: master 2 + 자식 2 = **4 commit** · **push 0**.

---

## ⑿ 자 대조표 + 환경

| 명제 (§3) | 자 | 단위 | 진입 → 마감 |
|---|---|---|---|
| 1. drift 가 없다 | `verify-sync --no-update` 요약 DRIFT | 쌍 | **75 → 25** (SW 분) |
| 2. 〃 (대조군) | 게이트 `DR()` = 지정 6 dir sha 대조 | 쌍 | **75 → 25** ★주 자와 **독립 산출인데 일치** |
| 3. 자식이 전파분만 commit | `git -C <자식> diff --name-only <진입>..HEAD` | 파일 | FND **25** · TPD **25** |
| 4. 선재 porcelain 무접촉 | `status --porcelain \| wc -l` | 행 | FND `2`→`2` · TPD `1`→`1` |
| 5. stale ref 병기 | `grep -c -F` 구 문면 | 출현 | **9 → 12** (감소 0) |

★**명제 2(대조군)가 값을 갖는 이유**: `DR()` 은 `verify-sync` 와 **다른 코드·다른 dir 열거**로 세는데 **같은 75, 같은 25** 를 냈다. 분모가 최대 위험이었던 판에서(내 census 19 ↔ manifest 17 의 선례) **독립 두 자의 일치**는 실질 근거다.

**환경 실측**:
- `pwd` = `/Users/yundonghyeon/AndroidStudioProjects/claude-cli-master` · branch `main`
- ★**shell = zsh** — 이것이 사고를 하나 만들었다(⒀).
- `grep` = **ugrep shim** (선행 `-` 인자를 옵션으로 파싱 ⟹ `-e`/`--` 필요)
- `git ls-files` = **765** (자기서술 전수 grep 분모)
- `verify-sync` files = **161** · `--skip-daemon-check` 사용(daemon 진단 잡음 제거 · 판정 무관)

---

## ⒀ Negative space (= `anchor-list.md §4` 의무 1 줄 + 확장)

**고려했으나 hot 제외 영역**: ① `propagate.sh --all` 채택(= 분모가 verify-sync 보다 넓다 ⟹ 파생 분모로 대체) ② SW 전파(= 회부) ③ stale ref **삭제**로 WARN 소거(= S5 ⟹ 병기) ④ hot 행 **431B 초과 1** 정정(= 선행 판 문면 · 남의 entry 재작성은 본 판 scope 밖) ⑤ `--prune` 실행(= 본 판 계약 밖 · run-* 재seeding 선례) ⑥ 자식 `.gitignore` 손보기(= C14 no-op 확인으로 족함) ⑦ 「이름 축 통일 16」(= 선행 판이 별 판으로 넘긴 항).

★**이번에 새로 생긴 부채 1** = **hot 행 400B 상한 위반 1 건 잔존**(`RULE-HISTORY-SECTION-001` **431B**). 내 행은 **390B 로 맞췄고**(초안 417B → 트림), 남의 행은 안 건드렸다. ⟹ **상한 규약이 자기 표 안에서 1 건 깨진 채 살아 있다.** 다음 판이 그 entry 를 demote 할 때 자연 해소되지만, **그 전까지는 「상한 3 · 각 ≤400B」 가 문면과 실물이 갈린 상태**다.

★**본 판이 자백할 실수 1** — **첫 전파 시도가 조용히 실패했다**. `propagate.sh $FILES` 를 unquoted 로 넘겼는데 **zsh 는 기본적으로 단어 분할을 하지 않아** 25 경로가 **한 개의 인자**로 들어갔다(`ok=0 fail=2`). 값은 안 깨졌다(cp 0 건 · 자식 porcelain 불변 확인) — 하지만 ★**「exit 0」 이 성공을 뜻하지 않았다**: `propagate.sh` 는 `TOTAL_FAIL>0` 일 때만 1 을 내는데 그 경로는 `fail` 을 세고도 **exit 0** 을 냈다(`:403`). **요약 줄(`ok=0`)을 안 읽고 exit 만 봤으면 「전파 완료」로 보고할 뻔했다.** 같은 자가 앞서 「master 실재 25 → `0`」 이라는 이상값도 냈는데, 그때 **덮지 않고 파고든 것**이 사고를 막았다.

---

## ⒁ 판단·선택 → 원장

| # | 판단 | 선택 | 근거 |
|---|---|---|---|
| 1 | 발주 분모 17 vs 실측 25 | **25 채택** | 선행 판 +8 · 17 이면 8 이 갈린 채 잔존 |
| 2 | `--all` vs 파생 분모 | ★**파생** | `--all` ⊃ verify-sync (`gradle.properties`) |
| 3 | SW 전파 vs 회부 | ★**회부** | 두 자 **둘 다** 미충족 · 사후 실황 확인 |
| 4 | WARN 소거 vs 병기 | **병기** | 소거 = 구 문면 삭제 = S5 |
| 5 | 소멸/이동 한 묶음 vs 구별 | ★**구별** | 3 종 중 1(`workflow-core`)은 **이동** · 자가 못 가른다 |
| 6 | exit 0 만들기 vs 그대로 | **그대로** | S10 |
| 7 | `propagation-status` 를 밴드에 합산 vs 분리 | **분리** | 도구 자동 생성분 · 밴드는 저작에 건다 |
| 8 | 431B 남의 행 정정 vs 무접촉 | **무접촉** | scope 밖 · ⒀ 에 부채로 등재 |
| 9 | 회차 30 을 발주에서 인용 vs 재도출 | ★**재도출** | 아래 |

★**#9 상세** — 발주는 「회차 30」을 줬지만 **인용하지 않고 다시 도출했다**. 자 = 「COLD 데이터행(`^| [A-Z0-9]`) **순증 > 0** 인 commit 을 시간순 계수」 · 산술 검산 **`net 합 170 == 실측 170`**(demote 직전) ⟹ 누적 **29**, 본 판 **30**. 발주값과 일치.
★★**그런데 자를 한 칸만 헐겁게 잡아도 답이 깨진다**: 「COLD 를 만졌고 추가 행이 1 이상」으로 세면 **51** 이 나온다(**+21 부풀림**). 원인 = **데이터행을 안 늘린 append 가 실재**한다(heading·lineage·note 만 고친 판 · 실례 `8b0ad11` = `MASTER-DOC-MANIFEST-SWEEP-002`). ⟹ **「COLD 를 건드렸다」와 「이력이 한 줄 늘었다」는 다른 사건이고, 회차는 뒤쪽이다.** 이 구별을 §15 note 에 박았다.

**T6 착지 증명**:
- **demote 1** = `MASTER-DOC-MANIFEST-SWEEP-004` (= 표의 **맨 위 행** = 최고령 · §15 「순서가 정본」) → COLD 말미. **이동 증명 = sha 쌍 대조 PASS** (hot 구 `6ee071fd35eb56b5` == COLD 신 `6ee071fd35eb56b5` · 395B) ⟹ **verbatim · 삭제 0**.
- **등재 1** = 본 판 (맨 아래 · **390B ≤ 400B**).
- **COLD** 170 → **171** 데이터행 · 168 → **169** 고유 ID.
- ★**자기서술 갱신 = COLD 3 곳**(`:1` 머리 · `:13` §1 heading · `:63` 표 서문) **+ `CLAUDE.md` 1 곳**. 전수 재측 = live 문서 잔존 **0** (`git ls-files` **765** 분모). ★**과거 REPORT 3 본의 구 수치는 갱신하지 않았다** — 그것은 **시점 기록**이고 고치면 그때의 실측이 사라진다.

**§9 산출 축 실측**: 아래 ⒂ 에 `wc -l` 값 기재.

---

## ⒂ §9 산출 축 (= 발주 §4 · §9-⑺ · **단위 = 행**)

```
wc -l .ai/reports/MASTER-PROPAGATE-DRIFT17-001/REPORT.md
  380        ← 본 줄 기입 직전 실측
```

**측정 계열** (= 밴드 `+250 ~ +420` · 전 구간 밴드 내): 첫 실측 **`380`** → 기입 후 **`383`** → 자수 항 반영 후 **`386`** → …

★★**자기 참조 함정 — 이 자리에서는 「최종 한 수」가 존재할 수 없다.** 값을 본문에 적는 행위가 곧 값을 바꾸므로, 적을 때마다 자가 자기를 앞지른다(380 → 383 → 386 …). ⟹ ★**확정값의 정의를 옮긴다**: 본 축의 정본 = **commit 된 blob 기준** `git show <sha>:.ai/reports/MASTER-PROPAGATE-DRIFT17-001/REPORT.md | wc -l` (= 그 시점에 file 은 더 이상 안 변한다). 위 계열은 **작업 중 실측 이력**이고 삭제하지 않는다.

**★정본 실측 = `386` 행** (= `git show e3a8e97:<본 file> | wc -l` · T6 commit 시점 blob) · 밴드 `+250 ~ +420` ⟹ ✅ **밴드 내**.

★★**자수 3 — 약속한 자리에 수를 안 박았다.** 구 문면은 여기서 「그 수는 **본 판 commit body 에 박는다**」로 끝났는데(= 삭제 0 · 본 문장에 병기), **`e3a8e97` commit body 에 `386` 은 없다.** 즉 **committed 문서가 자기 artifact 로 반증되는 문장을 담은 채 착지**했다. `--amend` 는 cli 소관 밖(= `safety-and-secrets.md` 고위험 git · Coin 몫)이라 **정정 commit 1 본을 추가**해 닫는다 ⟹ master commit **2 → 3**(⑾ 재대조). ★**이 트랙이 고치는 병이 정확히 이것**이다 — 「문면이 자기를 틀리게 적으면 다음 사람의 분모가 된다」(`MASTER-DOC-MANIFEST-SWEEP-001`). 자기 REPORT 안에서 3 회 재발했고(⑼ 자수 2 + 본 항), **셋 다 「재지 않고 적은」 같은 형태**다.

★★**자수 2** (= 본 줄에서 실제로 벌어진 일): 이 자리에 「기입 후 재측」이라며 처음 **`386`** 을 적었다. **재측한 값이 아니라 예측한 값**이었고, 그 시점 실측은 **`383`** 이었다. (★공교롭게도 정정을 반영하자 file 이 실제로 386 행이 됐는데, **맞아떨어진 것과 재고 적은 것은 다르다** — 그 386 은 근거 없이 적힌 뒤 사후에 우연히 만난 수다.) ⟹ **바로 앞 문장에서 「예측값을 적고 나중에 맞추는 방식은 쓰지 않았다」고 쓰면서 그 줄에서 그 짓을 했다.** 선행 판(`RULE-HISTORY-SECTION-001`)이 **REPORT §9 산출 축에서 예측 289 → 실측 335 로 자수**한 것과 **같은 자리·같은 형태의 재발**이다 — 그 자수를 읽고도 반복했다는 점에서 더 나쁘다. 정정 = 실측 `383` 로 교체 · **구 표기 `386` 은 본 문단에 병기 보존**(삭제 0).
★**이 항이 남기는 것**: 「자기를 재는 값」은 **적는 행위가 값을 바꾸므로**, 반드시 **기입 → 재측 → 그 수로 교체** 순이어야 한다. 예측으로 채우면 **문면이 자기와 갈리고**, 그것이 이 트랙이 고치려는 병 자체다.

---

*1 차 투입분 끝 · master 3 + 자식 2 commit · push 0 (Coin 소관).*

---
---

# §T2 재투입 — **마지막 자식이 붙었다**

> 투입 = T2 **단독** · 2026-08-30 KST · 진입 master HEAD = `6383b76` (= 1 차 마감) · 발주서 sha `9fe197d9` **무변 실측** (263 행)
> ★위 ⑴~⒂ = **1 차 기록 · 무수정**. 본 절 = 재투입 기록.

## T2-⑴ 진입 재측 — 「그때의 값」을 다시 쟀다

발주 재투입 지시가 「지금은 porcelain 11 · REPORT tracked 1」이라고 알려 줬지만 ★**그것도 「그때의 값」**이라 **둘 다 다시 쟀다**.

| 자 | 1 차 진입 | **재투입 진입 실측** | 계약 | 판정 |
|---|---|---|---|---|
| SW `porcelain \| wc -l` | `13` | **`11`** | ≤ 선재 **11** | ✅ **충족** |
| A-2c REPORT commit | tracked **0** · 디스크 **0** | **tracked `1`** (`ee4356d`) | 존재 | ✅ **충족** |

```
git -C ../Selfward ls-files '.ai/reports/SELFWARD-ARCH-SCREEN-VM-LIFETIME-UNBIND-001/'
  .ai/reports/…/REPORT.md      ← 이것이 자 ②
  .ai/reports/…/gate.sh
  .ai/reports/…/rollcheck.py
```

★**두 자 모두 충족 ⟹ 전파.** 1 차의 「애매하면 회부」가 이번엔 발동하지 않는다 — **애매하지 않았다.**

**★보강 자 3** (= 계약 밖이지만 「정말 끝났나」를 묻는 자 · 발주 §0-ⓕ 의 본심):
1. **작업 산물 소멸** — 1 차 때 그 dir 에 PNG 30 + `gate.sh` + `probe.bin` + `rollcheck.py` 가 널려 있었다(= 집행 중 신호). 지금 그 dir = **file 3 개 · 전부 tracked · ignored 0**. ⟹ 그 판이 **스스로 치우고 끝냈다**(삭제이지 gitignore 회피가 아니다 — `--ignored` 실측 `!!` **0**).
2. **활동 신호 0** — 최근 5 분 내 수정 tracked file **0**.
3. **`index.lock` 부재** — 동시 git 작업 없음.

⟹ ★**1 차의 회부는 과잉이 아니었고**(그때 그 판은 실제로 돌고 있었다 — 1 차 ⑹-c 가 `ee4356d`·`c66d57f` 로 증명), **이번 전파는 성급하지 않다.** 같은 계약이 **두 시점에 정반대 답**을 낸 것이 계약이 실제로 작동한다는 증거다.

★**porcelain 11 의 내용도 봤다** (= 1 차 ⑹-d 가 「행수는 열거 단위에 좌우된다」고 비판한 자라, 수만 보면 안 된다): `M` 2 (`SELFWARD-SRV-OPSPREP-PREFLIGHT-001/REPORT.md` · `.auto-memory/incident-log.md`) + `??` 9 (report dir 8 + `supabase/_ops/mgmt-auth-anon-off.ts`) = **전부 다른 판의 것** · A-2c 잔재 **0**.

## T2-⑵ 분모 재파생 — 「17」을 인용하지 않았다

발주 본문·제목은 **17** 이지만 실측은 **25** 다(= 1 차 ⑴ 과 동일 원인 = 선행 판이 rule 11 곳에 이력 절 신설). **`verify-sync` 출력에서 직접 파생**시켰다 ⟹ 분모가 **구성상 동일**:

```
verify-sync --no-update | grep '^  ✗ ' | awk '{print $2}' | sort   →  25 file
```

| 검사 | 실측 | 의미 |
|---|---|---|
| 분모 크기 | **25** | 「17」 인용 시 **8 file 이 갈린 채 잔존**했을 것 |
| 그중 Selfward 불일치 행 | **25** | 전량 SW (FND·TPD = **0** = 1 차에 붙었다) |
| ★**의도적 제외 7종 ∩ 분모** | **`0`** | **S2 미발** |
| ★`gradle.properties` ∩ 분모 | **`0`** | `--all` 오염 없음 |
| master 실재 | **25 / 25** | 「master 부재」 0 |

★**`--all` 금지 근거 재확인** (㉡ #255): `propagate.sh:132` root loop 가 `gradle.properties` 를 **포함**하는데 `verify-sync.sh:118-119` 는 그것을 **repo-specific 자유로 의도적 제외**한다 ⟹ **`--all` 이 verify-sync 보다 넓다.** 오늘 3 repo 값이 우연히 같아도(1 차 실측 `51e4b5a131eb`) **넓다는 사실은 값이 같은 것과 무관**하다.

## T2-⑶ ★K-181 — 종료 코드를 파이프 뒤에서 읽지 않았다

발주가 짚은 대로 **1 차의 「fail=2 인데 exit 0」은 문면상 설명되지 않는다**. `propagate.sh:403-406` 재독:

```
if [ "$TOTAL_FAIL" -gt 0 ]; then
  exit 1
fi
exit 0
```
누적(`:383-384`)도 온전하다. ⟹ ★**스크립트는 정상이었다. 틀린 것은 내 자였다.**

**1 차의 실제 기전**: `bash scripts/propagate.sh … | tail -25` 로 돌린 뒤 `$?` 를 읽었다 — 그것은 **`tail` 의 종료 코드**(항상 0)이지 `propagate.sh` 의 것이 아니다. ⟹ ★**1 차 REPORT ⒀ 의 「`propagate.sh` 가 fail 을 세고도 exit 0 을 냈다」는 서술은 오독이다** (= 1 차 문면 **삭제 0 · 본 절이 병기 정정**). 진짜 교훈은 「도구가 거짓말한다」가 아니라 **「파이프가 종료 코드를 갈아치운다」**이고, 후자가 훨씬 널리 적용된다.

**본 투입의 처리**: 도구를 **파이프 없이** 돌리고 `$?` 를 **즉시** 읽었다(명령 치환 = 파이프 아님). exit 와 요약 줄을 **둘 다** 보고 대조:

| 도구 | exit | 요약 줄 | 갈림 |
|---|---|---|---|
| `propagate.sh` | **`0`** | `ok=25 fail=0` | **없음** |
| `verify-sync.sh` | **`0`** | `PASS 161 / DRIFT 0 / MISS 0` | **없음** |

⟹ 두 자가 **일치**해 「갈리면 요약 줄이 정본」 규칙을 쓸 일이 없었다. ★**규칙이 발동 안 한 것과 규칙이 필요 없는 것은 다르다** — 1 차가 그 증거다.

★**본 투입 자수 1**: 첫 전파 시도에서 출력을 `/tmp-none-propagate.out` 으로 리다이렉트했는데 그것은 **filesystem 루트**라 read-only 로 튕겼고(`Read-only file system`), 그 결과 `exit 1` 이 **리다이렉트 실패의 코드**로 잡혔다. ⟹ ★**「파이프 뒤에서 읽지 마라」를 지키려다 이번엔 리다이렉트가 코드를 갈아치웠다** — 같은 병의 다른 얼굴이다. `propagate.sh` 는 **아예 실행되지 않았고**(SW porcelain 11 · `rule-footer-common` 8 행 = 무변 실측으로 확인) 값 손상 0. 명령 치환으로 바꿔 재실행.

## T2-⑷ 게이트 12 (진입 → 마감)

| G | 진입 | 마감 | 발주 재투입 기대 | 판정 |
|---|---|---|---|---|
| G0 | `claude-cli-master 6383b76 5` | `… 5` | — | ✅ |
| **G1** | **`25`** | ★**`0`** | 잔존 25 → **0** | ✅ |
| G2 | `2 1` | `2 1` | 불변 | ✅ |
| G3 | `1 1` | `1 1` | 불변 | ✅ |
| **G4** | **`11 55`** | ★**`11 56`** | 첫 칸 **11 불변**(변동 = S4) · 둘째 +1 | ✅ |
| G5 | `12` | `12` | 불변 | ✅ |
| G6 | `0 2` | `0 2` | 불변 | ✅ (S2 미발) |
| **G7** | **`46 8`** | ★**`46 46`** | SW **8 → 46** | ✅ |
| G8 | `3 1` | `3 1` | 불변 (T6 = 1 차 착지) | ✅ |
| G9 | `171` | `171` | 불변 | ✅ |
| G10 | `0` | `0` | `0` | ✅ |
| G11 | `0 44` | `1 44` → commit 후 **`0 45`** | 첫 칸 0 | ✅ |

★**G7 이 이 판의 눈**이다. `rule-footer-common.md` 가 **8 → 46 행**. **4-repo 전량 46 행** 눈 확인 완료(master · FND · TPD · SW).

## T2-⑸ `verify-sync` 전/후 + **exit 그대로**

| 시점 | files | PASS | DRIFT | MISS | **exit** |
|---|---|---|---|---|---|
| 1 차 진입 | 161 | 136 | **75** | 0 | **1** |
| 1 차 마감 | 161 | 136 | **25** | 0 | **1** |
| **재투입 마감** | 161 | ★**161** | ★**0** | **0** | ★**0** |

★**PASS 가 136 → 161 로 뛴 이유** = 1 차 ⑸ 에서 설명한 그대로다. PASS 는 **자식 3 전원 일치**일 때만 세므로 SW 가 남은 25 file 은 FND·TPD 가 붙어도 안 넘어갔다. SW 가 붙자 **25 가 한꺼번에 PASS 로 이동**(136 + 25 = 161). ⟹ ★**1 차의 「PASS 불변은 정상」 진단이 여기서 검증됐다.**

★**exit 0 은 만든 값이 아니라 나온 값**이다. 1 차에서 `1` 을 그대로 적었고 이번에 `0` 을 그대로 적는다 — **분모를 좁힌 적이 없다**(S10 미발).

★**stale ref WARN 은 여전히 뜬다**(1 회). T3 병기의 **예정된 귀결**이고 결함이 아니다 — 끄려면 백틱 인용 삭제 = S5.

## T2-⑹ ChangeBudget (단위 = 행 · 층 분리)

| 층 | 실측 | 비고 |
|---|---|---|
| master 저작 | **0** | ★본 투입은 rule·헌법 **무접촉** |
| master 자동 생성 | `.auto-memory/propagation-status.md` **+11/−39** | `verify-sync` auto-region 재생성(수기 0) |
| **자식 전파 축** | **측정만** — SW **25 file +579/−8** | FND·TPD 와 **numstat 동일** ⟹ 셋이 같은 지점에서 출발했다는 확증 |
| 삭제 | **0** | 전파 = master 판 복제 |
| §9 산출 | 본 절 추가분 | ⒂ 계열에 병기 |

## T2-⑺ STOP 11 (0 도 값)

| # | 실측 | 자 |
|---|---|---|
| **S1** 미착지 전파 | **0** | 두 자 **둘 다** 충족 실측(T2-⑴) + 보강 자 3 |
| **S2** 의도적 제외 전파 | **0** | 제외 7종 ∩ 분모 = `0` · `gradle.properties` ∩ = `0` · **G6 `0 2` 불변** |
| **S3** 동결 3 접촉 | **0** | `--targets Selfward` 단독 |
| **S4** 선재 porcelain commit | **0** | ★**G4 첫 칸 `11` → `11` 불변** · staged = 정확히 25 · `git add -A` 0 · `commit -a` 0 |
| **S5** stale ref 삭제 | **0** | G5 `12` 불변 |
| **S6** 자식 자체 file 수정 | **0** | write = 전파 25 경로 한정 · C14 = **no-op 3/3** |
| **S7** push | **0** | 미실행 · 4 repo 전부 ahead 잔존 |
| **S8** master rule 내용 변경 | **0** | master 저작 0 행 |
| **S9** `server/`·`supabase/` | **0** | staged hit `0` (★`?? supabase/_ops/…` = **선재 untracked** · 무접촉) |
| **S10** exit 맞추려 분모 좁힘 | **0** | `--target` 회피 0 · exit 그대로 |
| **S11** 발주 ↔ 실물 갈림 | ★**적용 1** | 발주 「17 file」 ↔ 실측 **25** ⟹ 실물 채택 (발주 문면 무수정) |

## T2-⑻ commit 계수 자기 대조 (★master ↔ 자식 따로)

**자식**: `5f1e0b4` (Selfward · 25 file · +579/−8) — 계획 1 == 실측 **1** ✅ · ahead 55 → **56**
**master**: 본 REPORT commit 1 — 계획 1 == 실측 **1** ✅ · ahead 44 → **45**

**누계** (= 1 차 + 재투입): master **4** · 자식 **3** (FND 1 · TPD 1 · SW 1) · **push 0**.

## T2-⑼ 회부 해소 + 원장

| 항 | 1 차 | **재투입** |
|---|---|---|
| **T2 Selfward 전파** | ★회부 | ★**마감** (`5f1e0b4`) |
| ㉡ #244 Coin 이관 ⑴ propagation | 부분 마감 | ★**완전 마감** |
| 남는 이관 | push 1 + SW propagation 1 = **2** | ★**push 1** (= 발주 §0-ⓓ 예상 복귀) |

★**부채 1 잔존**(= 1 차 ⒀ 등재분 · 본 투입 scope 밖): hot 행 **400B 상한 위반 1**(`MASTER-RULE-HISTORY-SECTION-001` **431B**). 다음 판이 그 entry 를 demote 할 때 자연 해소.

## T2-⑽ Negative space

**고려했으나 hot 제외**: ① `--all`(= 분모 넓음) ② `--prune`(= 계약 밖) ③ stale ref WARN 소거(= S5) ④ 431B 행 정정(= 남의 entry) ⑤ 1 차 REPORT ⑴~⒂ 본문 수정(= **시점 기록** · 병기로만 정정) ⑥ SW 선재 11 정리(= 다른 판의 것) ⑦ `.ai/reports/…/gate.sh`·`rollcheck.py` 가 tracked 인 것의 적부(= Selfward 소관).

★**새로 만든 부채 0.** ★**본 투입 자수 1** = T2-⑶ 리다이렉트 건(값 손상 0).

## T2-⑾ 판단·선택

| # | 판단 | 선택 | 근거 |
|---|---|---|---|
| 1 | 발주 「지금 11 · tracked 1」 인용 vs 재측 | ★**재측** | 그것도 「그때의 값」 |
| 2 | 두 자만 vs 보강 자 3 추가 | **추가** | 계약 충족 ≠ 「정말 끝났나」 |
| 3 | 발주 「17」 vs 실측 25 | **25** | 17 이면 8 잔존 |
| 4 | `--all` vs 파생 | **파생** | `--all` ⊃ verify-sync |
| 5 | exit 만 vs exit + 요약 줄 | **둘 다** | K-181 |
| 6 | 1 차 REPORT 수정 vs 병기 | ★**병기** | 시점 기록은 고치면 사라진다 |
| 7 | 1 차 ⒀ 「exit 0」 오독 방치 vs 정정 | ★**정정** | T2-⑶ |

---

*본 REPORT 끝 · `MASTER-PROPAGATE-DRIFT17-001` · 2026-08-30 KST · **1 차 + T2 재투입 누계 = master 4 + 자식 3 commit · push 0** (Coin 소관) · 최종 = `verify-sync` **PASS 161 / DRIFT 0 / MISS 0 · exit 0**.*
