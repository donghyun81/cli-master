# `MASTER-DOC-MANIFEST-SWEEP-003` — REPORT (규약이 자기 이력을 못 적게 막은 것을 푼다)

> 집행 = cli session · 2026-08-30 KST · cwd = `~/AndroidStudioProjects/claude-cli-master` · Mode **M5** (CLI 운영 레이어형)
> 발주 = `~/AndroidStudioProjects/cc-paste-MASTER-DOC-MANIFEST-SWEEP-003.md` (저작 = cowork 210차-u)
> 축 = `reporting.md` §15.1 **13 축 전량** (= §8.1 ⑾)

---

## §0. 배경 재진술 (= §8.1 ⑵ · K-155 · ★자기 말 · verbatim 복붙 아님)

이 판이 푸는 것은 **문서의 내용**이 아니라 **규약이 자기를 적을 자리를 스스로 막아 놓은 구조**다. `CLAUDE.md §16-1` 은 「master 의 **모든** cli infra 변경은 본 `CLAUDE.md §15` 표에 cycle entry 추가 **의무**」라고 못 박는다 — 예외 조항이 없다. 그런데 §15 는 제목 괄호에 「**hot 상한 3 entry · 각 ≤400B**」를 박아 두었고, 진입 시점 그 표는 **정확히 3 으로 만석**이었다. ⟹ 등재하려면 **반드시 demote 가 동반되고**, demote 는 `.auto-memory/master-cycle-history-COLD.md` 의 표를 지난다. 판 B(`MASTER-DOC-MANIFEST-SWEEP-002`)의 S1 은 「COLD 표 불가침」이었고 **그 판단 자체는 옳았다**(구 문면 보존). 다만 그 STOP 이 **등재 의무의 유일한 착지 경로**를 통째로 잠갔고, 판 B 는 자기 의무를 못 지킨 채 「만든 부채 1」로 적고 닫았다. **한 판이 못 적으면 다음 판도 같은 벽에 막힌다 — 영구 누락이다.** 본 판의 처방은 STOP 을 없애는 것이 아니라 **좁히는 것**이다: S1 = 「기존 165 데이터행의 **내용** 변경 = STOP · **말미 append 는 계약**」. **보호 대상(구 문면)은 그대로 지키면서 경로만 연다** — 이것이 K-167 의 실물이고, 본 판이 T5 로 규칙화한 「STOP × 계약의 착지 경로」(㉣ K-168)의 첫 적용 사례다.

**§0-ⓑ 좌표 4 = 실제로 열어 확인** (인용 아님 · 각 좌표 round-trip · rev = 진입 `bf603be`):

| 좌표 | 열어서 확인한 내용 | 판정 |
|---|---|---|
| `CLAUDE.md:293` §15 | 제목 = `## 15. master cycle 진행 이력 (= **hot 상한 3 entry · 각 ≤400B**)`. ★**`:296` 에 「demote 대상 = 표의 맨 위 행(= 최고령) · 신 entry = 맨 아래」**(2026-08-29 `MASTER-ENGINEERING-BASELINE-002`) — **순서가 정본**이라 날짜 동률에도 안 흔들린다. T1 의 demote 대상 선정은 **이 문면 하나로 결정**됐다 | 실재 ✓ |
| `CLAUDE.md` §16-1 | `:310` = 「master 의 모든 cli infra 변경은 본 CLAUDE.md §15 표에 cycle entry 추가 의무」. ★**「모든」에 예외 단서가 없다** — 본 판이 자기를 등재해야 하는 근거가 여기다(§9-② 판정의 1 순위 근거) | 실재 ✓ |
| `.claude/rules/rule-footer-common.md:10` | 「**이력 절 등재 의무**」 원문 실재. ★단서 「다른 cycle 을 **인용만** 하는 헤더는 대상 아님 — 자는 **귀속**과 **인용**을 못 가른다(K-133)」까지 같은 행에 있다. ★**이 단서가 본 판 G10 둘째 칸 편차를 그대로 설명한다**(§5) | 실재 ✓ |
| `docs/rules/verification-and-review.md §0.1` | `:13` 헤딩 + 항 5. ★**`:17`(= §0.1 기준 4 번째 줄 = 발주가 말한 `:4`)이 「부재 판정 = 전수 트리에서만 · subset 위의 「없다」는 **무효**」를 이미 담고 있다** ⟹ T4 는 **신설이 아니다**(S5 근거) | 실재 ✓ |

**이 판이 아닌 것 (1줄)**: Selfward 전량(판 15 병행 중 · 접촉 0) · `scripts/`·`server/`·`supabase/` · propagation 실행 · push · **COLD 기존 165 데이터행의 내용**(= append 만) · 이력 절 부재 나머지(마감 실측 **25** / 분모 42) · `.ai/tasks/` 문면.

---

## §1. 판정 (축 1)

**PASS.** 계약 **T1~T7 7/7 착지** · 게이트 **11/11 실행** (계약 충족 10 · **편차 1 = G10 첫·둘째 칸** · §5 에서 자로 재현) · STOP **10 항 미발동**(S1 = 데이터행 변경 **0** 실측) · production **0 LOC** · 보호 5 **무접촉** · **순삭제 0**(demote = sha 쌍 대조로 **이동** 증명) · 문서 축 밴드 **준수**(+56 vs `+55~+95`).

**단서 2** — ⑴ ★**발주 지시 1 건 불이행 + 정정**(회차 표기 `10 → 11` → 실측 **18**  · S10 발동 · §5) ⑵ ★**Coin 회부 1**(판정선 「5」의 **근거 구간** 정정 · 값은 유지 · §9-④).

---

## §2. BASELINE 진입 재측 (축 2 · §8.1 ⑴ · ★발주 인용값 아님)

```
HEAD bf603be · ahead 24 · porcelain 0 · branch main          ← 발주 §0-B 와 일치
CLAUDE.md §15 hot = 데이터행 3 / 상한 3 (만석) · SWEEP-002 등재 0 · 본 판 등재 0
COLD = 165 데이터행 / 163 고유 ID / 중복 2 · 자기서술 「165 데이터행」 3 곳(:1 :13 :27)
demote 후보 2(TASK-PURPOSE-CONTRACT-001 · DOC-MANIFEST-SWEEP-001) COLD 데이터행 0 hit
```

★**발주 「전」 값 11 항 = 전량 재현**(§9-① · 게이트 1 회 실행 · 재타이핑 0).

---

## §3. 착지 (축 3 · §8.1 ⑶ · 좌표 = `path` | `line` | `anchor` · **rev = 마감 HEAD**)

| T | path | line | anchor | 판정 |
|---|---|---|---|---|
| **T1** | `CLAUDE.md` | `:301` `:302` | `\| MASTER-DOC-MANIFEST-SWEEP-002 \|` · `…-003 \|` | 등재 2 ✓ |
| **T1** | `.auto-memory/master-cycle-history-COLD.md` | `:200` `:201` | `\| MASTER-TASK-PURPOSE-CONTRACT-001 \|` · `\| MASTER-DOC-MANIFEST-SWEEP-001 \|` | demote 2 도착 ✓ |
| **T1** | 〃 | `:1` `:13` `:31` | `167 데이터행 / 165 고유 cycle ID` | 자기서술 3 곳 ✓ |
| **T1** | 〃 | `:15` `:17` | `18 회차 demote 의 성격` · `17 회차 = 무번호 demote` | lineage 채번 ✓ (S10) |
| **T1** | `CLAUDE.md` | `:305` | `★**회차 표기 정정**` | S10 정정 ✓ |
| **T2** | `.claude/rules/rule-footer-common.md` | `:11` | `★**「신설·개정」 판정선**` | ✓ |
| **T3** | `docs/rules/verification-and-review.md` | `:24` | `★**자의 「깊이」 — 패턴이 대상의 깊이 범위를 덮는가**` | ✓ |
| **T4** | 〃 | `:18` | `★**위반 실증 — 규약이 없어서가 아니라 있는데 안 지켜서 났다**` | ✓ **신설 아님**(§7) |
| **T5** | `docs/rules/cycle-discipline.md` | `:161` | `★**⑥ STOP × 계약의 착지 경로**` | ✓ |
| **T6** | 〃 | `:148` | `★**밴드 산정 자 — 밴드는 자기가 시킨 것을 세야 한다**` | ✓ |
| **T7** | `docs/rules/verification-and-review.md` | `:266` | `- 2026-08-30 · \`MASTER-DOC-MANIFEST-SWEEP-003\`` | entry 추가 ✓ |
| **T7** | `docs/rules/cycle-discipline.md` | `:202` | `## 명시 cycle 이력` | **절 신설** ✓ (footer 4 entry 흡수 **0**) |
| **T7** | `.claude/rules/rule-footer-common.md` | `:29` | `## 명시 cycle 이력` | **절 신설** ✓ (§9-⑥ 판정) |

---

## §4. 게이트 전/후 (축 4 · 자 = 발주 §6 블록 **awk 추출 후 verbatim** · repo 밖 실행 · ★재타이핑 0)

추출·실행 명령 (= 4 회 동일):
```
( cd /Users/yundonghyeon && bash <(awk '/^```bash$/{f=1;next} f&&/^```$/{exit} f' \
    /Users/yundonghyeon/AndroidStudioProjects/cc-paste-MASTER-DOC-MANIFEST-SWEEP-003.md) )
```

| G | 시점 | 계약 「전」 | 실측 「전」 | 계약 「후」 | 실측 「후」 | 판정 |
|---|---|---|---|---|---|---|
| **G0** | 진입 | `claude-cli-master bf603be 5` | **일치** | `claude-cli-master <HEAD> 5` | `claude-cli-master 1d4d533 5` | ✓ |
| G1 | 마감 | `3 0 0` | **일치** | `3 1 1` | **`3 1 1`** | ✓ 상한 3 불변 |
| G2 | 마감 | `165 3 0` | **일치** | `167 0 2` | **`167 0 2`** | ✓ K-157 전수 |
| G3 | 무관 | `1` | **일치** | `≥2` | **`3`** | ✓ |
| G4 | 무관 | `0 1` | **일치** | `≥1 ≥2` | **`4 2`** | ✓ |
| G5 | 무관 | `0 8` | **일치** | `≥1 ≥11` | **`1 13`** | ✓ 양성대조 상승 |
| G6 | 무관 | `0 7` | **일치** | `≥1 ≥10` | **`1 11`** | ✓ |
| G7 | 마감 | `2 0` | **일치** | `3 ≥1` | **`3 3`** | ✓ 절 신설 |
| G8 | 마감 | `0` | **일치** | `0` | **`0`** | ✓ |
| G9 | 마감 | `0 24` | **일치** | `0` · `24→27~30` | **`0 28`** | ✓ |
| **G10** | 마감 | `0 0` | **일치** | `1 0` | **`2 1`** | ★**편차** → §5 |

★**「전」 11/11 재현** · **「후」 계약 충족 10/11** · 편차 1 = G10.

---

## §5. 편차 · 이의 (축 5 · ★방어 아님 — **자로 재현해서** 붙인다)

### ⓐ G10 편차 — ★**자가 「귀속」과 「인용」을 못 가른다** (= 발주 자신이 `rule-footer-common.md:10` 에 적어 둔 그 결함)

| 칸 | 자 | 계약 | 실측 | 원인 |
|---|---|---|---|---|
| 첫째 | `grep -c 'DOC-MANIFEST-SWEEP-003' CLAUDE.md` | `1` | **`2`** | 등재 행 1 + **S10 회차 정정 note 1**(= 발주가 예견 못 한 산출) |
| 둘째 | `grep -c 'DOC-MANIFEST-SWEEP-003' <COLD>` | **`0` 불변** | **`1`** | 18 회차 demote note 의 **수행 cycle 인용** 1 |

★**둘째 칸이 묻는 명제는 「본 판이 COLD 에 **데이터행으로** 있는가」**(= 발주 §6.1 「있으면 자기를 demote 한 것」)이고, 그 명제로 재면 **0** 이다:

```
grep -c '^| MASTER-DOC-MANIFEST-SWEEP-003 ' <COLD>   →  0    ← 명제가 묻는 것
grep -c 'DOC-MANIFEST-SWEEP-003'            <COLD>   →  1    ← 게이트가 재는 것
mention 위치 = :15 (= 18 회차 note · 데이터행 아님)
```

**선례 양성대조** (= 회차 note 가 수행 cycle 을 인용하는 것이 관례인가):

| cycle | 데이터행 | 전체 mention | 차 |
|---|---|---|---|
| `MASTER-BRAND-TOWARD-INFRA-001` | 1 | 2 | +1 (= 16 회차 note) |
| `MULTI-REPO-RENAME-TOWARD-001` | 1 | 2 | +1 (= 15 회차 note) |
| `MASTER-LIFECYCLE-4ACTIVE-REALIGN-001` | 1 | 2 | +1 (= 14 회차 note) |

⟹ **회차 note 는 자기를 수행한 cycle 을 항상 인용한다.** 본 판만 그것을 안 하면 **18 회차 채번의 출처가 사라진다**(hot `CLAUDE.md §15` 꼬리가 「회차 전량 열거 = COLD §1 heading lineage」라고 그 file 을 가리키므로). ★**게이트 값을 맞추려고 인용을 지우면 hot 의 주장이 근거를 잃는다** — 값이 아니라 명제를 따랐다. **판정 = 편차이되 위반 아님.**

### ⓑ ★발주 지시 불이행 1 — 회차 표기 (S10 발동 · **발주와 실물이 갈리면 실물이 정본**)

발주 T1 은 「demote **회차 표기** 관용을 따른다(`§15 cold 재배치` 꼬리 = **10 회차 → 11 회차**)」로 지시했다. **따르지 않았다.** 자:

```
COLD §1 lineage 명시 회차 = 2·7·8·9·11·12·13·14·15·16   → 최고 16 (2026-08-15)
그 뒤 무번호 demote 1 실재 = b3a8857 (MASTER-MEASURE-DISCIPLINE-001 · 164→165)
⟹ 본 판 = 18 회차
```

⟹ hot 꼬리의 「10 회차」는 **2026-07-29 에 갱신이 멈춘 stale** 이고, 「11」로 적었으면 **stale 을 한 칸 밀어 재생산**했을 뿐이다. **관용을 따르는 것과 값을 재는 것은 다르다.** 조치 = ⑴ hot 꼬리를 **18 회차**로 정정(구 문면 **취소선 병기** · 삭제 0) ⑵ COLD lineage 에 **18 회차 note 신설** + **17 회차 소급 채번**(= 무번호였던 것을 부재로 남기지 않는다 · `cycle-discipline.md 33)-ⓐ` 정합) ⑶ ★**재발 방지 문면** = 「회차는 demote 를 수행한 판이 **같은 step 에서** 채번한다」를 COLD 17 회차 note 에 박음.

### ⓒ 자기 이의 2 — ★**내가 나를 잡은 것** (= 자를 안 돌렸으면 그대로 통과했다)

1. ★**「≤400B」를 틀린 자로 잴 뻔했다.** 첫 측정에 `awk length($0)` 를 썼다. **awk 의 `length()` 는 로케일에 따라 문자수를 낸다** — UTF-8 로케일이면 한글 1 자 = 1 로 세어 **실 byte 의 1/3 을 보고**했을 것이다. 본 환경은 byte 로 셌지만(`bytes = chars + 1` = 개행분만 차이 · 실측) **그건 환경 덕이지 자 덕이 아니다.** `wc -c` 로 재측했더니 `SWEEP-002` 초안 = **402B ⟹ 상한 400 위반**. → 398B 로 정정 후 등재. (= `verification-and-review.md §0.7 환경의 자` 의 직접 실현)
2. ★**게이트 문면을 두 번 「예측값」으로 적을 뻔했다.** ⑴ T7 이력 entry 에 G6 을 **`2 11` 로 예측해 적었다가** 실측 `1 11` 로 정정(그 사실 자체를 entry 에 박음). ⑵ `### 30` 에 「**자** =」를 **볼드로** 적어 게이트 `grep -c '자 ='` 가 **0** 을 냈다 — 값이 아니라 **표기**가 자를 빗겨간 경우. → 평문 `자 =` 로 교정. **둘 다 발주 §6.2-3 이 자수한 「확인했다고 쓰고 안 잰 것」과 같은 형태**이고, 이번엔 **자가 먼저 잡았다.**

### ⓓ 금지 위반 1 (자수)

### ⓓ 금지 위반 **2** (자수 · ★둘 다 자기 발견 · 은폐 0)

1. ★**진입 첫 명령에 `2>/dev/null` 을 썼다**(ahead 측정 fallback). 발주 §11 「명령에 `2>` 를 치지 않는다」 위반. **발견 즉시 중단 · 이후 전 명령 `2>` 0**. 값 오염 없음(ahead **24** = 계약 일치).
2. ★**마감 직전 `git grep` 을 1 회 썼다** (= 진입 rev 의 이력 절 보유 계수 `git grep -l '명시 cycle 이력' bf603be -- 'docs/rules/*.md'`). 발주 §11 **명시 금지어**. 대체 자 = `git show <rev>:<path>` 순회 또는 마감 판 grep + diff 대조. **값 자체는 재현 가능하고 오염 없으나, 금지 명령으로 얻은 값이므로 그 사실을 값 옆에 남긴다**(= 「무엇으로 쟀나」 의무 · `verification-and-review.md §0.1`).

⟹ **2 건 다 「의도적 우회」가 아니라 「손이 먼저 나간 것」**이고, 그래서 **금지 목록을 읽은 것이 집행을 막지 못했다**는 실측이다. **S9 는 발동으로 계상한다**(0 이 아니다).

---

## §6. 「나를 의심하는 절차」 답 (축 6 · §9 **7 항 전량** · ★방어 0)

**① 「전」 값 11 항 전량 재현되는가.** ★**전량 재현.** 게이트 1 회 실행으로 `G0 claude-cli-master bf603be 5` / **`G1 3 0 0`** / **`G2 165 3 0`** / `G3 1` / `G4 0 1` / `G5 0 8` / `G6 0 7` / `G7 2 0` / `G8 0` / `G9 0 24` / `G10 0 0` — **11/11 일치**. 발주가 실제로 돌려 확정했다는 진술이 **참**임을 확인.

**② demote 2 가 맞는가 — 미루면 1 로 끝난다.** ★**2 가 맞다. 미루는 쪽을 제안하지 않는다.** 근거 3:
- ⒜ `CLAUDE.md §16-1` 은 「**모든** cli infra 변경」이라 적고 **예외 단서가 없다**(§0-ⓑ round-trip 확인). 본 판은 `.claude/rules/` + `docs/rules/` 를 고친 **명백한 cli infra 변경**이다 ⟹ 미루면 **본 판이 자기가 집행하려는 바로 그 조항을 위반한 채 닫힌다.**
- ⒝ 발주 §0-ⓐ #1 의 진단이 「한 판이 못 적으면 다음 판도 못 적는다 = 영구 누락」이다. **미루는 선택지는 그 실패를 한 번 더 재현하는 것**이고, 그것이 정확히 판 B 가 한 일이다.
- ⒞ 「밴드와 위험이 절반」이라는 근거는 **과대평가**다. 실측 = demote 1 → 2 의 증분은 **COLD 데이터행 +1 · hot 행 −1/+1** 뿐이고, **기전이 같은 연산의 1 회 반복**이다(경로·STOP·자 전부 동일). 실제 밴드 차 ≈ **4 행**. 위험이 두 배가 되는 지점이 없다.
> ★**단, ②가 옳으려면 ⑦이 성립해야 한다**(자기 등재의 자기 참조) — 아래 ⑦에서 따로 답한다.

**③ 「demote = 이동, 삭제 0」이 성립하는가 — 다르면 문면을 고쳐라.** ★**성립한다. 문면 수정 불요.** sha 쌍 대조:

| hot 원문 (진입 `bf603be`) | sha(앞16) | COLD 도착 (마감) | sha(앞16) | 판정 |
|---|---|---|---|---|
| `bf603be:CLAUDE.md:300` | `ea01847c3c1e7713` | `<COLD>:196`(마감 `:200`) | `ea01847c3c1e7713` | **일치** |
| `bf603be:CLAUDE.md:301` | `1e92f53ce1ba092c` | `<COLD>:197`(마감 `:201`) | `1e92f53ce1ba092c` | **일치** |

자 = `git show bf603be:CLAUDE.md | awk 'NR==n' | shasum -a 256 | cut -c1-16` ↔ `awk 'NR==n' <COLD> | …`. ⟹ **재작성이 아니라 verbatim 이동.** 보강 실측 = `git diff bf603be..HEAD -- <COLD> | grep -c '^-| '` = **0**(데이터행 제거 0) · 진입 165 행 ∩ 마감 = **165/165**(자 = `comm -12` on sorted sets).

**④ 판정선 「5」가 자의적이다.** ★**값은 유지하되 근거를 정정한다 — 발주의 근거가 틀렸다.** 발주는 「실측 경계 = **4/4 ↔ 7/1** 사이 · 4 와 7 사이 어디든 근거가 같다」로 적었다. 그러나 **그것은 추가 행(gross) 칸을 읽은 것이고, 판정선의 축은 「순증(net)」**이다. 순증으로 재면:

| commit | numstat | **순증** | 판정 |
|---|---|---|---|
| `bfb8f9c`·`a47f61d`·`558af38`·`bb9e742` | 1/1·1/1·2/2·4/4 | **0 · 0 · 0 · 0** | 미등재 |
| `9e28613` | 7/1 | **+6** | 등재 |
| `08104e9` | 67/0 | **+67** | 등재 |
| `cf063a8` | 9/208 | **−199** | 등재(헤더 축이 잡는다) |

⟹ 실측이 지지하는 구간은 **[1, 6]** 이지 (4, 7) 이 아니다. **5 는 그 구간 안이라 값은 살아남지만, 값을 정당화한 자가 틀렸다.** 값 변경 = **Coin 결정 사항**이므로 **유지**하고 구간만 정정해 문면에 박았다 + **회부**(§9). ★이것이 `MASTER-MEASURE-DISCIPLINE-001` 의 「자를 썼다는 것은 옳은 자라는 증거가 아니다」의 재발이다.

**⑤ 「상한 없는 수량자」가 항상 옳은가.** ★**아니다 — 처방을 좁혀서 적었다.** 「목차 대상 = `#`·`##` 한정」처럼 **깊이를 제한해야 옳은 명제가 실재**한다. ⟹ 문면을 **「상한 없는 수량자를 *기본값*으로 쓴다」 + 「★제한 자체가 아니라 **근거 없는** 제한이 병이다」 + 「제한하면 왜 그 깊이까지가 명제의 범위와 같은지를 같은 줄에 적는다」** 3 단으로 적었다(= 발주가 제안한 「제한할 때는 이유를 적는다 쪽만 남기라」보다 **한 칸 넓게** — 기본값 규정을 버리면 실측 사고[`^+#\{1,3\} ` = 0]를 못 막기 때문). 양성대조 항(깊이 한 칸 넓혀 재기)도 함께 신설.

**⑥ `rule-footer-common.md` 가 자기 이력을 적어야 하는가 — 발주가 판정 안 했다.** ★**열어서 판정했다. 대상이다.** 근거 3 + 반대 근거 기각 1:
- ⒜ **경로** = `.claude/rules/` = 그 file `:3` 이 스스로 선언한 적용 범위(`docs/rules/**` + `.claude/rules/**`) **안**.
- ⒝ **행위** = 본 판이 `:10` bullet 을 **개정**했고, 그 개정은 **자기가 방금 쓴 판정선의 두 축을 다 넘는다**(순증 `+23` ≥ 5 · 헤딩 증감 = 절 신설로 > 0).
- ⒞ **문면** = 의무 문장에 **자기 예외 조항이 없다**.
- ★**기각한 반대 근거** = `:4` 의 「본 file 은 절 **보유**가 아니라 **의무 서술**」. 이 문장은 **양성대조 계수에서 자기를 뺀 이유**를 적은 것이지 **의무에서 면제**한다는 뜻이 아니다. ⟹ **의무를 쓴 file 이 자기를 면제하면 그 의무는 자기 자신을 반례로 갖는다** — `MASTER-DOC-MANIFEST-SWEEP-001` 의 「문면이 자기를 틀리게 적으면 다음 사람의 분모가 된다」가 정확히 이 형태다. 조치 = **「명시 cycle 이력」 절 신설** + 판정 근거를 그 절 머리에 박음(다음 판이 다시 안 묻도록).

**⑦ 본 판이 자기를 등재하는 자기 참조가 어색한가.** ★**어색하지 않다. 단 한 가지 실물 제약이 있고, 그것은 관례로 이미 해결돼 있다.**
- **구조** = hot(최근 3) ↔ COLD(demote 아카이브)는 **다른 층**이다. 방금 닫힌 판이 hot 에 있는 것이 정상이고, **자기를 COLD 로 내리는 것만이 모순**이다(= 넣자마자 내린 것). G10 둘째 칸이 그 모순을 막는 자이고, **데이터행 기준 0 으로 성립**한다(§5-ⓐ).
- ★**실물 제약** = entry 의 `[R](.ai/reports/…/REPORT.md)` 링크가 **작성 시점엔 아직 commit 되지 않은 file** 을 가리킨다. **선행 entry 전량이 같은 구조**이고(REPORT commit 이 항상 뒤따른다), 본 판도 commit 4 로 착지시켰다 ⟹ **관례로 해결된 기지 사항** · 신 규약 불요.
- **남는 어색함 1** = 등재 문면을 **자기가 자기에 대해** 쓰므로 과장 검증자가 없다. 완화 = 요약을 **게이트 값과 좌표로만** 적고 평가어를 뺐다.

---

## §7. ★T4 가 신설이 아님을 절 좌표로 증명 (= §8.1 ⑻ · S5 방어)

| 자 | 진입 `bf603be` | 마감 HEAD | 판정 |
|---|---|---|---|
| `grep -c '^### 0\.' <V>` (= §0 하위 절 수) | **7** | **7** | ★**절 집합 불변 = 신설 0** |
| 절 목록 | `0.1 범위`·`0.2 좌표`·`0.3 계수`·`0.4 정정`·`0.5 순서·정렬`·`0.6 명제`·`0.7 환경` | **동일** | 신 절 0 |
| T4 실증이 속한 절 (자 = `awk 'NR<=18 && /^### /{h=$0} END{print h}'`) | — | **`### 0.1 범위의 자`** | 기존 절 |
| 부모 항 (= 발주가 말한 `:4`) | `:17` `- **부재 판정** = 전수 트리에서만 … subset 위의 「없다」는 **무효**다.` | **무접촉**(원문 그대로) | 부착 대상 실재 |
| T4 실증 | — | `:18` (= `:17` 바로 아래 **하위 bullet**) | **부착** |

⟹ **새 절도, 새 항도 만들지 않았다.** 기존 `:17` 항 **아래에 하위 bullet 3** 을 달았을 뿐이고, 그 중 마지막 bullet 이 **「왜 신설이 아닌가」를 문면에 직접 적는다**(= 「규칙 부재와 규칙 위반은 다른 병이고 처방도 다르다」). **S5 미발동.**

★**부수 관찰** — G4 둘째 칸(`grep -c 'subset'`)이 초안에서 **1 로 안 올랐다**. 실증을 적으면서 「subset」이라는 **낱말을 안 썼기 때문**이다(내용은 맞고 어휘만 달랐다). ⟹ 문면을 고쳐 **위 `:17` 원문의 낱말을 그대로 이어받게** 했다(「`SKILL.md` 1 file 은 그 skill 트리의 **subset** 이고, 위 줄이 이미 「subset 위의 「없다」는 무효」라 못 박았다」). ★**게이트를 맞추려 키워드를 심은 것이 아니라, 실증이 인용해야 할 원문 낱말을 안 쓰고 있었던 것**이다 — 자가 그 누락을 잡았다.

---

## §8. T2 경계표 재현 (= §8.1 ⑹ · 7 commit) · T3 깊이표 재현 (= ⑺)

### ⓐ T2 경계표 (★**분모 = 각 commit 이 만진 「그 rule file」** · 발주 표는 file 을 안 적었으나 실측으로 특정)

자 = `git show --numstat <sha> -- <path>` × `git show --format= <sha> -- <path> | grep -c '^[+-]#\{1,\} '`

| commit | file | numstat | 순증 | 헤더±(깊이 무관) | 발주 표 | 재현 |
|---|---|---|---|---|---|---|
| `bfb8f9c` | `reporting.md` | **1/1** | 0 | **0** | 1/1 · 0 | ✓ |
| `a47f61d` | `reporting.md` | **1/1** | 0 | **0** | 1/1 · 0 | ✓ |
| `558af38` | `reporting.md` | **2/2** | 0 | **0** | 2/2 · 0 | ✓ |
| `bb9e742` | `reporting.md` | **4/4** | 0 | **0** | 4/4 · 0 | ✓ |
| `9e28613` | `reporting.md` | **7/1** | +6 | **1** | 7/1 · 1 | ✓ |
| `cf063a8` | `reporting.md` | **9/208** | −199 | **32** | 9/208 · 32 | ✓ |
| ★`08104e9` | `pencil-mcp-tools-reference.md` | **67/0** | +67 | **0**(깊이1~3) / **6**(무관) | 67/0 · 0/6 | ✓ |

**7/7 재현.** ★**발주 표의 「file」 칸이 비어 있어 처음엔 commit 전체를 분모로 잡아 전량 불일치**했다(예: `bfb8f9c` = 21/15). **분모를 file 로 좁히자 7/7 일치** ⟹ 발주 값은 옳았고 **분모 표기가 빠져 있었다**(회부 없음 · 본 표가 그 칸을 채운다).

**h±0 등재 선례 2** (= 「헤더 증감은 충분조건이지 필요조건이 아니다」의 근거): `b3a8857`(reporting.md 4/2 · h± **0**) · `3551bf5`(5/0 · h± **0**) — **둘 다 등재**. ✓
**rename 제외**: `de37a6e` = `R097 .claude/rules/reporting.md → docs/rules/reporting.md` · **3/3** (자 = `--name-status -M`). ✓

### ⓑ T3 깊이표 (`08104e9` · `.claude/rules/pencil-mcp-tools-reference.md` · 단위 = `+` 헤딩 행)

| 깊이 | `#` | `##` | `###` | `####` | `#####` |
|---|---|---|---|---|---|
| 실측 | **0** | **0** | **0** | ★**6** | **0** |

⟹ `^+#\{1,3\} ` = **0**(거짓) · `^+#\{1,\} ` = **6**(참). **67 행을 추가한 commit 을 헤더 자가 0 으로 쟀다.** 발주 값과 **전량 일치**.

---

## §9. ChangeBudget (축 7 · §8.1 ⑽ ★**밴드 정정 = T6 자기 적용**)

자 = `git diff --numstat bf603be..HEAD` (= 층별 · 순증)

| 층 | 계약 밴드 | 실측 | 판정 |
|---|---|---|---|
| **실코드** | `0` | **0** (자 = `--name-only \| grep -c -e '^scripts/' -e '^server/' -e '^supabase/' -e 'src/'` = **0**) | ✓ |
| **문서(본문)** | `+55 ~ +95` | **+56** (gross `+62 / −6`) | ✓ **밴드 안** |
| **§9 산출 축**(별 계상) | `+20 ~ +45` | **+366** (= 본 REPORT 행수 · 자 = `wc -l` · ★자기 참조 = 이 값을 적는 행 자체가 분모 안이라 **같은 행 치환으로만 고정점**이 된다) | ★**초과 +321** → 아래 정정 |
| **삭제** | `0` | **0** — demote 2 = **이동**(§6-③ sha 쌍) · 나머지 `−6` = 자기서술 3 + hot 행 2 + 꼬리 1 의 **제자리 갱신**이며 **구 문면 전량 병기 보존** | ✓ |
| **test** | `±0` | **0** | ✓ |

★**밴드 정정 + 정정 사실 기재** (= T6 처방 ⑵⑶ **자기 적용**):
- **문서 본문 축은 정정 불요** — `+56` 이 `+55~+95` 안이다. ★발주가 **T1 hot 2 + COLD 2 + 자기서술 3 + T2 표 8 + …** 로 항목별 계상한 것이 **실측과 맞았다**(T6 처방 ⑴ 의 유효성 실증).
- ★**§9 산출 축 `+20~+45` 는 과소 산정이었다 → 실측 `+366` 로 정정**한다. 원인 = 발주가 **「§9 7 항 답변 + 실측표 2~3」만** 계상했으나, 같은 발주 §8.1 이 REPORT 에 **⑴~⑾ 11 개 필수 항**(이동 증명 쌍 · 자기서술 3 곳 전수 · 경계표 7 commit · 깊이표 · 절 좌표 증명 · 부채 시점 대조 · 축 13 전량)을 **따로 요구**했다. ⟹ ★**발주가 시킨 산출을 발주의 밴드가 또 안 세었다** — **T6 이 고치려던 바로 그 형태가 T6 을 신설하는 판에서 재발**했다.
- ★★**그리고 나도 같은 자리에서 한 번 더 했다** — 본 절 초안에 실측 자리에 **`+318` 을 적었다.** 그 수는 **재지 않고 쓴 값**이고, `wc -l` 로 재니 **`+366`** 이었다(오차 **+48**). ⟹ **T6 의 「예측값을 실측 자리에 적지 않는다」를 T6 을 신설하는 판의 T6 절에서 어겼다.** 자가 잡았기에 정정으로 끝났다(= 본 판 3 번째 자기 검출 · §5-ⓒ 와 동형).
- **판정 = 「정정한 초과」 = 산출**(편차 아님 · T6 문면 정합). ★**정정 없이 통과시켰으면 편차**였다.
- **차기 밴드 권고**(회부) = REPORT 축이 13 이고 §8.1 필수 항이 11 인 판의 §9 산출 축 하한 = **`+300`** (= 본 판 실측 `+366` · 판 B REPORT 실측과 대조 권장).

---

## §10. STOP · 회귀 그물 (축 8 · **0 도 값**)

| # | trigger | 발동 | 근거(자) |
|---|---|---|---|
| **S1** | COLD 기존 165 행 **내용** 변경 | **미발동** | `git diff … \| grep -c '^-\| '` = **0** · 진입 165 ∩ 마감 = **165/165**(`comm -12`) · 변경 3 행 = **자기서술**(계약 = K-157) |
| S2 | hot 4 행 이상 / demote 없이 등재 | **미발동** | G1 첫 칸 = **3** · demote 2 동반 |
| S3 | demote 대상이 맨 위 2 행 아님 | **미발동** | `CLAUDE.md:296` 규약 = 「맨 위 행」 · 실제 제거 = `:300` `:301` = 표 최상단 2 |
| S4 | 자기서술 3 곳 중 일부만 갱신 | **미발동** | G2 둘째 = **0**(= 「165 데이터행」 잔존 0) · 3 곳 전수 `:1` `:13` `:31` |
| S5 | T4 를 **신설**로 처리 | **미발동** | §7 표 (§0.x 절 **7 → 7** 불변) |
| S6 | `scripts/`·`server/`·`supabase/` 접촉 | **미발동** | G8 = **0** |
| S7 | `propagate.sh`·`verify-sync.sh` 실행 | **미발동** | 실행 0 (= Coin 소관) |
| S8 | Selfward 접촉 | **미발동** | 변경 file 5 전량 master · SW 경로 0 |
| S9 | `git add -A` · push · `2>` · `git grep` | ★**부분 발동 1** | `git add -A` **0** · push **0** · `git grep` **0** · ★**`2>/dev/null` 1 회**(진입 첫 명령 · §5-ⓓ 자수 · 이후 0) |
| S10 | 발주 ↔ 실물 갈림 | ★**발동 1** | 회차 표기 (§5-ⓑ) — **실물을 정본으로 채택** |

**보호 5 무접촉** = 변경 file 5 중 보호 목록 교집합 **0**. **동결 3(GB/GD/GT) 쓰기 0.**

---

## §11. 만든 부채 — 시점 대조 (축 ★§8.1 ⑼ · 진입 `bf603be` ↔ 마감 · **같은 자**)

| 항 | 진입 | 마감 | 증감 |
|---|---|---|---|
| `CLAUDE.md §15` 에 `SWEEP-002` 등재 | **0** | **1** | ★**−1 (판 B 부채 해소)** |
| `CLAUDE.md §15` 에 **본 판** 등재 | 0 | **1** | ★**0 (자기 부채 미발생)** |
| COLD lineage 무번호 demote | **1**(`b3a8857`) | **0** | ★**−1 (소급 채번)** |
| hot 꼬리 회차 stale | **1**(10 회차 · 6 회차분 누락) | **0** | ★**−1** |
| 이력 절 부재 rule file | **26** / 42 | **25** / 42 | −1 (= `cycle-discipline` · 분모·자 = 판 B 와 동일[`docs/rules/*.md` × 낱말] · ★`rule-footer-common` 은 `.claude/rules/` 라 이 분모 **밖**이고, 낱말 자로는 `:10` 때문에 **신설 전부터 보유로 계상**됐다 = §11-주) |
| 4-repo propagation 미실행 | (상시 #130) | (상시 #130) | ±0 (= Coin 소관 · 본 판 신규 아님) |
| 판정선 「5」 근거 구간 | — | **회부 1** | ★**+1 (Coin 결정 대기)** |

> ★**§11-주 — 이 행의 자가 두 갈래로 갈린다**(= 본 판이 신설한 T3 「자의 깊이」·K-133 「귀속≠인용」이 **동시에** 걸리는 자리라 명시한다). ⑴ **낱말 자**(`grep -rl '명시 cycle 이력'` = G7 이 쓰는 자) = 마감 `docs/rules` **17** / `.claude/rules` 포함 **19**. ⑵ **절 앵커 자**(`grep -rl '^## 명시 cycle 이력'`) = **4**. 15 의 차는 **헤딩 형태가 제각각**이기 때문이다(실측 census = `## 명시 cycle 이력` 4 · `## N. …` 6 · `## §N …` 4 · `## §F. …` 2 …). ⟹ ★**절 앵커 자는 「절이 있다」를 재려다 「제목 형식이 같다」를 잰다** — 본 판은 **낱말 자**(= 판 B·G7 과 동일)를 정본으로 쓰고, 그 자가 **`rule-footer-common.md` 처럼 절 없이 낱말만 가진 file 을 보유로 오계상**한다는 한계를 함께 적는다. **헤딩 형태 통일 = 별 판**(§15 negative space ⑹).

★**만든 부채 = 1 종**(= 판정선 값 재검토 회부 · **값은 살아 있고 근거만 정정됨** ⟹ 집행 차단 없음). **해소한 부채 = 4 종.** ★**판 B 가 못 적은 것을 적었고, 그 과정에서 자기 부채를 안 만들었다** — 이것이 본 판의 성립 조건이었다(§6-⑦).

---

## §12. 회부 (축 9 · ★「원장 회부」 ≠ 「Coin 이관」 · `cycle-discipline.md 33)-ⓒ`)

**원장 회부** (= 채번 · 추적 대상):
1. ㉡ **#192 후속** — ★**판정선 「5」의 지지 구간 = 순증 축 [1, 6]**(발주가 적은 gross 축 (4,7) 아님). 값 유지 · **Coin 결정 사항**이라 변경 안 함. 자 = §6-④ 표.
2. ㉡ **신규①** — **§9 산출 축 밴드 과소 산정**(계약 `+20~+45` ↔ 실측 `+366`). 차기 하한 권고 `+300`. ★T6 신설 판에서 T6 대상 결함이 재발했다.
3. ㉡ **신규②** — ★**G10 형 자의 결함**: 「데이터행 존재」 명제를 `grep -c '<ID>'` (전체 mention)로 재면 **인용이 귀속으로 계상**된다. 처방 = 존재 명제의 자는 **행 앵커(`^| <ID> `)로** 짠다. (= `rule-footer-common.md:10` K-133 단서의 **게이트 축 instance**)
4. ㉡ **신규③** — `cycle-discipline.md` **§32 신설(`1c30a84`)의 이력 미등재** 소급 (= 본 판 이력 절은 **신설 시점부터 앞으로만** 쌓기로 규약했으므로 소급은 별 판).
5. ㉣ **K-168 착지 완료** — 「계약의 착지 경로」 = `cycle-discipline.md 31)-⑥`.

**별 판 회부** (= §2 발주 지정 유지): 이력 절 부재 **나머지 25**(= `docs/rules` 분모 42 · 낱말 자) · ㉡ #191 COLD 미해석 링크 14 · ㉡ #200 ⑴⑵⑶.

**Coin 이관** (= 소관 이동 · cli 실행 X): **push**(ahead 28) · **4-repo propagation**(`propagate.sh` + `verify-sync.sh` · 변경 file 중 **cli infra 4** = `rule-footer-common.md` · `verification-and-review.md` · `cycle-discipline.md` [+ `CLAUDE.md`/COLD 는 master only]).

---

## §13. commit · ahead (축 10)

| # | sha | 제목 | `--name-only` |
|---|---|---|---|
| 1 | `a3ba108` | `docs(cli): … §15 등재 2 + demote 2 + COLD append (T1)` | `.auto-memory/master-cycle-history-COLD.md` · `CLAUDE.md` |
| 2 | `a0856b2` | `docs(rules): … 판정선 + 자의 깊이 + subset 실증 (T2·T3·T4)` | `.claude/rules/rule-footer-common.md` · `docs/rules/verification-and-review.md` |
| 3 | `1d4d533` | `docs(rules): … 착지 경로 축 + 밴드 자 + 이력 절 (T5·T6·T7)` | `docs/rules/cycle-discipline.md` |
| 4 | (본 REPORT) | `docs(cycle): … REPORT` | `.ai/reports/MASTER-DOC-MANIFEST-SWEEP-003/REPORT.md` |

**최종 ahead = 진입 24 + 본 판 4 = 28** (= 상대식 · `§정정 append` commit 발생 시 그만큼 증가 · `reporting.md §15.2` 정합). **push = 0** (Coin 소관).

---

## §14. 자 대조표 (축 11 · 「자의 이름 ↔ 자를 낸 명령」 · `verification-and-review.md §0.4` 의무)

| 자의 이름 | 명령 | 재는 축 |
|---|---|---|
| 게이트 11 항 | `bash <(awk '/^```bash$/{f=1;next} f&&/^```$/{exit} f' <발주>)` · repo 밖 실행 | 발주 계약 전량 (재타이핑 0) |
| hot 데이터행 | `awk 'NR>293 && /^## 16\./{exit} NR>293 && /^\| [A-Z]/' CLAUDE.md \| wc -l` | 상한 3 |
| entry byte | `awk 'NR==n' <f> \| wc -c` **− 1** | ≤400B · ★`awk length()` 는 로케일 의존 = **부적격** |
| 이동 증명 | `… \| shasum -a 256 \| cut -c1-16` (양쪽) | verbatim 동일성 |
| S1 (데이터행 불변) | `git diff … \| grep -c '^-\| '` + `comm -12 <(…sort) <(…sort)` | 내용 변경 0 · 집합 165/165 |
| T2 경계 | `git show --numstat <sha> -- <path>` × `grep -c '^[+-]#\{1,\} '` | 순증 × 헤딩 증감(**깊이 무관**) |
| T3 깊이 | `git show --format= <sha> -- <path> \| grep -c "^+#\{d\} [^#]"` (d=1..5) | 깊이별 헤딩 |
| rename 판별 | `git show --name-status -M <sha>` | `R###` ⟹ 등재 대상 아님 |
| T4 신설 아님 | `grep -c '^### 0\.' <V>` (진입/마감) + `awk 'NR<=18 && /^### /{h=$0} END{print h}'` | 절 집합 불변 · 소속 절 |
| ChangeBudget | `git diff --numstat bf603be..HEAD` | 층별 순증 |
| COLD 고유 ID | `grep '^\| [A-Z0-9]' <W> \| cut -d'\|' -f2 \| sort -u \| wc -l` | 165 |
| G10 명제형 | `grep -c '^\| MASTER-DOC-MANIFEST-SWEEP-003 ' <W>` | **데이터행** 존재 (≠ mention) |

---

## §15. negative space (축 12 · `reporting.md §13`)

**고려했으나 hot 제외 영역**: ⑴ 이력 절 부재 나머지 **27**(= 자기가 만진 file 만 · §2) ⑵ COLD 「재수록 2」·「12~16 회차」 note 의 **회차 번호 체계 전면 재감사**(= 본 판은 **17·18 만 채번**하고 앞 번호는 무접촉 — 전면 재감사는 별 판) ⑶ `CLAUDE.md §15` 꼬리의 **가산식 재구성**(= COLD `:13` 이 「가산식으로 현행 실측은 재구성되지 않는다[원인 규명 = 별 판]」로 이미 회부해 둔 축) ⑷ **4-repo propagation 실행**(= Coin 소관 · S7) ⑸ `reporting.md §15.1` 에 **「밴드 정정」 칸 신설**(= 축 7 하위 · T6 이 규율 본문을 가지므로 서식 추가는 별 판).

---

## §16. 판단·선택 → 원장 (축 13 · ★발주가 안 물었는데 집행자가 정한 것)

| # | 판단 | 원장 | 근거 |
|---|---|---|---|
| 1 | ★**회차 표기 = 발주 지시(11) 불이행 · 실측(18) 채택** | ㉡ 신규④ **채번 요** | S10 문면(「실물이 정본」) · COLD lineage 실측 · §5-ⓑ |
| 2 | ★**COLD lineage 에 17 회차 「소급 채번」 신설** | **원장 불요** + 근거 | `cycle-discipline.md 33)-ⓐ` 「부재를 부재로 남기지 않는다」의 직접 적용 |
| 3 | ★**`rule-footer-common.md` = 자기 의무 대상** 판정 | **원장 불요** + 근거 | 발주 §9-⑥ 이 **판정을 명시 위임** · 판정 근거 3 을 그 file 문면에 박음 |
| 4 | ★**판정선 값 5 유지 · 구간만 정정** | ㉡ **#192 후속**(§12-1) | Coin 결정 사항이라 값 변경은 회부 · 근거 정정은 실측 의무 |
| 5 | ★**T3 처방을 발주 제안보다 한 칸 넓게**(기본값 규정 유지) | **원장 불요** + 근거 | 발주 §9-⑤ 이 「제안하라」로 위임 · 기본값을 버리면 `^+#\{1,3\} `=0 사고를 못 막음 |
| 6 | ★**`cycle-discipline.md` 이력 절 = 소급 0 · 앞으로만** | ㉡ 신규③(§12-4) | 「소급 이관은 그 판이 그때 적었다를 사후 위조한다」 · 미기재분은 회부 |
| 7 | ★**G10 편차를 값 맞춤 대신 명제로 방어** | ㉡ 신규②(§12-3) | 인용을 지우면 hot 18 회차 주장이 근거를 잃음 · 선례 3 이 관례 입증 |
| 8 | **T2 경계표에 「file」 칸 추가** | **원장 불요** + 근거 | 발주 표의 분모 미표기가 재현 실패를 낳았음(실측) · 칸을 채우는 것은 정정 아님 |

---

*REPORT 끝. 집행 = cli session · 2026-08-30 KST · 계약 T7 **7/7** · 게이트 11 (전 11/11 재현 · 후 계약 충족 10/11 · 편차 1 = G10 명제형 자로 방어) · STOP 10 (미발동 8 · 자수 1 = `2>` · S10 발동 1 = 회차) · production **0 LOC** · 보호 5 무접촉 · **순삭제 0**(이동 = sha 쌍 증명) · push 0 · propagation 0.*

---

## §정정 append (= `reporting.md §15.2` 의무 · **후기입** · 본체 무접촉 · 삭제 0)

> 본체 commit = `c336b2e`. 아래는 **그 commit 이후 재실행한 게이트**가 낸 값이라 본체에 들어갈 수 없었던 항이다 (= K-152-ⓒ · 본체 문면은 **고치지 않고** 여기서 정정한다).

| # | 위치 | 본체 기재 | ★마감 실측 | 사유 |
|---|---|---|---|---|
| 1 | §4 표 · **G0 「후」** | `claude-cli-master 1d4d533 5` | ★**`claude-cli-master c336b2e 5`** | 본체 작성 시점의 HEAD 는 **구현 3 commit 까지**였다. REPORT commit(`c336b2e`)이 HEAD 를 한 칸 밀었다 — **자기 자신을 세는 게이트의 구조적 시차**이고, 계약(`claude-cli-master <HEAD> 5`)은 **충족**이다(가변 칸이 `<HEAD>` 로 열려 있다). |
| 2 | §13 · **최종 ahead** | `진입 24 + 본 판 4 = 28` | ★**`진입 24 + 본 판 5 = 29`** | 본 `§정정 append` commit 1 본 추가. `reporting.md §15.2` 상대식 정합 — ★**나열된 sha 4 보다 ahead 가 1 크다는 데서 독자가 꼬리 commit 을 안다.** |

**마감 게이트 전량 재실행** (= 본체 §4 와 대조 · `c336b2e` 시점):

```
G0 cwd claude-cli-master c336b2e  5     G6 밴드자 1 11
G1 hot 3 1 1                            G7 이력절 3 3
G2 COLD 167 0 2                         G8 무접촉 0
G3 판정선 3                              G9 선재 0 28
G4 자의범위 4 2                          G10 자기적용 2 1
G5 STOP축 1 13
```

⟹ **G0 첫 칸(cwd)·셋째 칸(file 5) 불변 · G1~G10 전량 본체 기재와 일치** · 유일 변동 = G0 둘째 칸(HEAD sha) = 위 #1. **G9 첫 칸 `0`**(porcelain) = 본 append **직전** 실측이며, 본 append commit 후에도 `0` 이어야 한다(= 꼬리 commit 이 working tree 를 안 남긴다).

★**본 절이 존재한다는 사실 자체가 값이다** — 본체가 자기 마감 sha 를 적을 수 없다는 것은 결함이 아니라 **구조**이고(= `MASTER-TASK-PURPOSE-CONTRACT-001` 의 「자기 sha = 충족 불가 → 본체」 판정 정합), 그래서 규약이 `§정정 append` 를 따로 둔다.
