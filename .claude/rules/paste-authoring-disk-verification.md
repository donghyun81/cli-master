# Paste Source Umbrella Authoring 측 Disk 실측 의무 Paradigm SoT

> **단일 목적**: cowork chat 측 paste source umbrella authoring 시점 disk 실측 의무 paradigm 영구 SoT. trigger 기반 paradigm default (= cowork chat 측 paste source 본문 작성 시점 진입 default · 매 cycle 강제 X default · paste source umbrella `§1 cycle 본질` + `§2 scope` + `§3 contract SoT` 영역 본문 작성 시점 default). 책임 분리 paradigm default (= cowork chat 측 paste source authoring 측 disk 실측 의무 default · cli session 측 paste-back verify 의무 default · `cowork-project-instructions v14 §B-1 + §B-2` 정합 default).
> **신설**: MASTER-CLI-PASTE-AUTHORING-DISK-VERIFICATION-PARADIGM-001 (2026-05-22).
> **precedent**: `MASTER-CLI-LAUNCH-STATUS-AUTO-SYNC-PARADIGM-001` (= H35 마감 default · self-contained 12 section format default · 본 SoT format 차용 default) + `MASTER-CLI-RECOMMENDED-OPTION-DISK-VERIFICATION-PARADIGM-001` (= H29 마감 default · 동족 paradigm 본질 default · cli session 측 disk 측정 의무 paradigm default · 본 SoT = cowork chat 측 paste source authoring paradigm default · 별 file 분리 paradigm default).
> **trigger 본질**: H36 chat 측 3REPO-CRITICAL-PATH-PROGRESS-001 cycle GB scope 재 정의 사고 default (= paste source umbrella 측 `BreathSessionRepository.kt` + `BreathSessionDao.kt` 명시 default · 실 disk 부재 default ⚠ · 실 disk = `MeditationSessionsListRepository.kt` 정합 default · cli session 측 자율 scope 재 정의 마감 default · 본 사고 재발 risk 회피 default).
> **연관 파일**:
> - `cycle-discipline.md` §5 v2 line 75 ([agent-commit: yes] 묵시 동의 paradigm default)
> - `cycle-discipline.md` §7 (commit body 6-section 표준 default)
> - `cycle-discipline.md` §15 패턴 1 (master 측 cli infra 단방향 propagation default)
> - `cycle-discipline.md` §23 (= Recommended option disk verification paradigm default · 동족 paradigm default)
> - `cycle-discipline.md` §25 (= LAUNCH-STATUS auto-sync paradigm default · format precedent default)
> - `cycle-discipline.md` §26 (= 본 SoT 측 pointer default)
> - `recommended-option-disk-verification.md` (= 동족 paradigm 본질 default · cli session 측 disk 측정 의무 paradigm default · 본 SoT 측 §8 자기 정합 paradigm 본질 정합 default)
> - `launch-status-auto-sync.md` (= format precedent default · self-contained 12 section format default)
> SOT: `CLAUDE.md`

---

## §1 본 paradigm 본질

### §1.1 trigger 기반 paradigm default

매 cycle 강제 X default · cowork chat 측 paste source umbrella authoring 시점 진입 default · 본 SoT 측 §3 3 의무 영역 default. 본 SoT 진입 trigger:

| trigger | 본질 |
|---|---|
| cowork chat 측 paste source umbrella authoring 시점 default ⭐ | cowork chat 측 의무 영역 default · `§1 cycle 본질` + `§2 scope` + `§3 contract SoT` 영역 본문 작성 시점 default |
| cowork chat 측 후속 cycle 후보 발행 시점 | `recommended-option-disk-verification.md` 정합 default · 본 SoT 동족 paradigm default |
| cli session 측 paste-back verify 시점 | paste-back `[EC]` 섹션 측 disk 측정 결과 정합 verify 본문 명시 default |

### §1.2 책임 분리 (= `cowork-project-instructions v14 §B-1 + §B-2` 정합)

| 영역 | 책임 |
|---|---|
| **cowork chat** ⭐ | **paste source umbrella authoring 측 disk 실측 의무 default + §0 baseline 측정 default + §1 + §1.3 disk 측정 결과 섹션 명시 default + §2 scope 측 file path × N disk verify default + §3 contract SoT 측 인용 entry × N byte-identical quote default** |
| cli session | paste-back verify 의무 default (= paste-back `[EC]` 섹션 측 disk 측정 결과 정합 verify 본문 명시 default) · paste source 측 명시 영역 disk 부재 발견 시 자율 scope 재 정의 default (= `recommended-option-disk-verification.md` §2.3 정합 default) · 사용자 보고 권장 default |
| 사용자 본인 | terminal 진입 + paste 운반 + cleanup pass 결정 (= 자율 default) |

본 SoT 핵심 = **disk 실측 의무 영역 본질 = cowork chat 단일 default** · cli session 측 paste-back verify 영역 default.

---

## §2 trigger 조건

### §2.1 cowork chat 측 paste source umbrella authoring 시점 default (= 의무 영역)

- 본 SoT 측 §3 3 의무 영역 진입 default
- paste source umbrella `§1 cycle 본질` + `§2 scope` + `§3 contract SoT` 영역 본문 작성 시점 default
- 본 SoT 측 §8 자기 정합 paradigm 본질 정합 default

### §2.2 cowork chat 측 후속 cycle 후보 발행 시점 (= 동족 paradigm default)

- `recommended-option-disk-verification.md` 정합 default
- 본 SoT = cowork chat 측 paste source authoring paradigm default · 별 file 분리 default
- 후속 cycle 후보 영역 측 disk 측정 의무 default (= 동족 paradigm default)

### §2.3 cli session 측 paste-back verify 시점

- paste-back `[EC]` 섹션 측 disk 측정 결과 정합 verify 본문 명시 default
- cli session 자율 영역 default (= 사용자 보고 권장 default)

---

## §3 3 의무 영역

본 SoT 핵심 의무 영역 default. cowork chat 측 paste source umbrella authoring 시점 다음 3 영역 모두 의무:

| # | 영역 | 본질 | 위치 |
|---|---|---|---|
| 1 | **paste source umbrella §1 + §1.3 disk 측정 결과 섹션 명시 의무 default** ⭐ | cycle scope file × N 측 disk 측정 결과 본문 인용 default (= `grep / find / git ls-files / Read` 측정 결과 default · 가정 X default) | paste source `§0` baseline 영역 + `§1.3` trigger 영역 default |
| 2 | **paste source umbrella §3 contract SoT 섹션 byte-identical quote 의무 default** ⭐ | precedent rule + 인용 entry × N 측 byte-identical quote default (= line number 인용 default · 가정 X default) | paste source `§3` contract SoT 영역 default |
| 3 | **paste source umbrella §2 scope 섹션 file path 모두 disk 측정 의무 default** ⭐⭐ | cycle 변경 file × N 측 `git ls-files <path>` 또는 `find <path>` 또는 `Read <path>` 측정 의무 default · 부재 발견 시 scope 재 정의 의무 default (= `recommended-option-disk-verification.md` §2.3 + §2.4 정합 default) | paste source `§2` scope 영역 default |

### §3.1 의무 영역 1 본질

- scope file × N 측 disk 측정 결과 본문 인용 default
- 측정 명령 영역: `grep` (= keyword + symbol 측정) / `find` (= file pattern 측정) / `git ls-files` (= tracked file 측정) / `Read` (= 본문 측정)
- 측정 결과 baseline 영역 본문 인용 default (= 가정 X · 실 disk 측정 결과 단일 default)
- `cycle-discipline.md §17` BASELINE 실측 표준 (= filename + content 동시 grep 의무) 정합 default

### §3.2 의무 영역 2 본질

- precedent rule + 인용 entry × N 측 byte-identical quote default
- line number 인용 의무 default (= `line N~M` 형식 default)
- quote 본문 측 disk 측정 결과 단일 default (= 가정 X · 추정 X · 본문 변형 X default)
- 인용 entry 측 실 disk 존재 측정 의무 default (= `recommended-option-disk-verification.md` §3 정합 default)

### §3.3 의무 영역 3 본질

- cycle 변경 file × N 측 disk 측정 의무 default
- 측정 명령 영역: `git ls-files <path>` / `find <path>` / `Read <path>` / `ls -la <path>` default
- 부재 발견 시 scope 재 정의 의무 default (= 표면 stale 후보 명시 X default)
- 부분 구현 영역 발견 시 신 본질 명시 default (= `recommended-option-disk-verification.md` §2.3 정합 default)
- 완전 stale 영역 발견 시 후보 제거 또는 대체 default (= `recommended-option-disk-verification.md` §2.4 정합 default)

---

## §4 verify 의무 본문

본 SoT 핵심 의무 영역 default. cli session 측 paste-back 본문 발행 시점 다음 verify 영역 의무:

### §4.1 paste-back `[EC]` 섹션 측 disk 측정 결과 정합 verify 본문 명시 default

```
[EC]   <기존 verify 본문> · paste source 측 명시 file × N disk 측정 결과 정합 verify ✓ (= scope file × N 측 실 disk 존재 default 또는 cli session 자율 scope 재 정의 마감 default) · §3 contract SoT 측 인용 entry × N byte-identical quote verify ✓
```

### §4.2 paste-back `[Diff]` 섹션 측 갱신 file list 명시 default

```
[Diff] <기존 변경 file list> · disk 측정 결과 정합 영역 default
```

### §4.3 self-consistent 의무 default

본 SoT 본질 = self-consistent default · 본 SoT 신설 cycle 측 paste source umbrella authoring 자체 = 본 SoT 본질 사례 default. 본 SoT 신설 cycle (= MASTER-CLI-PASTE-AUTHORING-DISK-VERIFICATION-PARADIGM-001) 측 paste source umbrella (= `cc-paste-MASTER-CLI-PASTE-AUTHORING-DISK-VERIFICATION-PARADIGM-001.md`) 자체 = 본 SoT §3 3 의무 영역 본질 사례 default:

- `§0.4 신 rule 영역 disk 측정 결과` 인용 default (= §3 의무 1 정합)
- `§2.1 변경 영역` file path 모두 disk verify default (= §3 의무 3 정합)
- `§3 contract SoT` byte-identical quote default (= §3 의무 2 정합)

### §4.4 `recommended-option-disk-verification.md` §3.1 자기 정합 paradigm 정합 default

본 SoT = `recommended-option-disk-verification.md` 동족 paradigm default · 본 §4.3 self-consistent 의무 = `recommended-option-disk-verification.md` §3.1 자기 정합 paradigm 본질 정합 default.

---

## §5 cli session 자율 paradigm

본 SoT 측 cli session 자율 결정 영역 default:

| 영역 | 자율 본질 |
|---|---|
| paste source 측 명시 영역 disk 부재 발견 시 scope 재 정의 | 자율 default (= `recommended-option-disk-verification.md` §2.3 정합 default · 신 본질 명시 default · 표면 stale 후보 명시 X default) |
| 부분 구현 영역 발견 시 scope 재 정의 | 자율 default (= `recommended-option-disk-verification.md` §2.3 정합 default) |
| 완전 stale 영역 발견 시 후보 제거 또는 대체 | 자율 default (= `recommended-option-disk-verification.md` §2.4 정합 default) |
| 사용자 보고 paradigm | 권장 default (= 본 SoT 정합 default · 본 SoT 본질 영역 default) |
| paste-back `[EC]` 섹션 측 verify 본문 본질 | 자율 본문 default (= §4.1 권장 본문 default) |

---

## §6 STOP 조건

| # | trigger | mitigation |
|---|---|---|
| 1 | paste source 측 명시 영역 모두 disk 부재 발견 | 즉시 STOP + 사용자 회수 default (= scope 본질 전체 X default · cycle 본질 재 정의 default) |
| 2 | paste source authoring 측 본 SoT 본질 위반 누적 발화 (= 3 회 누적 default) | mitigation cycle 진입 default (= `recommended-option-disk-verification.md` §8 정합 default · 자동 enforce hook 신설 검토 default) |
| 3 | 보호 5 file sha drift 발견 | 즉시 STOP + 사용자 회수 default (= `cycle-discipline.md §10` 정합 default) |
| 4 | HIGH RISK 도메인 진입 시점 본 SoT 위반 발견 (= DB migration / Money / Auth 영역) | 즉시 STOP + 사용자 회수 default |
| 5 | 사용자 본심 분기 의제 본질 발견 (= scope 본질 결정 default · 자율 scope 재 정의 본질 default) | AskUserQuestion 회수 default |

---

## §7 적용 영역

| 영역 | 적용 |
|---|---|
| cowork chat 측 paste source umbrella authoring 시점 default | 의무 default ⭐ |
| cowork chat 측 후속 cycle 후보 발행 시점 | 의무 default (= `recommended-option-disk-verification.md` 정합 default · 동족 paradigm default) |
| cli session 측 paste-back verify 시점 | 의무 default (= paste-back `[EC]` 섹션 측 disk 측정 결과 정합 verify 본문 명시 default) |
| cowork chat 측 일반 응답 또는 정보 제공 | 적용 X default |
| cli session 측 cycle 진행 영역 (= 도메인 source 측정 시점) | 적용 X default (= `recommended-option-disk-verification.md` 정합 default · cli session 측 disk 측정 의무 paradigm default · 별 SoT default) |

---

## §8 자기 정합 paradigm 본질 default (= `recommended-option-disk-verification.md` §3.1 정합 default)

본 SoT 신설 cycle 측 paste source umbrella authoring 시점 자기 적용 의무 default. 본 cycle 측 paste source umbrella (= `cc-paste-MASTER-CLI-PASTE-AUTHORING-DISK-VERIFICATION-PARADIGM-001.md`) 자체 = 본 SoT §3 3 의무 영역 본질 사례 default:

- **§0.4 disk 측정 결과 인용 default** (= `paste-authoring-disk-verification` keyword grep `0 match` + `.claude/rules/` 측 기존 rule 수 `37 file` + `recommended-option-disk-verification.md` 존재 verify ✓ + `launch-status-auto-sync.md` 존재 verify ✓ + `cycle-discipline.md` 측 §26 영역 grep `0 match` + `scripts/propagate.sh` + `scripts/verify-sync.sh` 존재 verify ✓ default)
- **§2.1 file path 모두 disk verify default** (= `.claude/rules/paste-authoring-disk-verification.md` 0 match grep verify ✓ + `.claude/rules/cycle-discipline.md` 존재 verify ✓ + `CLAUDE.md` 존재 verify ✓ default)
- **§3 contract SoT byte-identical quote default** (= `launch-status-auto-sync.md` 12 section 인용 + `cycle-discipline.md §25` line 672~698 인용 + `recommended-option-disk-verification.md` §3 line 84~108 인용 default)

본 paradigm 자체 = 본 cycle 측 첫 self-consistent 사례 default · 후속 cycle 측 본 SoT 본질 적용 default.

---

## §9 인접 paradigm 정합

| 인접 entry | 본질 |
|---|---|
| `cycle-discipline.md §23` (= Recommended option disk verification paradigm) | 동족 paradigm default · cli session 측 disk 측정 의무 paradigm default · 본 SoT = cowork chat 측 paste source authoring paradigm default · 별 file 분리 paradigm default |
| `cycle-discipline.md §25` (= LAUNCH-STATUS auto-sync paradigm) | format precedent default · self-contained 12 section format 차용 default |
| `cycle-discipline.md §17` (= BASELINE 실측 표준) | 본 SoT 측 §3.1 의무 1 본문 정합 default (= filename + content 동시 grep 의무 default) |
| `cycle-discipline.md §7` (= commit body 6-section 표준) | 본 SoT 측 §4.1 paste-back `[EC]` 섹션 정합 default |
| `recommended-option-disk-verification.md` | 동족 paradigm 본질 default · 본 SoT 측 §8 자기 정합 paradigm 본질 정합 default + §3 의무 영역 본문 정합 default |
| `launch-status-auto-sync.md` | format precedent default · self-contained 12 section default · 본 SoT format 차용 default |
| `cowork-project-instructions v14 §B-1` | cowork chat 측 paste source 발행 책임 default · 본 SoT paradigm 정합 default |
| `cowork-project-instructions v14 §B-2` | 책임 분리 paradigm default · cli session 측 paste-back verify 책임 default · 본 SoT §1.2 정합 default |
| `cowork-project-instructions v14 §E-1` | prompt authoring cross-verify default · 본 SoT paradigm 본질 정합 default |

---

## §10 본 SoT 의 변경 정책

- cli infra 권장 byte-identical (= 5-repo · master + app-foundation + GentlyBreath + GentlyDay + GentlyTable)
- 변경 시 master cycle 신설 + 5-repo propagation 의무 (= `cycle-discipline.md` §15 패턴 1 정합)
- 자식 repo 측 직접 수정 금지

---

## §11 명시 cycle 이력

- 2026-05-22 · `MASTER-CLI-PASTE-AUTHORING-DISK-VERIFICATION-PARADIGM-001` · 본 SoT 신설 (= paradigm 본질 + trigger 조건 + 3 의무 영역 + paste-back verify 의무 본문 + cli session 자율 paradigm + STOP 조건 + 적용 영역 + 자기 정합 paradigm 본질 + 인접 paradigm 정합 default) + `cycle-discipline.md` §26 pointer 추가 default + CLAUDE.md §15 entry append default + 5-repo byte-identical propagation default
- precedent: `MASTER-CLI-LAUNCH-STATUS-AUTO-SYNC-PARADIGM-001` (= H35 마감 default · self-contained 12 section format default · 본 SoT format 차용 default) + `MASTER-CLI-RECOMMENDED-OPTION-DISK-VERIFICATION-PARADIGM-001` (= H29 마감 default · 동족 paradigm 본질 default · cli session 측 disk 측정 의무 paradigm default · 본 SoT = cowork chat 측 paste source authoring paradigm default · 별 file 분리 paradigm default)
- trigger baseline: H36 chat 측 3REPO-CRITICAL-PATH-PROGRESS-001 cycle GB scope 재 정의 사고 default (= paste source umbrella 측 `BreathSessionRepository.kt` + `BreathSessionDao.kt` 명시 default · 실 disk 부재 default · 실 disk = `MeditationSessionsListRepository.kt` 정합 default · cli session 측 자율 scope 재 정의 마감 default · 본 사고 재발 risk 회피 default)

---

## §12 본 SoT 측 후속 cycle 후보 default

- enforce hook 신설 후보 default (= cowork chat 측 paste source authoring 측 disk 실측 자동 verify hook default · 본 SoT 측 §6 STOP #2 정합 default · 누적 3 회 위반 발화 시점 mitigation cycle 진입 default)
- `recommended-option-disk-verification.md` + 본 SoT 통합 paradigm 후보 default (= 동족 paradigm 본질 default · 단 책임 분리 영역 단일 SoT 유지 default · lazy default)
- 본 SoT 측 자기 정합 paradigm 본질 후속 사례 누적 default (= 후속 cycle 측 본 SoT 본질 적용 default · 누적 통계 measurement default)
