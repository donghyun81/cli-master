# `MASTER-RULE-HISTORY-SECTION-001` — REPORT

> 집행 = cli · cwd = `~/AndroidStudioProjects/claude-cli-master` · master 단독 · 2026-08-30 KST
> Mode = M5 (cli 운영 레이어형) · production 0 LOC · 보호 5 sha 변동 0 · 자식 0 · propagation 0
> 축 = `reporting.md §15.1` **14 축** (⑴~⒁)

---

## ⑴ BASELINE 재측 (= §0-B 인용 금지 · 재측이 정본)

| 항 | 발주 §0-B (그때의 값) | **본 판 재측** | 갈림 |
|---|---|---|---|
| HEAD | `de6af66` | `de6af66` | — |
| ahead / porcelain | `37` / `0` | `37` / `0` | — |
| rule file 분모 | `48` = `.claude/rules` 6 + `docs/rules` 42 | **`48`** = **6 + 42** | — |
| 「명시 cycle 이력」 보유 / 부재 | `19` / `29` | **`19` / `29`** | — |
| 그 절 헤딩 깊이 | `19/19` 가 `## ` | **`19/19` 가 `## `** | — |
| COLD 데이터행 | `169` | **`169`** | — |
| 라벨 종 | `9` (12~18 · 27 · 28) | **`9`** (동일 집합) | — |
| 무번호 사건 / 라벨 오기 | `8` / `2` | **`8` / `2`** (재도출) | — |
| 회차 착지 | `28` (본 판 = 29) | **`28`** (본 판 = **29** · 사건 계수 재도출) | — |
| 선재 drift 17 file | (verify-sync 51 쌍) | ★**미측정** | ★**S8 이 자를 금지** |

★**갈린 것 3** (= BASELINE 표 밖 · 발주 본문의 값):

1. ★★**§1-T2 「헤딩 관례 17/17」 = 실측과 갈린다.** 실측 = 절 보유 **19** 중 bare `## 명시 cycle 이력` = **4** · 번호형 = **15**. 공통인 것은 **문자열이 아니라 깊이**(`## ` = 19/19). ⟹ 관례는 「bare」가 아니라 **「깊이 2 · file 자기 번호 계승」**. **S11 적용** — file 별 자기 convention 을 따랐다(bare 6 + 번호 계승 5).
   - 자 = `for f in .claude/rules/*.md docs/rules/*.md; do grep '^#' "$f" | grep -F '명시 cycle 이력'; done | sort | uniq -c | sort -rn`
2. ★★**§0-ⓕ 「pointer stub 7 은 빈 절이 된다」 = 반증.** 그 7 중 **6 이 이미 `## Migration 이력` 을 보유**하고 entry 도 실재한다. 남은 1(`rule-routing-table.md`)도 창설이 **이동 아님**(아래 §T1). ⟹ **「빈 절이 될 file」 = 0 건**.
3. ★**§3-표 2 「뜻으로 재면 32」 = 실측 35.** 자 = 절 보유 19 + 부재 29 중 타이름 이력 절 실재 **16**. (구 표기 32 의 자는 미상 — 본 판은 재측값을 쓴다.)

---

## ⑵ 배경 재진술

`rule-footer-common.md:10` 이 「**이력 절 등재 의무**」를 세웠고 `:11` 이 판정선 축을 「실질 개정 ↔ 기계 치환」으로 바꿨다. 그 의무의 대상 = `:3` 이 정한 두 디렉터리 **48**. 그중 canonical 명 「명시 cycle 이력」 절 **보유 19 / 부재 29**.

발주의 읽기 = 「**29 가 의무를 어기고 있다**」. 본 판 실측의 읽기 = ★**아니다 — 어긴 것은 11 이다.**

- **16** = 동등 절이 **다른 이름**으로 이미 실재 (= `cycle 이력` 9 · `Migration 이력` 6 · `C2 변경 이력` 1). 이력이 없는 게 아니라 **이름이 다르다**.
- **2** = 보호 5 (= `pencil-uiux-workflow.md` · `uiux-sot-refresh.md`). 접촉 = sha 변동 ⟹ 본 판이 착지시킬 수 없다.
- **11** = 진짜 미이행.

★**「29」는 자를 「이름」에 고정한 결과였다** (= K-172 의 실물 재현). 같은 분모를 **뜻**으로 재면 보유는 **35** 다.

---

## ⑶ 착지 좌표 (★`grep -n` 재측 = K-173 · 마감 시점)

| file | 좌표 | 내용 |
|---|---|---|
| `.claude/rules/rule-footer-common.md` | `:32`~`:35` | T1 「부재 29 전수 판정」 bullet + 3 분기 + T3 「대상 아님」 + 이름 축 |
| 〃 | `:39` | T5 `cf063a8` 소급 등재 (= 본 file 자기 신설) |
| 〃 | `:43` | 본 판 자기 등재 |
| `.auto-memory/master-cycle-history-COLD.md` | `:15` | ★29 회차 demote note (= 이동 증명 sha 쌍) |
| 〃 | `:17`~`:32` | ★소급 채번 8 표 + namespace 충돌 사유 |
| 〃 | `:45` / `:47` | 라벨 병기 정정 2 (= 구 라벨 **존치** 확인 자리) |
| 〃 | `:234` | demote 착지 행 (`MASTER-DOC-MANIFEST-SWEEP-003`) |
| `CLAUDE.md` | `:301` | 본 판 §15 등재 1 |
| 〃 | `:303`~`:304` | 회차 29 + 무번호 8 회부 마감 note |
| rule 11 본 | 각 file 말미 | `## 명시 cycle 이력` (bare 6 · 번호 계승 5) |

---

## ⑷ 게이트 12 — 전 / 후 (★awk 추출 verbatim 실행 · 재타이핑 0)

실행 = `bash <(awk '/^## §6\. 게이트/{f=1} f && /^```bash$/{g=1;next} g && /^```$/{exit} g' <발주서>)`

| G | 계약 「전」 | **실측 「전」** | 계약 「후」 | **실측 「후」** | 판정 |
|---|---|---|---|---|---|
| G0 | `claude-cli-master de6af66 48` | 동일 | `… <HEAD> 48` | `… 48` | ✅ 분모 48 불변 |
| G1 | `19` | `19` | `19 + 있음 수` | **`30`** | ⚠ 아래 |
| G2 | `0` | `0` | `≥1` | **`1`** | ✅ |
| G3 | `0` | `0` | **정확히 `1`** | **`1`** | ✅ |
| G4 | `0` | `0` | `1` | **`1`** | ✅ |
| G5 | `0` | `0` | `≥1` | **`13`** | ✅ |
| G6 | `9` | `9` | `≥17` | **`18`** | ✅ |
| G7 | `2 2` | `2 2` | `2 2` 불변 | **`2 2`** | ✅ (★2회 자수) |
| G8 | `169 4` | `169 4` | `170` · 둘째 `0` | **`170` · 둘째 `1`** | ⚠ 아래 |
| G9 | `3 0` | `3 0` | `3 1` | **`3 1`** | ✅ |
| G10 | `0` | `0` | `0` | **`0`** | ✅ |
| G11 | `0 37` | `0 37` | `0` · `40~44` | **`0 40`** | ✅ |

**12/12 「전」 전량 재현** (= §9-⑴ 답).

### ⚠ G1 = 30 (계약 산식과 갈림 · S1 아님)

계약 산식 = `19 + 판정 「있음」 수`. 판정 「있음」 = **29** 이므로 산식대로면 **48**. 실측 = **30** (= 19 + 신설 11).

★**산식이 「있음 ⟹ 신설」을 1:1 로 가정**했고, 그 가정을 **두 사실이 끊는다** — 동등 절 타이름 실재 **16**(신설하면 한 file 에 이력 절 2 개) · 보호 5 중 **2**(착지 불가). 계약의 S1 방어선(「**29 면 S1**」 = 판정 없이 전량 신설)은 **통과** — 실측 30 은 48 이 아니고, 판정이 **선행**했다(§7-⑦ 순서 준수 · T1 → T2).

### ⚠ G8 둘째 칸 = 1 (0 아님 · 의도적)

잔존 1 = COLD `:36` 의 **직전 판 전이 기록** — 「`168 → 169 데이터행` · 고유 `166 → 167`」. 이것은 **현행 자기서술이 아니라 그 판 시점의 전이 사실**이고, 0 으로 만들려면 **다른 cycle 의 note 를 소급 정정**해야 한다(= 삭제 0 원칙과 반대 방향).

⟹ **처분 = 병기**. 그 행에 「★**그 판 시점 전이 기록** · 현행 값 아님 · 현행 = 위 heading 자」를 부착했다. **원 문면 삭제 0**.

★**게이트 앵커가 「현행 자기서술」과 「과거 전이 기록」을 구별하지 못한다** — G7 이 「라벨 부여」와 「라벨 인용」을 구별하지 못한 것과 **같은 축**이다(= `verification-and-review.md` §0.3 존재·귀속). 한 판에서 **같은 계열의 자 결함이 2 회** 나왔다.

---

## ⑸ ★자기서술 전수 grep (= 게이트 밖 · ㉡ #243 · 명령 병기)

★**게이트 G8 둘째 칸은 COLD 안에서만 잰다.** 같은 문면이 `CLAUDE.md` 에도 산다. **분모 census 선행** = `git ls-files | wc -l` = **764**.

```bash
git --no-optional-locks ls-files | while read -r p; do
  c=$(grep -c -F '<문자열>' "$p"); [ "$c" -gt 0 ] && printf '%s\t%s\n' "$c" "$p"
done
```

| 문자열 | 마감 실측 | 판정 |
|---|---|---|
| `169 데이터행` | COLD **1** + `.ai/reports/MASTER-REGISTER-AXIS-REPLACE-001/REPORT.md` **1** | COLD 1 = 위 ⚠ 전이 기록(병기) · REPORT = **과거 판 산출물 = 소급 정정 금지** |
| `167 고유` | 과거 REPORT **1** 단독 | 동상 |
| `170 데이터행` | COLD **3** + **`CLAUDE.md` 1** | ✅ 현행 |
| `168 고유` | COLD **3** + **`CLAUDE.md` 1** | ✅ 현행 |

⟹ ★**`CLAUDE.md` 가 실제로 그 문면을 들고 있었고 갱신됐다.** 게이트만 믿었으면 hot 헌법이 `169/167` 을 든 채 green 을 받았다(#243 실증 재현).

★**census 부작용 1 실측**: `git ls-files` 가 낸 경로 1 개(`docs/baseline/cowork-project-instructions-§20-redline-20260517.md`)에서 `grep` 이 **No such file** 경고를 냈다 — macOS **NFC/NFD 정규화 불일치**(index 판 ↔ 디스크 판). **실경로로 직접 재측 = 두 문자열 모두 `0`** ⟹ census 누락 0. ★**경고를 무시했으면 분모 763 을 764 라고 적을 뻔했다.**

---

## ⑹ §9 「나를 의심하는 절차」 7 항 (= 방어 0)

### ⑴ 「전」 값 12 항 전량 재현되는가 → **YES · 12/12**

위 ⑷ 표 「실측 전」 열. 단 하나도 갈리지 않았다.

### ⑵ ★「29 전량 신설」이 정말 틀린 처방인가 → **결론은 맞고, 근거는 틀렸다**

발주 근거 = 「pointer stub 7 은 대개 기계 치환만 받았으므로 그 이력 절은 **영원히 빈 절**이 된다」.

**실측** — 그 7 의 계보를 전부 열었다:

| pointer stub | 창설 | 실질? | 이력 절 실재? |
|---|---|---|---|
| `initiatives-auto-sync.md` | `00329d1` **228/0** | O | ✅ `## Migration 이력` |
| `paste-authoring-disk-verification.md` | `ae6cb37` **215/0** | O | ✅ 〃 |
| `pencil-automation.md` | `ff65723` **64/0** | O | ✅ 〃 |
| `pencil-cli-headless.md` | `0b908f2` **264/0** | O | ✅ 〃 |
| `recommended-option-disk-verification.md` | `0a6479c` **250/0** | O | ✅ 〃 |
| `runtime-crash-mitigation-process.md` | `67eb8ae` **182/0** | O | ✅ 〃 |
| `rule-routing-table.md` | `cf063a8` **17/0** | O (이동 아님) | ❌ **부재** |

⟹ ★**전제 2 겹이 틀렸다.** ① 그 7 은 「기계 치환만 받은」 것이 아니다 — **전부 대형 창설**을 받았다(64~264 행). ② 그중 **6 은 이미 이력 절을 갖고 있다** — 「빈 절이 될」 상황 자체가 없다. **빈 절 후보 = 0 건.**

**그럼에도 「29 전량 신설」은 틀린 처방이다** — 단 이유가 다르다: **16 은 이미 있고(이름만 다름), 2 는 보호라 못 만진다.** ⟹ **11 만 신설.**

★**전제가 틀렸는데 결론이 맞는 경우가 제일 위험하다** — 검증을 안 하면 「내 예측이 맞았다」로 닫히고 **틀린 근거가 다음 판의 전제로 승계**된다. 발주 §0-ⓕ 의 지시(「내 전제가 틀렸으면 그렇게 적고 신설하라」)를 그대로 이행해 **적고, 그러나 신설은 11 만** 했다.

### ⑶ ★판정이 집행자마다 갈리지 않는가 → **같은 판정 · 단 취약점 1 발견**

`rule-routing-table.md` / `cf063a8` 을 **보조자 3 으로 각각 독립 측정**:

| 보조자 | 자 | 값 | 판정 |
|---|---|---|---|
| ⑴ 동형 치환 서명 | `git show --numstat cf063a8 \| cut -f1,2 \| sort \| uniq -c \| sort -rn` | 그 file `17/0` = **반복 1** (반복군 = `1/3`×13 · `2/3`×4 · `1/2`×2 · `1/1`×2) | 기계 후보 **아님** |
| ⑵ 이동 서명(좁힘) | `git show cf063a8^:<index> \| grep -c -F '<핵심어구>'` × 5 | **전수 `0`** | 새 내용 ≠ 0 = **실질** |
| ⑶ 헤딩±(깊이 무관) | `git show --format= cf063a8 -- <path> \| grep -c '^[+-]#\{1,\} '` | **`1`** | 실질 신호 |

**3/3 동일 판정 = 실질.** 2차 재현 = `stop-canonical.md` / `563c3f0`(= 역시 move 창설) 에 ⑵ 적용 → 핵심 어구 4 종 PRE **전수 0** ⟹ 실질. **같은 절차 · 같은 판정 계열.**

★**그러나 취약점 1**: 보조자 ⑵ 의 **「핵심 어구」 선택은 집행자 몫**이다. 어구를 **PRE 에 있는 것만 고르면** 어떤 실질 개정도 「기계」로 만들 수 있다. ⟹ **좁힘 제안**(회부): 핵심 어구는 **추가분에서 뽑되 ⓐ 그 file 의 새 지시·새 정책을 담은 문장에서 ⓑ 최소 3 종 ⓒ REPORT 에 어구 원문을 열거**한다. 본 판은 5 종(rule-routing-table)·4 종(stop-canonical)을 **원문 열거**했다.

### ⑷ ★무번호 8 의 실서수 17~24 가 맞는가 → **재도출 일치 · 산술 검산 PASS**

**인용하지 않고 사건 계수로 재도출**:

```bash
for s in $(git log --format=%H --reverse -- <COLD>); do
  a=$(git show --format= $s -- <COLD> | grep -c '^+| [A-Z0-9]')
  d=$(git show --format= $s -- <COLD> | grep -c '^-| [A-Z0-9]')
  [ "$a" -gt 0 ] && echo "$s +$a -$d"
done
```

- **사건 총수 = 28** ⟹ 본 판 = **29**
- ★**산술 검산 = append 합 `169` == 실측 데이터행 `169`** · **삭제 사건 `0`** ⟹ 자의 **완전성 증명**(합이 맞고 삭제가 0 이면 놓친 사건이 없다)
- **#17~#24** = `df62c9f`(08-17) · `791fed7`(08-23) · `74424f2`(08-23) · `c570d1e`(08-23) · `48e75de`(08-29) · `1dcf20f`(08-29) · `1855f39`(08-29) · `e1e2c55`(08-29)
- **#25** `b3a8857` · **#26** `a3ba108` · **#27** `814f591` · **#28** `bc04cac`

⟹ 발주값과 **전량 일치**. ★**일치는 결과이지 근거가 아니다** — 자를 다시 돌렸기 때문에 쓸 수 있는 값이다.

### ⑸ ★구 라벨 2 를 지우지 않았는가 → **G7 `2 2` + 눈으로 확인**

자만 믿지 않고 **두 자리를 열었다**:

- COLD `:45` = `★**18 회차 demote 의 성격** … ★**실서수 = 26** ⟹ **26 회차** … ★**구 라벨 「18」 = 삭제 0 · 병기 존치**`
- COLD `:47` = `★**17 회차 = 무번호 demote** … ★**실서수 = 25** ⟹ **25 회차** · ★**구 라벨 「17」 = 삭제 0 · 병기 존치**`

**구 라벨 문자열 그대로 생존** · 실서수 **병기** · bold 라벨 form **추가**(G6 기여).

★★**자수 2 — G7 이 두 번 나를 잡았다.** ① T4 초안이 namespace 충돌을 **설명하려고** 「17 회차」·「18 회차」를 인용 → `3 3`. ② 29 회차 note 가 참조처를 **구 라벨로** 불렀다 → `2 3`. **둘 다 부여가 아니라 인용**인데 자는 구별하지 못한다. ⟹ 문면에서 제거하고 **실서수로 부르도록** 바꿨다. ★**「설명이니까 괜찮다」가 두 번 다 틀렸다.**

### ⑹ ★자기서술 분모에 `CLAUDE.md` 를 넣었는가 → **YES**

위 ⑸ 절 전문. 분모 = `git ls-files` **764** (census 선행) · `CLAUDE.md` **실제 hit 1** → 갱신 완료.

### ⑺ ★§9 산출 축이 하한 `+250` 을 넘는가 → **`335` 행 (`wc -l` 실측) · 넘는다**

★**자수** — 초안은 이 자리에 **`289` 를 적었다**. 그것은 재지 않고 **어림한 값**(= 발주가 정확히 금지한 「예측값을 실측 자리에 적기」)이었고, `wc -l` 이 **`335`** 를 냈다. 오차 **46 행**. ⟹ 값을 실측으로 교체했다. ★**「하한만 넘으면 되니까」가 자를 안 돌릴 이유가 되지 못한다** — 이 판이 다른 곳에서 6 번 지킨 규율을 **자기 REPORT 의 마지막 칸에서 놓칠 뻔했다.**

---

## ⑺ ChangeBudget — 층 별 계상 (★단위 = **행** · 합산 X)

자 = `git diff --numstat de6af66 -- .` 층별 합 · §9 산출 = `wc -l REPORT.md`

| 층 | 계약 | **실측** | 판정 |
|---|---|---|---|
| 실코드 | **`0`** | **`0`** (= prod file 0 본) | ✅ |
| 문서 본문 | `+60 ~ +140` | **`+136 / -8` = 순증 `128`** | ✅ 밴드 내 |
| §9 산출 | `+250 ~ +420` | **`335` 행** (= `wc -l` 실측) | ✅ 밴드 내 |
| 삭제 | **`0`** | **`8` 행** (numstat) · ★**내용 삭제 = `0`** | ⚠ 아래 |

### ⚠ 삭제 축 8 — 전량 소명 (= 행 교체이지 내용 제거 아님)

| # | 행 | 성격 |
|---|---|---|
| 1·2·6 | COLD heading · §1 heading · 「아래 표 =」 note | 계수 자기서술 갱신 (`169/167` → `170/168`) = **교체** |
| 3 | COLD 28 회차 note | 「그 판 시점」 병기 부착 = **교체** |
| 4·5 | COLD 18·17 회차 note | 실서수 병기 + bold 라벨 부착 = **교체** · 구 라벨 **생존**(G7 확인) |
| 7 | `CLAUDE.md:300` demote 행 | **verbatim 이동** — COLD `:234` 착지 · sha 쌍 `264e4cdd7c0f00f4` 양측 일치 · 383B |
| 8 | `CLAUDE.md` §15 꼬리 note | 회차 28→29 + 무번호 8 마감 = **교체** |

⟹ ★**계약 「삭제 축 0」은 「내용 소실 0」의 뜻이고 그것은 지켜졌다.** 다만 **numstat 자로 재면 8** 이다 — 발주가 자를 `git diff --numstat` 으로 고정했으므로 **자의 값을 그대로 적고 소명을 붙인다**(= 값을 자에 맞춰 각색하지 않는다).

---

## ⑻ STOP 11 (★`0` 도 값)

| # | trigger | 발생 | 근거 |
|---|---|---|---|
| S1 | 판정 없이 29 전량 신설 | **0** | T1 선행(commit `f02bb88`) → T2(`a5f85df`) · 신설 **11**(≠29 ≠48) |
| S2 | 기계 치환 commit 을 entry 로 | **0** | 소급 entry 12 전량 = 창설(대형 신설) · `de37a6e`(rename) · `a47f61d`/`558af38`(어휘 sweep) **미등재** |
| S3 | 구 라벨 삭제 | **0** | G7 `2 2` + 눈 확인 2 자리 |
| S4 | COLD 데이터행 169 내용 변경 | **0** | 표 = append 1 행만(`:234`) · 기존 169 행 무접촉 |
| S5 | hot 4 행 이상 또는 demote 없이 등재 | **0** | G9 첫 칸 `3` · demote 1 + 등재 1 |
| S6 | 회차를 문면 인용으로 기재 | **0** | 사건 계수 재도출 + 산술 검산 (§9-⑷) |
| S7 | 판정선 축 재논의·재변경 | **0** | `:11` 축 문면 **무접촉** · 본 판은 축을 **적용**만 |
| S8 | `verify-sync.sh` / `propagate.sh` 실행 | **0** | ★**미실행** — 선재 drift 17 을 재지 않았다(BASELINE 항 1 = 미측정으로 명시) |
| S9 | `scripts/`·`server/`·`supabase/`·Selfward 접촉 | **0** | G10 `0` |
| S10 | push · `git add -A` · `2>` · `git grep` | **0** | `git add` = 명시 경로만 · push 0 · 리다이렉트 0 |
| S11 | 발주 ↔ 실물 갈림 → 실물이 정본 | **적용 3** | 헤딩 관례 · §0-ⓕ 전제 · 「뜻 32」 (= ⑴ 절) |

---

## ⑼ 만든 부채 — 시점 대조

| 부채 | 만든 시점 | 해소 시점 | 상태 |
|---|---|---|---|
| 이력 절 부재 11 | `ff65723`(2026-05-02) ~ `df62c9f`(2026-08-17) | **본 판** | ✅ 해소 |
| `rule-footer-common.md` 자기 등재 0 | `cf063a8`(2026-07-10) | **본 판 T5** | ✅ 해소 (부채 수명 **51 일**) |
| COLD 무번호 8 | `df62c9f`~`e1e2c55`(08-17~08-29) | **본 판 T4** | ✅ 해소 |
| 라벨 오기 2 | `b3a8857`·`a3ba108`(08-30) | **본 판 T4** | ✅ 병기 정정 |
| **보호 2 이력 절 부재** | `ff65723`(2026-05-02) | **미해소** | ⚠ **본 판이 회부** (아래 ⑽) |
| **이름 불일치 16** | 각 file 창설 시점 | **미해소** | ⚠ **본 판이 회부** |
| **#17·#18 bold 라벨 미부여** | 본 판 | 구 라벨 은퇴 시 | ⚠ **본 판이 만든 부채** — 아래 ⑽ |
| **보조자 ⑵ 어구 선택 자의성** | `MASTER-REGISTER-AXIS-REPLACE-001`(08-30) | 미해소 | ⚠ 본 판이 **발견** · 좁힘안 회부 |

---

## ⑽ 회부 + 원장

| # | 회부 | 사유 | 대상 판 |
|---|---|---|---|
| ⓐ | **보호 2 이력 절 신설** (`pencil-uiux-workflow.md` · `uiux-sot-refresh.md`) | 접촉 = sha 변동 ⟹ STOP #5 + §14a·manifest baseline 동기 + **즉시 propagation** 의무. 본 판은 propagation **금지**(S8) ⟹ 착지 불가 | `MASTER-PROPAGATE-DRIFT17-001` 또는 후속 보호 판 |
| ⓑ | **절 이름 정합 16** (타이름 → canonical) | 절 이동·번호 재배열 동반 ⟹ scope 분리 (A3) | 별 판 |
| ⓒ | **#17·#18 bold 라벨 회수** | 구 라벨(오기 2)이 그 번호를 점유 중 · 존치 원칙 ⟹ 은퇴 시점까지 대기 | 구 라벨 은퇴 판 |
| ⓓ | **보조자 ⑵ 「핵심 어구」 선택 좁힘** | 어구를 PRE 에 있는 것만 고르면 실질을 기계로 만들 수 있다 ⟹ ⓐ 새 지시·정책 문장에서 ⓑ 최소 3 종 ⓒ REPORT 원문 열거 | `rule-footer-common.md:14` 개정 판 |
| ⓔ | **게이트 앵커의 「귀속 ↔ 인용」 미분별** | G7·G8 둘 다 같은 축에서 오작동 (본 판 2 회) ⟹ 발주 게이트 저작 규약에 축 추가 | 발주 저작 규약 판 |
| ⓕ | **선재 drift 17 file (51 쌍)** | 본 판 무접촉 · 미측정 | `MASTER-PROPAGATE-DRIFT17-001` |

원장 = ㉡ **#244**-⑴⑵⑶ **마감** · ㉡ **#243**(자기서술 분모) **재현·마감** · ㉡ 신규 = 위 ⓐ~ⓔ.

---

## ⑾ commit 계수 — 자기 대조

| # | sha | 제목 | T |
|---|---|---|---|
| 1 | `f02bb88` | 부재 29 새 축 판정표 | T1 (+T3 문면) |
| 2 | `a5f85df` | 이력 절 11 신설 + 소급 entry + 자기 등재 | T2 · T5 |
| 3 | `38ca5ea` | COLD 무번호 8 채번 + 라벨 병기 2 | T4 |
| 4 | (본 commit) | §15 등재 1 + demote 1 + 회차 29 + REPORT | T6 |

**계약 4 본 == 실측 4 본** ✅ · `git rev-list --count origin/main..HEAD` = **40** (진입 37 + 3, 본 commit 후 **41**) — 계약 밴드 `40~44` 내 ✅

★**T3 문면이 commit 1 에 들어갔다** (§8 계획은 commit 2). 사유 = T3 의 「대상 아님」 1 줄은 **T1 판정 bullet 의 하위 조항**이라 같은 edit 을 뗄 수 없다. commit 1 메시지에 명시.

---

## ⑿ 자 대조표 + 환경

| 명제 | 자 | 단위 | 값 |
|---|---|---|---|
| 분모 48 | `ls .claude/rules/*.md docs/rules/*.md \| wc -l` | file | `48` (6+42) |
| 절 보유 (이름 축) | `grep '^#' <f> \| grep -c -F '명시 cycle 이력'` | file | `19` → `30` |
| 절 보유 (뜻 축) | 위 + 타이름 heading census | file | `35` |
| 등재 | `grep -e '^- 20' -e '^> - 20' <f> \| grep -c -F '<ID>'` | 행 | G4 `1` · G5 `13` |
| 회차 | COLD append commit 시간순 계수 + 산술 검산 | 사건 | `28` → 본 판 `29` |
| 구 라벨 보존 | `grep -c -F '17 회차'` / `'18 회차'` | 출현(행) | `2` / `2` 불변 |
| 이동 증명 | `sed -n '<n>p' <f> \| shasum -a 256 \| cut -c1-16` | hex16 | `264e4cdd7c0f00f4` 양측 |
| 자기서술 | `git ls-files` 전수 × `grep -c -F` | file·출현 | ⑸ 절 표 |

**환경**: macOS Darwin 25.5.0 · zsh · git (`--no-optional-locks`) · ★**`grep` = ugrep shim**(= `grep -r` 은 gitignore 인지 · 본 판은 **명시 file 목록 / `git ls-files` 파이프**만 써서 분모를 고정) · 시간대 KST.

---

## ⒀ Negative space

고려했으나 hot 제외 영역:

- **절 이름 정합 16 즉시 실행** — 제외. 절 이동·번호 재배열이 11 신설과 섞이면 판정 층과 정합 층이 한 commit 에 들어간다(A3).
- **보호 2 강행 + §14a·manifest 동기** — 제외. propagation 없이 sha 만 바꾸면 **본 판이 drift 를 만든다**(S8 이 막으려던 바로 그 오독을 실재로 만듦).
- **소급 entry 를 file 전 계보로 확대** — 제외. T2 가 「판정 근거 한정」으로 상한을 정했다(무한 소급 금지).
- **`## Migration 이력` 16 을 canonical 로 rename** — 제외(= ⓑ 회부).
- **`.claude/agents/**`·`.claude/skills/**` 로 의무 분모 확대** — 제외. `:3` 이 정한 분모는 두 디렉터리다. 분모 확대 = 규약 개정(§2 scope 밖).
- **과거 REPORT 2 본의 `169/167` 정정** — 제외. 과거 판 산출물 = **그때의 값** · 소급 정정 금지.

---

## ⒁ 판단·선택 → 원장

1. ★**「29 가 의무를 어긴다」를 그대로 집행하지 않았다.** 분모를 **뜻**으로 다시 재서 16 을 걷어내고, 보호 2 를 회부하고, **11 만** 신설했다. — **자를 이름에 고정하면 분모가 3 배 부푼다**(19 ↔ 35).
2. ★**발주 전제를 반증하고도 발주 결론을 유지했다.** §0-ⓕ 의 「빈 절」 시나리오는 **0 건**이었지만 「전량 신설 금지」는 **다른 근거로** 옳았다. 틀린 근거를 승계시키지 않기 위해 **둘 다 적었다**.
3. ★**게이트가 나를 2 회 잡았고 2 회 다 「설명이니까 괜찮다」였다.** G7 이 라벨 **인용**을 **부여**로 셌다 — 자를 탓하지 않고 문면을 고쳤다(실서수 호칭으로 전환). 같은 축의 3 번째가 G8 이다.
4. ★**G8 둘째 칸을 0 으로 만들지 않았다.** 0 을 만들려면 다른 판의 전이 기록을 소급 정정해야 했다. **게이트 값보다 삭제 0 원칙이 위**라고 판단하고, 병기 + REPORT 소명으로 닫았다.
5. ★**삭제 축을 계약값(0) 이 아니라 자의 값(8) 으로 적었다.** 소명이 있으면 값을 각색할 유혹이 생기는데, **자를 고정한 것은 발주**이므로 값은 그대로 두고 소명을 붙였다.
6. **헤딩을 file 자기 번호에 맞췄다** — T2 의 문자열(`bare`)보다 **실측 관례**(깊이 2 · 번호 계승)가 정본(S11).

---

*REPORT 끝 · `MASTER-RULE-HISTORY-SECTION-001` · 2026-08-30 KST · master 단독 · push 0 · propagation 0*
