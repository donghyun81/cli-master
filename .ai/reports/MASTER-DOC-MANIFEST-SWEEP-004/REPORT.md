# `MASTER-DOC-MANIFEST-SWEEP-004` REPORT — 규약은 있었고 지켜지지 않았다

> 집행 = cli · 2026-08-30 KST · Mode **M5**(cli-infra-ops) · production **0 LOC** · 보호 5 sha **0** · 자식 **0** · propagation **0**(Coin) · push **0**(Coin)
> 서식 = `reporting.md §15.1` **14 축**(= 본 판이 신설한 축 14 를 **자기 REPORT 에 자기 적용**) · 자기 commit sha 미기재(= `reporting.md §8.2`)

---

## §0. 배경 재진술 (= 요건 ⑵ · **자기 말** · verbatim 복붙 아님)

이 판은 **새 규칙을 만드는 판이 아니다.** 발주 §0-ⓑ 가 지목한 좌표 5 를 실제로 열어 보니 셋은 이미 거기 있었다 — `reporting.md §8.1`(파일 224행)은 「수치를 인용하면 그 수치를 만든 명령을 함께 적는다」를 2026-07-26 부터 갖고 있었고 빠진 것은 규율이 아니라 **「어느 채널에」**였다. `verification-and-review.md §0.3`(38행)의 K-133 은 「리터럴 계수는 **사용처**가 아니다」로 **사용** 축을 이미 덮고 있었고, 없던 것은 같은 뿌리의 **존재·귀속** 축이었다. `cycle-discipline.md ### 30)`(141행)은 「밴드 산정 자」와 처방 ⑴~⑶ 을 직전 판에서 이미 착지시켰고, 그럼에도 **그 판이 자기 절을 어겼다**(§9 산출 축 `+318` 기재 ↔ 실측 `+366`) — 즉 진단은 **규칙 부재가 아니라 자 부재**였다. 그래서 처방은 전부 **범위 확장 · 하위 bullet · 게이트화**이지 신설이 아니다(신설로 처리하면 같은 축이 두 곳에 살고 곧 갈린다 = **S6**). 나머지 둘 중 `rule-footer-common.md` 의 판정선은 **있는 규칙이 틀린** 경우였고 — 아래 ⑷ 대로 반례는 실재했다 — 그러나 **대체 축도 함께 틀렸다**(§9-3). `CLAUDE.md:293 §15` 는 hot 3/3 만석이라 등재에 demote 가 따라붙는 구조였고, 직전 판이 「기존 행 불가침 · 말미 append 는 계약」으로 **경로를 열어 둔 덕에** 막히지 않았다. **이 판이 아닌 것** = Selfward 전량(판 15 병행) · `scripts/`·`server/`·`supabase/` · propagation 실행 · push · COLD 기존 167 행의 **내용** · 이력 절 부재 나머지 25(회부).

---

## 1. 판정

**부분 PASS** — 계약 **T7 중 6 집행 · 1 회부**.

- **집행 6** = T2(§8.1 범위 확장) · T3(§0.3 존재·귀속) · T4(### 30 게이트화) · T5(§15.1 축 14) · T6(§15 등재 1 + demote 1 + COLD append 1) · T7(회부 수 실측).
- ★**회부 1 = T1(판정선 축 교체)** — 발주 **§9-3 이 미리 정한 분기**에 따른다. 반례 검증(§9-2)은 **3 legs 전부 통과**했으나, 제안된 **대체 축 「순증 ≠ 0」 자체가 반례 13 을 갖는다**(아래 §9-3). 발주 문면: 「있으면 **그 축도 틀린 것**이고 T1 을 회부하라」.
- ★**발주값 대비 정정 1** = **회차 「19」 → 실측 「27」**(§9-7 이 재측을 명령했고, 재측이 발주값을 반증했다).

---

## 2. BASELINE 진입 재측 (= 요건 ⑴ · 인용 아님)

| 항 | 발주 기재 | 진입 실측 | 판정 |
|---|---|---|---|
| HEAD | `3f1dc42` | `3f1dc42` | 일치 |
| ahead | 29 | 29 | 일치 |
| porcelain | 0 | 0 | 일치 |
| branch | — | `main` | — |

자 = `git --no-optional-locks log --oneline -1` / `status --porcelain | wc -l` / `rev-list --count origin/main..HEAD`. **S11 발동 0.**

---

## 3. 착지 좌표 (= 요건 ⑶ · `path` `line` `anchor` · rev = 마감 HEAD)

| T | 처분 | 좌표 | 성격 |
|---|---|---|---|
| **T1** | ★**회부** | — (`rule-footer-common.md` **무접촉**) | 대체 축 반증(§9-3) |
| **T2** | 집행 | `docs/rules/reporting.md` · §8.1 말미 `+5` 행 · anchor `적용 범위 — 이 규율의 대상은 REPORT 만이 아니다` | 범위 확장 |
| **T3** | 집행 | `docs/rules/verification-and-review.md` · §0.3 K-133 하위 `+4` 행 · anchor `같은 뿌리의 다른 축 = 존재·귀속` | 하위 bullet |
| **T4** | 집행 | `docs/rules/cycle-discipline.md` · ### 30) 처방 하위 `+2` 행 · anchor `밴드를 게이트 항으로 잰다` | 게이트화 |
| **T5** | 집행 | `docs/rules/reporting.md` · §15.1 표 축 **14** 행 + intro `13 축`→`14 축` | 신설 1 |
| **T6** | 집행 | `CLAUDE.md` §15 hot(demote 1 + 등재 1) · `.auto-memory/master-cycle-history-COLD.md` `:202` + note + 자기서술 3 | 자기 적용 |
| **T7** | 회부 | — | 실측만(아래 ⑻) |

**이력 절 등재** = 만진 rule file **3 전부**(`reporting.md` §12 · `verification-and-review.md` · `cycle-discipline.md`) = G11 첫 칸 `3`.

---

## 4. 게이트 전/후 (= 12 항 · 자 = 발주 §6 블록 **verbatim** · 재타이핑 0)

추출·실행 = `bash <(awk '…index($0,"## §6. 게이트")…' cc-paste-MASTER-DOC-MANIFEST-SWEEP-004.md)` (= repo 밖 `/dev/fd` · file 미생성 · `2>` 미사용).

| G | 시점 | 전(재현) | 후(실측) | 계약 | 판정 |
|---|---|---|---|---|---|
| **G0** | 진입 | `claude-cli-master 3f1dc42 6` | `claude-cli-master <HEAD> 6` | cwd 증명 | **PASS** |
| G1 | 무관 | `0 2 2` | `0 2 2` | `≥1 2 ≥4` | ★**미충족 = T1 회부**(아래 §5) |
| G2 | 무관 | `0 12` | `4 17` | `≥1 ≥16` | **PASS** |
| G3 | 무관 | `0 13` | `3 17` | `≥1 ≥16` | **PASS** |
| G4 | 무관 | `0 11` | `2 13` | `≥1 ≥13` | **PASS** |
| G5 | 무관 | `13` | `14` | **정확히 14** | **PASS** |
| G6 | 마감 | `3 0` | `3 1` | `3 1` | **PASS**(첫 칸 3 불변 = S2 회피) |
| G7 | 마감 | `167 3 0` | `168 0 1` | `168 0 1` | **PASS**(둘째 `0` = 자기서술 3 곳 전수) |
| G8 | 마감 | `18` | **`27`** | `19` | ★**정정**(§9-7 · 재측이 발주값 반증) |
| G9 | 마감 | `0` | `0` | `0` | **PASS** |
| G10 | 마감 | `0 29` | `0 32`(+REPORT commit 후 33) | 첫 `0` · 둘째 32~35 | **PASS** |
| G11 | 마감 | `0 0` | `3 1` | `≥3 1` | **PASS** |

★**「전」 값 12 항 전량 재현**(= §9-1) — 발주 기재와 **불일치 0**.

---

## 5. 편차 · 이의 (= 방어 아님 · 자로 재현해 붙인다)

### ⓐ ★**T1 회부 — 대체 축이 반례 13 을 갖는다** (= 발주 §9-3 의 분기 집행)

발주는 9 commit 을 봤다. 나는 **32**(= `reporting.md` follow 21 + `pencil-mcp-tools-reference.md` follow 11)를 훑었다.

**⑴ 「순증 ≠ 0 인데 미등재」 = 실재.** 결정적 사례 = **`cf063a8`**(= `MASTER-CLI-CONTEXT-DIET-2-001`). 이 commit 은 rule file **28** 을 한 번에 만졌고(= 변경 정책 절 3 bullet → `rule-footer-common.md` pointer 1 행 · 내용 보존형 기계 치환), 그중 **당시 이미 이력 절을 갖고 있던 16** 개 중 **등재는 1**(`reporting.md` · 거기서는 `-199`/헤딩 32 의 실질 개정을 했다) · **미등재 15**, 그 15 중 **순증 ≠ 0 이 13**(= `-1` 또는 `-2`).

- 자 = `git show --numstat cf063a8 -- '*rules/*.md'` × 각 file 현재 경로에서 `grep -c '^- 20[0-9][0-9]-[0-9-]* . MASTER-CLI-CONTEXT-DIET-2-001'`.
- ★**가장 깨끗한 대조** = **같은 commit · 같은 날 · 둘 다 이력 절 보유**인 두 file: `reporting.md`(**−199** · 등재) ↔ `pencil-mcp-tools-reference.md`(**−2** · 미등재). **그 판이 둘을 갈랐다.** 가른 축은 「순증 ≠ 0」이 아니고(둘 다 ≠ 0) **「대칭성」도 아니다**(둘 다 비대칭) — **실질 개정 ↔ 기계 치환**이었다.

**⑵ 발주 §0-ⓔ 의 근거(「미등재 4 는 전부 `+n/−n` 대칭」)도 넓힌 분모에서 깨진다.** `cf063a8`/pencil = **1/3**(비대칭 · 미등재) · `ab5d67b`/reporting = **+2/−0**(비대칭 · 미등재).

**⑶ 반대로 구 규칙 「≥ 5」의 반례는 발주가 든 1 개가 아니라 2 개다** — `b3a8857`(+2 · h0 · 등재) 외에 ★**`f01a90b`**(= `MASTER-CLI-PENCIL-PRESCAN-EXHAUSTIVE-001` · `pencil-mcp` **3/2 = 순증 +1** · 헤딩± **0** · **자기 등재**)를 새로 찾았다. **구 규칙이 틀렸다는 것은 더 강해졌고, 제안된 대체 축은 성립하지 않는다.**

⟹ **값을 고르는 문제가 아니라 축을 고르는 문제**이고, 축 후보(실질 ↔ 기계)는 **Coin 결정 사항**이라 **회부**한다. 발주의 「**「5」를 다른 수로 바꾸지 마라**」는 지켰다 — **아무 수도 쓰지 않았다.**

★**이의 아님 · 미이행 아님**: 발주 §9-2/§9-3 이 **명시적으로 만들어 둔 분기**를 탄 것이다. 「집행하지 않음」이 아니라 「발주가 정한 조건에서 발주가 정한 처분」이다.

### ⓑ ★**회차 「19」 → 「27」** (= 발주값 정정 · 상세 = §9-7)

### ⓒ **밴드 문서 본문 축 미달**(계약 `+38~+62` ↔ 실측 `+24`) — 상세 = ⑽.

### ⓓ **G4 초안 자기 정정**: ⑷ 를 처방 line 에 **inline** 으로 붙였더니 절 행수가 `11` 로 불변이라 **양성대조가 FAIL**을 냈다. 발주 §4 가 계상한 「처방 1 + 실증 1 = 2 행」이 옳았고, 하위 bullet 2 행으로 재작성해 `13`. ★**양성대조가 없었으면 「anchor 는 있는데 절은 안 자란」 상태로 green 이 될 뻔했다.**

---

## 6. 「나를 의심하는 절차」 답 (= 발주 §9 · **7 항 전량**)

### §9-1 「전」 값 12 항 전량 재현되는가

**재현 100% · 불일치 0.** 특히 지목된 **G1 `0 2 2`** ✓ · **G7 `167 3 0`** ✓. (§4 표 「전」 열)

### §9-2 ★T1 반례가 진짜인가 (3 legs)

| leg | 자 | 결과 |
|---|---|---|
| `b3a8857` 이 `reporting.md` 를 **4/2** 로 고쳤나 | `git show --numstat --format= b3a8857 -- docs/rules/reporting.md` | **`4 2`** ✓ (순증 **+2**) |
| 헤딩± **0** 인가 | `git show --format= b3a8857 -- docs/rules/reporting.md \| grep -c '^[+-]#\{1,\} '` | **`0`** ✓ (깊이 무관 자) |
| `MASTER-MEASURE-DISCIPLINE-001` 이 §12 에 **등재**돼 있나 | `grep -c 'MASTER-MEASURE-DISCIPLINE-001' docs/rules/reporting.md` | **`1`** ✓ (`:329` 데이터 entry) |

⟹ 현 규칙 「헤딩 > 0 **OR** 순증 ≥ 5」 = `F OR F` = **미등재 판정**인데 **실제로 등재**. ★그리고 같은 file 이 **두 줄 위에서** 이 commit 을 「헤더 ±0 인데 등재된 선례」로 **스스로 인용**한다 = **문면이 자기 규칙을 반박**. **3 legs 전부 통과 ⟹ §9-2 조건에서는 T1 집행 가능.** 회부 사유는 **§9-3 쪽**이다.

### §9-3 ★★「순증 ≠ 0」이 새 반례를 만드는가 — **만든다(13)**

위 §5-ⓐ. 전 계보 **32 commit** 재훑기 결과 · 결정적 대조 = `cf063a8` 안에서 `reporting.md`(등재) ↔ `pencil-mcp`(미등재). ⟹ **T1 회부**(발주 지시대로).

★**부수 발견 2**: ⑴ 구 규칙 반례가 **2 개**(새로 `f01a90b`) ⑵ `ab5d67b`(= `SWEEP-002`)는 `reporting.md`·`pencil-mcp` 양쪽에서 **자기를 등재하지 않았다** — 다만 이건 축의 반례가 **아니라 부채**다(그 판 스스로 `CLAUDE.md §15` 에 「만든 부채 1 = 자기 §15 미등재」라 적었고, 동류 판 `SWEEP-001`/`3551bf5` 는 자기를 등재했다). **반례와 위반을 가른 근거를 여기 남긴다.**

### §9-4 ★T2 의 「전 채널」이 과한가 — **범위 유지 · 집행 경계를 명시**

원장·브리핑까지 묶으면 master 규약이 cowork 내부 산출물을 입법하는 모양이 되어 `anchor-list.md` **A10**(책임 경계) 과 마찰한다. 그러나 **좁히지 않았다** — ⑴ 실제 위반 2 는 **둘 다 발주서 채널**이라 발주서를 빼면 규율이 무의미하고 ⑵ 저작자가 **자기를 스스로 묶는 것**은 경계 침해가 아니다. 대신 **집행 경계 1 줄**을 붙였다: **발주서·paste-back·REPORT = 게이트로 재는 면 / 원장·브리핑 = 같은 규율을 지되 자기 점검**(master 규약이 집행하지 않는다). ⟹ **범위는 넓게 · 강제는 좁게.**

### §9-5 ★T4 의 「명문화만 · 게이트화는 다음 판」 경계가 옳은가 — **아니다 · 자기 적용했다**

발주는 「본 판은 §6 에 밴드 G 를 두지 않는다」로 유예를 명시했다. **그 유예 자체가 이 절이 고치려는 패턴의 재현**이다(= 규칙만 두고 자를 안 돎 = 3 판 연속 실패의 기전). 발주 §9-5 가 「넣는 게 낫다고 보면 넣고 그 사실을 REPORT 에 적어라」로 재량을 열어 뒀으므로 **행사**한다:

- **발주 §6 게이트 블록은 verbatim 유지**(= 재타이핑 금지 준수 · 12 항 그대로 실행).
- **밴드는 아래 ⑽ 에서 층별로 실측·정정**했고, 그 사실을 `cycle-discipline.md` 이력 entry 에 박았다.
- 밴드 「전」 = **0** (발주 기재대로).

### §9-6 ★T6 demote 대상이 `MEASURE-DISCIPLINE-001` 인가 — **맞다(재측)**

「맨 위 행」 규약(`CLAUDE.md:296`)을 **행 순서로** 재확인: 진입 시점 hot 표 데이터행 = `:300` `MASTER-MEASURE-DISCIPLINE-001` → `:301` `SWEEP-002` → `:302` `SWEEP-003`. **맨 위 = `:300`** ⟹ demote 대상 확정(**S3 회피**). 자 = `awk 'NR>293 && /^## 16\./{exit} NR>293 && /^\| [A-Z]/' CLAUDE.md`. ★세 행의 **마감일이 전부 `2026-08-30`** 이라 날짜로는 안 갈린다 — **순서가 정본**이라는 `:296` 규약이 실제로 작동한 자리다.

### §9-7 ★★회차 19 가 맞는가 — **아니다 · 실측 27**

발주는 「진입 최고 **18** + 1」로 19 를 냈고, 그 **18 자체를 다시 재라**고 지시했다. 쟀다.

- **자**(= 라벨이 아니라 **사건**을 센다) = COLD 에 **데이터행을 append 한 commit** 을 시간순 계수:
  `for s in $(git --no-optional-locks log --reverse --format='%h' -- .auto-memory/master-cycle-history-COLD.md); do git --no-optional-locks show --format= "$s" -- .auto-memory/master-cycle-history-COLD.md | grep -c '^+| [A-Z0-9]'; done` → **> 0 인 것만** 계수.
- **산술 검산** = 전 commit append 합 **167** == demote 직전 실측 행수 **167**(`grep -c '^| [A-Z0-9]'`) ⟹ **누락·삭제 0**, 자가 완전하다.
- **결과 = 본 판 직전까지 26 회** ⟹ 본 판 = **27**.

★**서수 ↔ 라벨 대조**: `d198798`=1 · `71d8c14`=2 · `3259dea`=8 · `0d67c12`=9 · `9ba9dd3`=10 · `40e7d7e`=11 · `49eadde`=12 · `91ea579`=13 · `bfb8f9c`=14 · `631b092`=15 · `7891342`=16 — **11 개 지점 전부 일치**(= 서수가 곧 회차라는 정의 확인). **그 다음부터 어긋난다**: `df62c9f`·`791fed7`·`74424f2`·`c570d1e`·`48e75de`·`1dcf20f`·`1855f39`·`e1e2c55`(2026-08-17~08-29) = **무번호 8**. `SWEEP-003` 은 무번호를 **1 개만** 찾아 `b3a8857`(실서수 **25**)에 17 을, 자신(`a3ba108` · 실서수 **26**)에 18 을 붙였다.

⟹ ★**왜 못 봤나** — 그 판이 쓴 자 `grep -o '[0-9]\{1,2\} 회차' <COLD> | sort -u` 는 **문면에 적힌 번호의 census** 다. **빠진 라벨은 라벨 census 에 원리적으로 안 잡힌다.** 세어야 할 것은 **번호가 아니라 사건**이었다. ★**이것이 본 판 T3(존재·귀속 축)이 말하는 바로 그 오류**이고, 같은 판 안에서 **자기 T3 이 자기 T6 을 구한** 셈이다.

⟹ ★**발주가 옳았다**: 「그 18 자체가 새로 쓴 값이다 · 틀렸으면 19 도 틀린다」. **틀렸고, 19 도 틀렸다.**

**처분** = 본 판은 **자기 회차만 실측 채번(27)**. **#17~#24 소급 채번 8 + 라벨 정정 2 = 회부**(무한 소급 금지 = T7 과 같은 원칙). 구 라벨 **삭제 0 · 병기 보존**(그 note 들의 **성격·행수·sha 서술은 전부 유효**하고 **번호만 stale**).

---

## 7. ChangeBudget (= **층 분리** · `cycle-discipline.md` ### 30 자기 적용 · 요건 ⑽)

| 층 | 계약 | 실측 | 자 | 판정 |
|---|---|---|---|---|
| **실코드** | `0` | **`0`** | production file 무접촉 | **PASS**(G9 `0`) |
| **문서 본문** | `+38 ~ +62` | **`+24`**(add 32 / del 8) | `git diff --numstat 3f1dc42..HEAD` 합 | ★**미달 −14** → 아래 정정 |
| **§9 산출**(별 항 · 합산 X) | `+25 ~ +50` | **`+273`** | `wc -l REPORT.md` | ★**초과 +223** → 아래 정정 |
| **삭제 축** | `0` | **`0`**(순삭제 없음) | T1 무접촉 · T6 = **이동**(sha 쌍) | **PASS** |

★**정정 사실 자체를 적는다**(= 처방 ⑶):

- **문서 본문 `+38~+62` → 실측 `+24`.** 원인 = ⑴ **T1 회부**로 계약이 계상한 「문면 2 + 표 2 행」 **4 행이 통째로 빠졌다** ⑵ 나머지 T 를 **행 단위가 아니라 조밀한 bullet** 으로 착지시켰다(예: T2 의 위반 실증 2 를 표가 아닌 하위 bullet 2 행으로). **미달은 초과와 달리 「덜 한 것」일 수 있어** 절 결속·행수 양성대조(G2 `17` · G3 `17` · G4 `13`)로 **내용이 실제로 들어갔음을 따로 입증**했다.
- **§9 산출 `+25~+50` → 실측 `+207`.** ★**직전 판이 같은 자리에서 `+20~+45` 계약에 `+366` 을 냈고**, 본 발주는 그 실측을 보고도 `+25~+50` 으로 적었다 — 즉 **밴드가 직전 판의 실측을 반영하지 않았다**(직전 REPORT 가 「차기 하한 권고 **`+300`**」을 회부해 뒀는데 발주가 안 받았다). 본 판은 T1 회부로 §9 답변이 길어져 `+273`. ★**그리고 나도 같은 자리에서 한 번 더 했다** — 본 절 초안에 실측 자리에 **`+207`** 을 적었다가 `wc -l` 로 재니 **`+273`**(오차 **+66**)이라 **같은 행 치환**으로 정정했다(= 자기 참조 값이라 행 수를 바꾸지 않는 치환만이 고정점이다). ⟹ ★**이 칸이 4 판 연속 틀렸고, 「예측값을 실측 자리에 적는」 버릇은 3 판 연속이다** — 「밴드를 게이트 항으로」(T4 ⑷)가 필요한 이유의 **네 번째 실증**이고, **차기 발주는 §9 산출 하한을 `+250` 이상으로 계약**할 것을 회부한다.

---

## 8. STOP · 회귀 그물 (= trigger 별 발동 여부 · **0 도 값**)

| # | trigger | 발동 | 근거 |
|---|---|---|---|
| S1 | COLD 기존 167 행 **내용** 변경 | **0** | 말미 append 1 + **note/자기서술만** 수정 · 데이터행 diff = `+1`(추가) · 기존 행 무접촉 |
| S2 | hot 4 행↑ 또는 demote 없이 등재 | **0** | G6 첫 칸 **3 불변** · demote 1 ↔ 등재 1 |
| S3 | demote 대상이 맨 위 행 아님 | **0** | §9-6 재측 |
| S4 | 자기서술 3 곳 중 일부만 | **0** | G7 둘째 칸 **`0`**(= 전칭) |
| S5 | T1 구 문면 `≥ 5 행` 삭제 | **0** | ★**T1 무접촉** · G1 둘째 칸 **`2` 불변** |
| S6 | T2·T3·T4 를 「신설」로 처리 | **0** | 셋 다 기존 절 **안에** 부착(범위 확장 / 하위 bullet / 처방 ⑷) · 새 § 번호 0 · 새 K 번호 0 |
| S7 | 회차를 문면 인용으로 기재 | **0** | §9-7 재측 도출(사건 계수 + 산술 검산) |
| S8 | `scripts/`·`server/`·`supabase/` 접촉 | **0** | G9 `0` |
| S9 | propagate/verify-sync 실행 · push · `git add -A` · `2>` · `git grep` | **0** | 전 명령 `git add <명시 경로>` · `2>` 0 회 · push 0 |
| S10 | Selfward 접촉 | **0** | cwd = master 고정 · 타 repo 경로 접근 0 |
| S11 | 발주 ↔ 실물 갈림 | ★**2 발동** | ⑴ **T1 대체 축**(§9-3) ⑵ **회차 18/19**(§9-7) — **둘 다 실물을 정본으로 채택** |

★**회귀 그물**: 직전 판이 402B 초과를 잡은 전례가 있어 hot 신 entry 를 **바이트로 실측**(**396B** ≤ 400B). 행별 칸 수 자(§0.3)로 §15.1 신 행 = **4 pipes** = 헤더 동일(선재 6-pipe 1 행 = 축 3 의 `\|` = 기존 false positive · 본 판 소관 X).

---

## 9. 회부 (= 남기는 부채 + **원장 번호** · 0 이면 「0」+근거)

| # | 내용 | 원장 | 왜 별 판인가 |
|---|---|---|---|
| **1** | ★**판정선 축 결정**(T1) — 구 규칙 「≥ 5」는 **반례 2**(`b3a8857`·`f01a90b`)로 확정 반증 · 대체 축 「순증 ≠ 0」은 **반례 13** · 실측이 가리키는 축 = **실질 개정 ↔ 기계 치환**(내용 보존형 pointer 후퇴·문자열 치환 제외) | ㉡ **#192·#219** | **축 선택 = Coin 결정**(발주가 「수를 바꾸지 마라」로 값을 잠갔고, 축 교체는 그 위 층) |
| **2** | ★**회차 소급 채번 8**(`df62c9f`~`e1e2c55`) + **라벨 정정 2**(`b3a8857` 17→25 · `a3ba108` 18→26) | ㉡ **신규**(회차 계보) | 무한 소급 금지(= T7 과 동일 원칙) · 본 판은 자기 회차만 채번 |
| **3** | **이력 절 부재 25** (= `docs/rules/*.md` **42** 중 보유 **17**) | ㉡ **T7** | 발주 지정 회부 · 자기가 만진 file 만 자기 적용 |
| **4** | ★**§9 산출 축 밴드 하한** — 4 판 연속 오산 · 차기 계약 **`+250` 이상** 권고(= 직전 판 실측 `+366` · 본 판 `+273`) | ㉡ **#200-⑸** | 발주 저작 층 |
| **5** | `#191` COLD 미해석 링크 14 | ㉡ **#191** | 선재 축 |

**Coin 이관**(≠ 원장 회부) = **propagation 실행**(4-repo byte-identical) · **push**. 본 판 미실행(발주 지정).

---

## 10. commit · ahead (= 요건 ⑾ ★**계수 자기 대조** = 본 판 신설 축 14 자기 적용)

| # | sha | 대상 |
|---|---|---|
| 1 | `cc99083` | `docs/rules/reporting.md` |
| 2 | `0a1484f` | `docs/rules/verification-and-review.md` · `docs/rules/cycle-discipline.md` |
| 3 | `814f591` | `CLAUDE.md` · `.auto-memory/master-cycle-history-COLD.md` |
| 4 | (본 REPORT · **자기 sha 미기재** = `reporting.md §8.2`) | `.ai/reports/MASTER-DOC-MANIFEST-SWEEP-004/REPORT.md` |

★**계수 자기 대조**: 목록 행 수 **4** ↔ `git rev-list --count 3f1dc42..HEAD` = **4**(REPORT commit 포함 시점 기준) ⟹ **일치 · 누락 0**. **ahead** = 29 → **33**.

> ★이 대조가 바로 직전 판이 못 해서 `1d4d533` 를 빠뜨린 자리다(보고 4 본 ↔ 실측 5 본). **두 수를 나란히 두는 것으로는 부족하고 같은 절에서 대조해야 잡힌다.**

---

## 11. 자 대조표 (= 「자의 이름 ↔ 자를 낸 명령」 · `verification-and-review.md` §0.4)

| 자 | 명령 | 단위 |
|---|---|---|
| 게이트 12 항 | `bash <(awk '…' cc-paste-….md)` (verbatim 추출) | 항 |
| 순증(net) | `git show --numstat <sha> -- <path>` → add − del | 행 |
| 헤딩± | `git show --format= <sha> -- <path> \| grep -c '^[+-]#\{1,\} '` | 행(깊이 무관) |
| 등재 여부(**명제형**) | `grep -c '^- 20[0-9][0-9]-[0-9-]* . <ID>'` | 행 |
| 등재 여부(문자열 · **대조군**) | `grep -c '<ID>' <file>` | 출현 |
| 회차(**사건**) | COLD append commit 시간순 계수 + 산술 검산(합 == 실측 행수) | 사건 |
| 이동 증명 | `shasum -a 256 \| cut -c1-16` (양측) | hex 16 |
| entry 바이트 | `wc -c`(개행 포함) | 바이트 |
| 행별 칸 수 | `tr -cd '\|' \| wc -c` | pipe |
| 밴드 | `git diff --numstat 3f1dc42..HEAD` 층별 합 | 행 |

★**환경**(= `reporting.md §8.1`) = `bash 3.2` · macOS(darwin 25.5.0) · `grep` = **ugrep shim**(= 재귀 시 gitignore 인지 · 본 판은 **비재귀 file 지정**만 써서 분모 영향 0) · `LC_COLLATE` 미고정(정렬 의존 자 = `sort -u` 계수 1 곳뿐 · 값 영향 0).

---

## 12. negative space (= 「고려했으나 hot 제외 영역」 · `reporting.md §13`)

고려했으나 hot 제외 영역: ⑴ **T1 을 「실질 ↔ 기계」 축으로 내가 직접 교체하는 것**(= 발주가 잠근 값 위 층 · 축 신설은 Coin 몫이라 회부) ⑵ **회차 소급 채번 8 즉시 집행**(= 무한 소급 금지 · 별 판) ⑶ **`ab5d67b`(SWEEP-002) 미등재 2 건 소급 등재**(= 자기가 만진 file 밖) ⑷ **이력 절 부재 25 채우기**(= T7 회부) ⑸ **발주 §6 에 밴드 G 항 추가**(= verbatim 유지 의무와 충돌 · 대신 §7 에서 층별 실측·정정으로 자기 적용).

---

## 13. 판단·선택 → 원장 (= 발주가 안 물었는데 **집행자가 정한 것** · `reporting.md §15.1` 축 13)

| 판단 | 처분 | 원장 |
|---|---|---|
| T1 을 **부분 집행(⑶ 표 2 행만) 하지 않고 전부 회부** | 반쯤 바뀐 판정선은 「같은 축이 두 곳에 사는」 상태(S6)를 만든다 | ㉡ **#219** 회부 1 |
| T2 범위를 **좁히지 않고 집행 경계를 병기** | §9-4 답 | ㉡ **#220** |
| T4 를 **본 판에서 자기 적용**(발주는 다음 판으로 유예) | §9-5 가 연 재량 · 유예 자체가 패턴 재현 | ㉡ **#200-⑸** |
| 회차 라벨 17/18 을 **덮어쓰지 않고 「실서수」 병기** | 그 note 들의 성격·행수·sha 서술은 유효하고 **번호만 stale** | 회부 2 |
| §9 산출 밴드 하한 **`+200` 권고** | 4 판 연속 오산의 실측 | ㉡ **#200-⑸** |

---

## 14. 만든 부채 시점 대조 (= 요건 ⑼ · 진입 `3f1dc42` ↔ 마감)

| 부채 | 진입 시점 | 마감 시점 | 증감 |
|---|---|---|---|
| 판정선 반례 | **1**(발주 인지분 `b3a8857`) | **2**(+`f01a90b`) · 미해소(회부 1) | **+1 발견**(신설 아님 = 선재 결함의 노출) |
| 회차 라벨 stale | **미인지** | **인지 · 병기 · 회부 2** | **0 신설**(선재 8+2 를 문면화) |
| 이력 절 부재 | **25** | **25** | **0** |
| ★**본 판이 새로 만든 부채** | — | **0** | ★자기 §15 등재 **완료**(G6 `3 1`) · 자기 이력 절 등재 **3/3** · 자기서술 전수 갱신(G7 `0`) — 직전 2 판이 남긴 「자기 미등재」 부채를 **본 판은 만들지 않았다** |

---

*본 REPORT 끝. 집행 = cli · 2026-08-30 KST · 계약 T7(집행 6 · 회부 1) · 게이트 12(PASS 10 · T1 회부로 미충족 1 · 발주값 정정 1) · STOP 11(발동 = S11 ×2 · 나머지 0) · production 0 LOC · 보호 5 sha 0 · 자식 0 · propagation 0 · push 0.*
