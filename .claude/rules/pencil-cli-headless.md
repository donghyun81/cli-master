# Pencil CLI Headless Mode — thin pointer

> **본문 단일 SoT** = [`.claude/skills/pencil-cli/SKILL.md`](../skills/pencil-cli/SKILL.md)
>
> 본 file = thin pointer default (= `MASTER-CLI-SKILLS-MIGRATION-PHASE-1-001` 마감 default · 2026-05-26 default · L1-4 단일 SoT paradigm 정합 default · Anthropic Skills paradigm 정합 default · trigger 시점 lazy load default · token savings 정합 default).
>
> 본문 본질 = `.claude/skills/pencil-cli/SKILL.md` 측 SKILL.md body 단일 default (= 직전 본 file 측 264 lines 본문 본질 보존 default · skill body 측 frontmatter `name` + `description` + `paths` + `allowed-tools` 추가 default).
>
> 기존 pointer × N (= `CLAUDE.md` line 261 + line 312 + `pencil-automation.md` §13 본문 인용 등) = 본 file path 정합 default (= 무접촉 default · L1-4 단일 SoT paradigm 정합 default · SoT 위치만 rule body → skill body 이동 default · pointer × N file path 본질 보존 default).

---

## Migration 이력

- 2026-05-19 · `MASTER-CLI-PENCIL-OPTIMIZATION-001` · 본 rule 신설 default (= 264 lines · 12 section default · Pencil CLI 본질 + 인증 방식 + Agent mode + Interactive mode + Batch tasks.json + Save As 모달 회피 + desktop vs CLI headless 분기 + Model 선택 + STOP 조건 + CI/CD 통합 default)
- 2026-05-26 · `MASTER-CLI-SKILLS-MIGRATION-PHASE-1-001` · 본 rule body → `.claude/skills/pencil-cli/SKILL.md` 측 이동 default (= 옵션 A thin pointer paradigm 정합 default · 본문 본질 보존 default · 기존 pointer × N 무접촉 default · L1-4 단일 SoT paradigm 정합 default)
