# `MASTER-VERIFY-SYNC-MARKED-REF-AXIS-001` — REPORT

> 집행 = cli session · 2026-08-31 KST · cwd = `~/AndroidStudioProjects/claude-cli-master` · master 단독
> Mode = M5 (cli-infra-ops) · production 0 · 보호 5 sha 변동 0 · 자식 3 repo 무접촉
> 발주 = `cc-paste-MASTER-VERIFY-SYNC-MARKED-REF-AXIS-001.md` (272 행) · 형식 = `reporting.md §15.1` 14 축

---

## ⑴ BASELINE 재측 (★인용 아님 · 재측이 정본)

| 항 | 발주 §0-B | 재측 | 갈림 |
|---|---|---|---|
| master HEAD | `71eb015` · ahead 0 · porcelain 0 | 동일 | — |
| `verify-sync.sh` | 333 행 · `59cd9128` | 동일 | — |
| WARN 블록 / 분모 / 존재검증 | `:310~:326` / `:316` / `:317` | 동일 | — |
| RC · PASS · DRIFT · MISS | 0 · 23 · 0 · 0 | 동일 | — |
| 자기 출력 WARN path | 3 | 동일 3 | — |
| 인용 path uniq | 33 / 29 | **33 / 29** | — |
| 부재 path | 3 (전량 protected) | **3** (propagation 0) | — |
| **부재 인용 hit** | **8** (「행×path 축」) | ★**행 7 · 출현 8** | ★**갈림 1** |
| 마킹 없는 부재 | 0 | **0** (7/7 마킹) | — |
| 마커 실좌표 | 435 · 524 · 715 자 | ★**436 · 525 · 716** 자 / 545 · 595 · 839 byte | ★**갈림 2** |
| 4-repo footer | 46 행 `671ae5ec` ×4 | 동일 | — |
| `CLAUDE.md:301` | 432B | 432B | — |

**★갈림 1 — 「8」은 행 수가 아니라 출현 수다** (S10)

```
check-abbreviation.sh   lines=2  occurrences=2
abbreviation-policy.md  lines=3  occurrences=3
workflow-core.md        lines=2  occurrences=3   ← 갈리는 지점
```

`workflow-core.md` 는 **`:111` 한 행 안에서 두 번** 인용된다(① `rules × 2 보강` 열거 ② `위 인용 … = 현 경로`).
⟹ 행×path = **7** · 출현 = **8**. 값은 살아 있고 **축 이름이 틀렸다**. 자 = `grep -c -F`(행) vs `grep -o -F | wc -l`(출현).
★무해하지 않다 — 발주 §3 표 2 는 「행 축으로 재면 8 이 나오고 뜻이 다르다」고 경고하는데, **행 축으로 재도 8 이 아니라 7**이다. 경고문이 가리킨 대안 축의 값 자체가 틀렸다.

**★갈림 2 — 좌표 1-off + 단위 미표기.** 차 = 정확히 1(0-origin ↔ 1-origin) ⟹ **논지는 그대로 성립**(마커가 잘린 출력 밖 먼 지점에 산다). 단위 병기가 없어 byte 로 오독될 여지가 있었다 — `awk length()` 는 `LANG=C.UTF-8` 에서 **byte** 를 센다(⑿).

---

## ⑵ 배경 재진술

WARN 은 **문서 안 backtick path 문자열의 실재**를 센다. 원장 규약은 **삭제 0 · 구 문면 병기**(㉣ K-171)라 소멸·이동한 경로를 표시해서 **남긴다**. ⟹ **원리적 양립 불가** · 병기를 지키는 한 WARN 은 **영구히** 뜬다.

⑴ 상시 소음이 진짜 drift 를 덮는다(3 줄이 늘 뜨면 4 줄째를 못 본다). ⑵ 다음 판이 스크립트 자신의 지시(`→ 정정: 해당 .auto-memory 상태문서 본문 갱신`)를 따라 **K-171 을 위반하도록 유도**된다.

★해소는 문서가 아니라 **「자」 쪽**. 본 판은 상태문서 본문을 **한 글자도 고치지 않았다**(⑷ G6 · ⑻ S2).

---

## ⑶ 착지 좌표 (★`grep -n` 재측)

`scripts/verify-sync.sh` (333 → **370** 행):

| 좌표 | 내용 | T |
|---|---|---|
| `:310` | `# === 상태문서 부재 참조 WARN ===` (블록 시작 · 기존) | — |
| `:312` | `# T1: 「모든 인용 행이 마킹된 path」= 병기(K-171) → 분모에서 제외 (★path 축)` | T1 |
| `:313` | `STALE_REF_MARKERS='~~\|소멸\|이동\|구 경로\|폐기'` | T1 |
| `:314~:332` | T4 「왜」 주석 블록 (선택지 3 · 버린 이유 · 근거 좌표 · 19 행) | T4 |
| `:333` | `STATUS_DOCS=` (기존) | — |
| `:343` / `:344` | `ref_cited=$(grep -c -F …)` / `ref_marked=$(grep -F … \| grep -c -E …)` | T1 |
| `:345~:348` | `[ "$ref_cited" -eq "$ref_marked" ]` → `REF_MARKED++` · `continue` | T1 |
| `:355` / **`:356`** | `REF_LEFT=…` / **요약 `printf '[verify-sync] stale-ref: …'`** | **T2** |
| `:357~:363` | 기존 stderr WARN 블록 (**존치 · 무접촉**) · `:363` = 블록 끝 `fi` | — |
| `:365~` | exit code 절 (**무접촉** = S5) | — |

★**§9-⑹ 답 = 요약 printf `:356` 은 WARN 블록 `:310~:363` 안**이다(S5 무발동). 발주 §7-⑥ 은 「블록 안(`:310~:326`) 또는 직후」를 요구했고, 본 판 삽입으로 블록 끝이 `:326 → :363` 으로 밀렸으며 요약 행은 그 **안**이다.

- `docs/rules/stale-artifact-tracking.md` — `§2` 4 bullet (`:34~:37`) · `§9` 이력 entry (`:100`)
- `CLAUDE.md` — `§15` demote 1 + 등재 1 + lineage 행 1 갱신
- `.auto-memory/master-cycle-history-COLD.md` — `:238` demote 행 1 **verbatim** append

---

## ⑷ 게이트 10 전/후 (★awk 추출 15 행 `ab05397b` · 재타이핑 0 · verbatim 2 회)

| G | 전 (진입) | 발주 기대 「후」 | 후 (마감) | 판정 |
|---|---|---|---|---|
| G0 | `claude-cli-master 71eb015  333` | `<HEAD> 345~361` | `claude-cli-master fdf39ae  370` | ★밴드 밖 |
| G1 | `0` | `0` | `0` | ✓ |
| G2 | `0` | `1` | `1` | ✓ |
| G3 | (빈 문자열) | `분모=3 마킹제외=3 잔존=0` | `[verify-sync] stale-ref: 분모=3 마킹제외=3 잔존=0` | ✓ |
| G4 | `1` | `1` | `1` | ✓ |
| G5 | `0` | `≥1` | `2` | ✓ |
| G6 | `0` | `0` | **`1`** | ★갈림 |
| G7 | `0` | `0` | **`1`** | ★갈림 |
| G8 | `671ae5ec` ×4 | 불변 | `671ae5ec 671ae5ec 671ae5ec 671ae5ec` | ✓ (S7 0) |
| G9 | `0 0` | `0` · `3~5` | `0 4` | ✓ |

진입 = **10/10 일치**. 마감 = **7/10 · 3 갈림**. 셋 다 **게이트 계약 쪽 결함**이고 착지는 정상:

**★G0 — 발주가 두 축을 합산하지 않았다.** §4 는 `스크립트 축 +12~+28` 과 `주석 축 +10~+20` 을 **별 계상**(합산 X)으로 못 박았는데, G0 밴드 `345~361` = `333 + (12~28)` 로 **스크립트 축만** 더해 만들었다. T4 주석은 **같은 file 에 산다** ⟹ 총행 = `333 + 두 축` = `355~381`. 실측 370 = `333 + 18(순증) + 19`. **두 축 모두 밴드 안**이고 **G0 밴드만 산술이 불완전**하다.

**★G6·G7 — 게이트가 T5·§8.1 을 금지한다(발주 내부 모순).** 둘이 문 file = **`.auto-memory/master-cycle-history-COLD.md` 단 1**:

```
=== G7 이 문 file (범위 밖) ===
.auto-memory/master-cycle-history-COLD.md
```

★그 append 는 **T5 가 명령한 것**이다 — `CLAUDE.md §15` 상한 규약 = 「3 초과분 = **즉시 COLD demote**(verbatim append · advisory 대기 X · **마감 step 안에서 집행**)」이고 §4 도 `문서 본문 축` 에 **예산까지 잡았다**. ⟹ G6/G7 을 0 으로 만드는 유일한 길은 **demote 를 안 하는 것**이고 그것이 헌법 위반이다.

G6 의 **의도**는 S2(상태문서 **병기 삭제·경로 갱신**)이고 그 의도로는 **위반 0**이다 — 두 상태문서 **무접촉**(numstat 부재 · T3 원복 sha 동일). 자(`grep -F 'auto-memory/'`)가 **의도보다 넓다**: COLD 는 상태문서가 아니라 **이력 보존처**이고, 거기 append 는 삭제의 반대 = **K-171 을 집행하는 기전 그 자체**다. 같은 이유로 `.ai/reports/…`(§8.1 필수)도 G7 을 1 더 올린다 ⟹ REPORT commit 후 **G7 = 2** 예상.

**처분** = 게이트 계약 정정은 **본 판 scope 밖**(발주 문서 수정 권한 없음) ⟹ **⑽ 회부**.

---

## ⑸ ★T3 red 실행 출력 전문

★**대조군 자격 3 항 선검**(§9-⑸) — 자격 미달이면 red 는 아무것도 증명하지 않는다.

```
--- [1] 대조군 자격 검사 ---
  주입 path        = .claude/rules/zzz-t3-control-does-not-exist.md
  디스크 실재?      = ABSENT
  기존 3 과 교집합  = 0
  주입 행 마커 수   = 0  (전문: > T3 CONTROL (temporary): `.claude/rules/zzz-t3-control-does-not-exist.md`)
  주입 전 sha       = 82d23ed574747c0648598d3f3c694a3e1cf4474e4c3a285c8454a1ee174273d8
--- [2] 주입 후 RED 실행 ---

[verify-sync] ⚠ 상태문서 부재 참조 (= stale ref · drift 재발 신호):
  - .claude/rules/zzz-t3-control-does-not-exist.md (in propagation-status.md)
  → 정정: 해당 .auto-memory 상태문서 본문 갱신 (master cycle)

  RC = 0
  요약행 = [verify-sync] stale-ref: 분모=4 마킹제외=3 잔존=1
--- [3] 원복 ---
  원복 후 sha       = 82d23ed574747c0648598d3f3c694a3e1cf4474e4c3a285c8454a1ee174273d8
  sha 동일?         = YES
  porcelain         = []
--- [4] 원복 후 GREEN 재실행 ---
  RC = 0 · 요약행 = [verify-sync] stale-ref: 분모=3 마킹제외=3 잔존=0
```

**판정 = PASS.** 읽어야 할 것:

- 분모 `3→4` · 마킹제외 `3→3`(**안 늘었다** = 비마킹은 안 삼킨다) · 잔존 `0→1`.
- stderr WARN 재출현 · 뜬 것은 **주입 path 단 1** — 병기 3 은 계속 조용하다.
  ⟹ ★**「잔존 0」이 판정이 넓어서 나온 값이 아님을 이 한 줄이 증명한다**(= 발주 §6.2 가 「게이트가 `잔존=0` 을 냈다는 사실은 판정이 옳다의 증거가 아니다」로 경고한 지점 · **S1 무발동**).
- 원복 sha **동일** · porcelain **빈 문자열** ⟹ 상태문서 무접촉 증명(S2 0).
- 주입 행은 **순 ASCII** ⟹ 마커 5 종을 **구조적으로 포함할 수 없다**(마킹 0 이 우연이 아니다). 주입처를 `propagation-status.md`(부재 0 인 쪽)로 고른 이유 = 기존 3 과 **doc 까지 분리**해 「분모 +1 · 마킹제외 불변」이 한 doc 안 상호작용이 아님을 함께 보이기 위함.

---

## ⑹ §9 「나를 의심하는 절차」 7 항 답 (방어 0)

**⑴ 「전」 10 항 재현?** → 게이트 **10/10 재현**. 비게이트 항 **2 갈림**(⑴ 갈림 1·2). 실물이 정본(S10).

**⑵ ★「부재 3 이 전량 마킹」이 정말인가 — 행 전문으로 다시.** → **참. 단 자가 갈린다.** 인용 행 = **7**(발주 8 = 출현). 7 행 **전량** 마커 실재 · **마킹 없는 부재 0**.

| 행 | 인용 path | 마커 (byte 위치) |
|---|---|---|
| `:11` | check-abbreviation.sh | 소멸(55) |
| `:12` | abbreviation-policy.md | 소멸(65) |
| `:13` | workflow-core.md | 이동(54) · 소멸(64) |
| `:87` | abbreviation-policy.md | `~~`(3) · 소멸(85) · 폐기(111) |
| `:98` | check-abbreviation.sh | 소멸(545) |
| `:99` | abbreviation-policy.md | `~~`(3) · 소멸(34) · 폐기(60) |
| `:111` | workflow-core.md ×2 | 이동(595) · 소멸(839) · 구 경로(968) |

★**중간에 내 자가 한 번 틀렸다** — 1 차 census 에서 마커 집합을 **공백 split** 해 `구 경로` 가 `구`+`경로` 두 토큰으로 갈렸고, `:13`·`:98` 에 없는 `경로` hit 가 떴다. `|` 구분자로 재측해 정정(`:98` = 소멸 **단 1** · `:13` = 이동+소멸 2). **결론은 안 바뀌었으나 근거 표가 바뀌었다** — 적어 둔다.

semantic 도 확인: 7 행 전부 **진짜 병기**다(우연한 동거 아님). `:11~:13` = 병기 표 3 행(소멸/소멸/**이동**) · `:87`·`:99` = `~~취소선~~ (소멸)` · `:98` = 「★경로 부재 … 제거됐다」 · `:111` = 「★경로 이동 … 소멸이 아니라 이동이다」.

**⑶ ★축이 path 인가 행인가 — 왜 path 축인지 한 줄.** → 두 축 다 냈다: **path 3 · 행 7**(출현 8).
★한 줄: **현행 자가 `sort -u` 로 path 를 세고 WARN 도 path 단위로 보고하므로, 행 축으로 빼면 분모(path)와 뺄셈(행)의 단위가 갈려 숫자가 뜻을 잃는다.** (덧붙여 행 축은 S1 을 부른다 — 비마킹 행이 남아도 계수만 줄어 그 path 가 조용해진다.)

**⑷ ★마커 5 가 필요충분인가 — 다른 병기 어휘 census.** → **충분(현 데이터) · 필요는 5 중 1 뿐.** 정직하게 적는다.

*필요성* (1 개씩 빼서 재측):

| 뺀 마커 | 마킹제외 | 판정 |
|---|---|---|
| `~~` / `이동` / `구 경로` / `폐기` | 각 **3/3** | 현 데이터 **불필요** |
| **`소멸`** | **1/3** | ★**유일하게 일한다** |

`소멸` 이 **7 행 전량**을 덮는다. 빼면 `workflow-core.md` 만 `이동` 으로 생존(1/3). ⟹ 나머지 4 = **선행 커버리지**로 존치하고 **그 사실을 주석에 박았다**(다음 판이 「안 쓰는 마커」로 오독하고 지우면 소음이 돌아온다).

*뺀 어휘* (census · 단위 = 행 · 「추가 시 새로 마킹되는 행 수」):

| 후보 | protected | propagation | 처분 |
|---|---|---|---|
| 부재 | +4 | +6 | ★**기각** — 본 WARN 자신의 **증상어**("경로 부재"). 처분을 안 적고 **서술만** 해도 마킹 ⟹ 진짜 stale 을 삼킨다(S1). 넓힘 최대 |
| 제거 / 삭제 / 통합 / deprecated | +3 / +2 / +2 / +2 | +1 / +1 / +0 / +2 | 기각 — 그 cycle 이 **다른 file 에** 한 일 서술 |
| 은퇴 / 미사용 / 중단 | 0 | 0 | **부재** — 넣을 이유 0 |

**채택 기준** = 「그 path 의 부재가 **의도된 기록**임을 단언하는 어휘」이지 「무언가 없어졌다」가 아니다. 현행 5 가 무는 행 = protected **17/150** · propagation **3/198**(좁다).

**⑸ ★T3 대조군이 진짜 대조군인가.** → 교집합 **0** ✓ · 마킹 **0** ✓(순 ASCII 구조적 보장) · 원복 sha **동일** ✓ · porcelain 빈 문자열 ✓ (⑸ 전문).

**⑹ ★요약 행이 WARN 블록 밖으로 나갔는가.** → **안이다.** 블록 `:310~:363` · 요약 `:356` · exit 절 `:365~` 무접촉. S5 무발동.

**⑺ ★`$?` 를 파이프 뒤에서 읽었는가 (K-181).** → **안 읽었다.** 게이트는 원문 그대로 `bash $V … > "$O"; RC=$?`(파이프 0). 구현·검증 중 RC 를 읽은 자리 **전량**(T1/T2/T4 · T3 red · T3 green)도 `> file; RC=$?` 형. 스크립트 안 `ref_marked=$(… | grep -c …)` 는 **출력값**을 받는 자리이고 그 뒤 `$?` 를 판정에 쓰지 않았다(`set -uo pipefail` 하에서도 `set -e` 부재라 흐름 영향 0).

---

## ⑺ ChangeBudget 층 분리 (★단위 = 행 · 합산 X)

| 축 | 계약 | 실측 | 자 | 판정 |
|---|---|---|---|---|
| 스크립트 | `+12~+28` | **+19 / −1** (순증 18) | `92f007e`(+14/−1) + `b11d94f`(+5/−0) | ✓ |
| 주석 | `+10~+20` | **+19 / −0** | `22b2887` | ✓ |
| 문서 본문 | `+4~+10` | **+8 / −2** | `fdf39ae`(CLAUDE.md +2/−2 · COLD +1 · rule +5) | ✓ |
| §9 산출 | `+180~+320` | 본 REPORT(`wc -l`) | 별 commit | ✓ |
| 삭제 | `0` | ★해석 = **준수** | 아래 | ✓ |

★**삭제 축 「0」의 실측 해석**: 계약 문면 = 「상태문서 병기도, 기존 stderr WARN 블록도 지우지 않는다」 ⟹ **둘 다 0** ✓(상태문서 numstat 부재 · WARN 블록 `:357~:363` 무접촉). numstat 음수는 전부 **계약이 지시한 치환·demote** 다 — `−1`(스크립트) = `[ -e … ] || REF_MISSING=…` → `[ -e … ] && continue` **행 치환** · `−2`(CLAUDE.md) = demote 로 빠진 행 1(**T5 가 명령**) + lineage 행 치환 1.

★**본 REPORT 자체가 1 회 밴드를 넘었다** — 초판 **413 행**(밴드 +320 초과 93). 발행 전 재작성해 밴드 안으로 넣었다. 값을 재고 나서야 알았고, **재지 않았으면 넘긴 채 냈다**.

---

## ⑻ STOP 10 (★0 도 값)

| # | trigger | 발동 | 근거 |
|---|---|---|---|
| **S1** | 마킹 없는 부재를 분모에서 뺌 | **0** | T3 red = 마킹 0 주입 → **잔존 1 · WARN 재출현** · 마킹제외 3 에서 **안 늘었다** |
| **S2** | 상태문서 본문 수정 | **0** | 두 상태문서 numstat **부재** · T3 원복 sha 동일 · porcelain 빈 문자열 |
| **S3** | 마킹 판정을 행 축으로 구현 | **0** | `:343~:348` = path 당 `ref_cited == ref_marked` 전량 비교 |
| **S4** | red 미실행인데 「확인」이라 적음 | **0** | ⑸ 출력 전문 + RC + sha 2 개 병기 |
| **S5** | WARN 블록 밖 접촉 | **0** | 착지 전량 `:310~:363` 안 · exit 절 `:365~` 무접촉 |
| **S6** | `propagate.sh` 실행/수정 · 자식 접촉 | **0** | 변경 file 4 = 전부 master · propagate 호출 0 |
| **S7** | 4-repo footer sha 변동 | **0** | G8 진입·마감 `671ae5ec` ×4 **불변** |
| **S8** | push · `git add -A` · `2>` · `git grep` · `/tmp` · 맨몸 `git status` | **0** | push 0 · `git add` 명시 경로만 · `2>` 0(stderr 는 리다이렉트 없이 읽음) · `git grep` 0 · `/tmp` 0 · status 전량 `--no-optional-locks --porcelain` |
| **S9** | 게이트 앵커에 백슬래시 · `\b` · `.`/`.*` | **0** | 게이트 = awk 추출 `ab05397b` **verbatim · 수정 0** |
| **S10** | 발주 ↔ 실물 갈림 | ★**5 건 · 전량 기록** | 갈림 1(hit 8→7) · 갈림 2(좌표 1-off) · G0 밴드 · G6/G7 · demote 대상(아래) |

★**S10-⑤ demote 대상 선정**: 발주 §1-T5 는 `:301`(432B)을 「demote 후보」로 적었다. 그러나 `CLAUDE.md §15` **「방향」 조항**(2026-08-29 `MASTER-ENGINEERING-BASELINE-002`)은 **「demote 대상 = 표의 맨 위 행 · 순서가 정본」**을 못 박고, 그 조항의 사유가 **「마감일이 같아 날짜로 안 갈리는 경우」**다. 본 판 3 행 = **전부 2026-08-30 동일** ⟹ 정확히 그 경우. ⟹ **맨 위 행 `MASTER-REGISTER-AXIS-REPLACE-001` 을 demote**(헌법 우선). `:301` = **미처리 존치**(⑼).

---

## ⑼ 만든 부채 (시점 대조)

| # | 부채 | 시점 | 왜 지금 안 닫나 |
|---|---|---|---|
| 1 | §15 hot 첫 행 **432B** 상한 위반 1 건 존치 (구 `:301` → 현 `:300`) | **진입 이전부터** 실재(발주 §0-B 가 이미 적음) | 발주 §2 = 「demote 후보로만 · **단독 처리 금지**」. 다음 판 demote 로 자연 해소 |
| 2 | 게이트 G6/G7 이 **T5 demote + §8.1 REPORT 를 금지** | 본 판이 **발견**(진입 시엔 0 이라 안 보였다) | 발주서 수정 권한 = cowork ⟹ ⑽ 회부 |
| 3 | G0 「후」 밴드가 **주석 축 미합산** | 본 판이 발견 | 동일 ⟹ ⑽ 회부 |
| 4 | 신 계약(`STALE_REF_MARKERS` · path 축) **4-repo 전파 미착지** | 본 판 = master 단독(발주 §2) | **후행 판**(§0-ⓒ 「propagation 이 아니다」) |
| 5 | 마커 4 가 **현 데이터에서 무증상** | 본 판이 계측 | 선행 커버리지로 **의도적 존치** · 주석에 사유 박음 |

★**본 판이 만든 새 부채 = 0.** 1 = 선재 · 2·3 = **발견**(생산 아님) · 4 = 설계상 후행 · 5 = 의도.

---

## ⑽ 회부 (+ 원장)

| # | 회부 항 | 대상 |
|---|---|---|
| 1 | **게이트 계약 정정** — G6 자를 **상태문서 2 file 한정**으로 좁히고, G7 허용 prefix 에 `.auto-memory/master-cycle-history-COLD.md` + `.ai/reports/` 추가. 현행대로면 **모든 §15 마감 판이 자기 게이트를 못 통과한다** | cowork |
| 2 | **G0 밴드 산식** — `총행 = 333 + 스크립트축 + 주석축`(별 계상 원칙과 정합) | cowork |
| 3 | ★**「hit 8」 축 이름** — 「행×path 축」 → **「출현 축」**(행 축은 7). §3 표 2 의 「행 축으로 재면 8」도 함께 | cowork (㉡ #272 정정) |
| 4 | **4-repo 전파** — `verify-sync.sh` + `stale-artifact-tracking.md`(cli infra 권장 byte-identical) | 후행 판 (Coin 소관) |
| 5 | **`:300`(432B) 상한 위반** — 다음 demote 판에서 자연 해소 확인 | 다음 §15 마감 판 |
| 6 | ★**「소멸 ↔ 이동」 분리 보고** — `protected-file-hashes.md:15` 가 「WARN 은 부재 하나로만 보고하므로 **자가 그 차이를 못 낸다**」고 적은 미해결 지적. 본 판 계약 밖 | 후속 후보 |

★**원장**: ㉡ **#261**(원 발견) **닫음** · **#272** 근거 사용 + **축 이름 1 항 정정 회부** · **#274**(Coin 안 A) **1 번 판 이행 완료** · ㉣ **K-171**(병기 보존 = S2 0) · **K-175**(게이트 무수정) · **K-181**(파이프 뒤 `$?` 0) 전량 준수.

---

## ⑾ commit 계수 자기 대조

계획 §8 = **4 본** · 실제 = **5 본** (★+1 = REPORT 분리)

```
92f007e  fix(cli-infra): verify-sync stale-ref 분모에서 병기 마킹 path 제외 (T1)
b11d94f  feat(cli-infra): verify-sync stale-ref stdout 요약 1 행 (T2)
22b2887  docs(cli-infra): 마커 집합 「왜」 + 선택지 3 · 버린 이유 2 (T4)
fdf39ae  docs(cli): §15 등재 1 + demote 1 + rule 이력 entry (T5)
<본 commit>  docs(cli): REPORT (14 축)
```

★**분리 사유**: §8 은 commit 4 = 「T5 + REPORT」로 묶었으나 REPORT ⑷ 는 **마감 게이트 값**을 담아야 하고 그 값은 T5 가 **commit 된 뒤에만** 존재한다(G0/G6/G7/G9 전부 `71eb015..HEAD` **커밋** 비교). 묶으면 REPORT 는 **자기가 만들기 전의 게이트 값**을 적게 된다 ⟹ 분리했다. **G9 둘째 칸 계약 `3~5` 안**(마감 4 → REPORT 후 5) ⟹ 계약 위반 0. push **0** · propagation **0**.

---

## ⑿ 자 대조표 + 환경

| 명제 (§3) | 자 | 단위 | 실측 | 판정 |
|---|---|---|---|---|
| 1. 부재 path = 3 | 스크립트 자기 출력 요약 행 · 분모 = 두 문서 **합집합** | path | **3** (3+0) | ✓ |
| 2. 그 3 이 전량 마킹 | path 의 **모든** 인용 행에 마커 | path | **3/3** (행 7/7) | ✓ |
| 3. 마커가 전문에 산다 | 행 **전체**를 읽는다 | 문자 위치 | 436·525·716 자 / 545·595·839 byte | ✓ (1-off 정정) |
| 4. 진짜 stale 은 여전히 뜬다 | T3 주입 red 출력 | 사건 | **잔존=1 · WARN 1 행** | ✓ |
| 5. DRIFT 0 불변 | 4-repo footer sha 4 | file | `671ae5ec` ×4 (진입·마감) | ✓ |

```
OS      Darwin 25.5.0 (arm64-apple-darwin25)
bash    GNU bash 3.2.57(1)-release        ← set -uo pipefail · declare -A 불가
grep    ugrep 7.5.0 (shim · /usr/bin/grep 아님)
awk     awk version 20200816 (BWK)        ← ★length() 가 LANG=C.UTF-8 에서 byte 를 센다
shasum  6.02 · locale LANG=C.UTF-8 · LC_ALL 미설정 · KST 2026-08-31 01:2x
```

★**awk 단위가 갈림 2 의 원인**이다 — `length($0)`=825 인데 `wc -m`=601. 좌표엔 **단위를 병기**해야 한다. 시크릿 grep(11 패턴) = 본 REPORT **0 match**.

---

## ⒀ negative space

**고려했으나 hot 제외**: ① `:300`(432B) **단독 정정** — §2 「단독 처리 금지」 ⟹ 제외 ② 마커 집합 `repo-config.sh` **외부화** — 소비처 1 · 조기 추상화 ⟹ 제외 ③ WARN 을 **exit code 에 반영**(잔존>0 = exit 1) — S5 정면 위반 ⟹ 제외 · 별 판 의제 ④ **「소멸 ↔ 이동」 분리 보고** — 계약 밖 ⟹ ⑽-6 회부 ⑤ 상태문서를 `STATUS_DOCS` 에서 **제외** — 감시 축소 = 선택지 ⓑ 와 동형 ⟹ **기각** ⑥ `STALE-DEBT.md` **대장 등재** — 본 판 발견은 「문면↔실물 갈림」이 아니라 **자의 결함**이라 그 축이 아니다 ⟹ §2 판정 자 개정으로 갈음.

---

## ⒁ 판단·선택 → 원장

1. ★**착지점을 path 축으로 고정** — 행 축은 분모(path)와 뺄셈(행)의 **단위를 갈라** 숫자의 뜻을 죽인다. (→ ㉣ 신 K 후보: **「뺄셈은 분모와 같은 단위로 한다」**)
2. ★**마커 5 중 4 가 무증상임을 숨기지 않고 주석에 박았다** — 「안 쓰는 마커」로 오독되어 삭제되면 소음이 그대로 돌아온다. **무증상 ≠ 불필요**이고, 그 구별은 **적어 두지 않으면 다음 판이 못 한다**.
3. ★**「부재」를 마커에서 뺀 것이 이 판의 진짜 판단이다** — 넣어도 잔존은 지금도 0 이라 **아무 차이가 안 보이지만**, 그 순간부터 「처분을 안 적고 부재만 서술한 행」이 전부 면죄부가 된다. **지금 안 보이는 넓힘이 나중에 진짜 stale 을 삼킨다.**
4. ★**demote 에서 발주(`:301`)가 아니라 헌법(맨 위 행)을 따랐다** — §15 「방향」 조항은 **마감일 동률**을 위해 만들어졌고 본 판이 정확히 그 경우다. 발주의 괄호 제안보다 **그 조항이 상위**다.
5. ★**게이트가 계약을 금지하는 모순을 발견했고, 게이트를 고치지 않고 회부했다** — 게이트를 손대면 그 순간 **「자기 게이트를 통과하도록 자를 고친 판」**이 된다(= 이 트랙이 고치는 병의 재생산). 갈림은 기록하고 통과 여부는 정직하게 **3 갈림**으로 남긴다.
6. ★**cowork 의 「8」을 확인하러 갔다가 축 이름이 틀린 것을 찾았다** — 값(8)은 살아 있고 **축(행×path)이 틀렸다**. 발주 §3 이 「행 축으로 재면 8」이라 적은 문장은 **행 축으로 재면 7** 이므로 **그 경고문 자체가 반증된다**. (→ ㉣ 「값이 맞다고 축이 맞는 건 아니다」 = `MASTER-MEASURE-DISCIPLINE-001` **「자를 썼다는 것은 옳은 자라는 증거가 아니다」**의 인접 형)
7. ★**내 자도 한 번 틀렸다**(⑹-⑵ 공백 split) — 결론은 안 바뀌었으나 **근거 표가 바뀌었다**. 결론이 같다는 이유로 안 적으면, 다음 판은 **틀린 자를 물려받는다**.

---

*REPORT 끝 · 집행 = cli session · 2026-08-31 KST · 재측이 정본.*
